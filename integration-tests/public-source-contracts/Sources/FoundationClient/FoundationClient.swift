import NucleusFoundation

public func makeFoundationValues() -> (Color, Rect) {
    (
        Color(0.25, 0.5, 0.75, 1),
        Rect(origin: Point(x: 2, y: 3), size: Size(width: 40, height: 20))
    )
}
