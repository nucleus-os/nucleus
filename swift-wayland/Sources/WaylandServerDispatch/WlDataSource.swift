// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for wl_data_source: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

import WaylandServerC
import WaylandServer

public protocol WlDataSourceRequests: AnyObject {
    func offer(_ resource: UnsafeMutablePointer<wl_resource>, mime_type: UnsafePointer<CChar>?)
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
    func setActions(_ resource: UnsafeMutablePointer<wl_resource>, dnd_actions: UInt32)
}

public extension WlDataSourceRequests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum WlDataSourceServer {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_wl_data_source_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_wl_data_source_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_wl_data_source_requests.self, capacity: 1)
        vt.pointee.offer = offer_impl
        vt.pointee.destroy = destroy_impl
        vt.pointee.set_actions = setActions_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendTarget(_ target: UnsafeMutablePointer<wl_resource>, mime_type: UnsafePointer<CChar>?) {
        wl_data_source_send_target(target, mime_type)
    }
    public static func sendSend(_ target: UnsafeMutablePointer<wl_resource>, mime_type: UnsafePointer<CChar>?, fd: Int32) {
        wl_data_source_send_send(target, mime_type, fd)
    }
    public static func sendCancelled(_ target: UnsafeMutablePointer<wl_resource>) {
        wl_data_source_send_cancelled(target)
    }
    public static func sendDndDropPerformed(_ target: UnsafeMutablePointer<wl_resource>) {
        wl_data_source_send_dnd_drop_performed(target)
    }
    public static func sendDndFinished(_ target: UnsafeMutablePointer<wl_resource>) {
        wl_data_source_send_dnd_finished(target)
    }
    public static func sendAction(_ target: UnsafeMutablePointer<wl_resource>, dnd_action: UInt32) {
        wl_data_source_send_action(target, dnd_action)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> WlDataSourceRequests? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? WlDataSourceRequests
    }

    private static let offer_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafePointer<CChar>?) -> Void = { _, res, mime_type in
        guard let res, let h = handler(res) else { return }
        h.offer(res, mime_type: mime_type)
    }
    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
    private static let setActions_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { _, res, dnd_actions in
        guard let res, let h = handler(res) else { return }
        h.setActions(res, dnd_actions: dnd_actions)
    }
}
