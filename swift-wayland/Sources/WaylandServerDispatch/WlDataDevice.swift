// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for wl_data_device: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

import WaylandServerC
import WaylandServer

public protocol WlDataDeviceRequests: AnyObject {
    func startDrag(_ resource: UnsafeMutablePointer<wl_resource>, source: UnsafeMutablePointer<wl_resource>?, origin: UnsafeMutablePointer<wl_resource>?, icon: UnsafeMutablePointer<wl_resource>?, serial: UInt32)
    func setSelection(_ resource: UnsafeMutablePointer<wl_resource>, source: UnsafeMutablePointer<wl_resource>?, serial: UInt32)
    func release(_ resource: UnsafeMutablePointer<wl_resource>)
}

public extension WlDataDeviceRequests {
    func release(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum WlDataDeviceServer {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_wl_data_device_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_wl_data_device_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_wl_data_device_requests.self, capacity: 1)
        vt.pointee.start_drag = startDrag_impl
        vt.pointee.set_selection = setSelection_impl
        vt.pointee.release = release_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendDataOffer(_ target: UnsafeMutablePointer<wl_resource>, id: UnsafeMutablePointer<wl_resource>?) {
        wl_data_device_send_data_offer(target, id)
    }
    public static func sendEnter(_ target: UnsafeMutablePointer<wl_resource>, serial: UInt32, surface: UnsafeMutablePointer<wl_resource>?, x: Double, y: Double, id: UnsafeMutablePointer<wl_resource>?) {
        wl_data_device_send_enter(target, serial, surface, swift_wayland_fixed_from_double(x), swift_wayland_fixed_from_double(y), id)
    }
    public static func sendLeave(_ target: UnsafeMutablePointer<wl_resource>) {
        wl_data_device_send_leave(target)
    }
    public static func sendMotion(_ target: UnsafeMutablePointer<wl_resource>, time: UInt32, x: Double, y: Double) {
        wl_data_device_send_motion(target, time, swift_wayland_fixed_from_double(x), swift_wayland_fixed_from_double(y))
    }
    public static func sendDrop(_ target: UnsafeMutablePointer<wl_resource>) {
        wl_data_device_send_drop(target)
    }
    public static func sendSelection(_ target: UnsafeMutablePointer<wl_resource>, id: UnsafeMutablePointer<wl_resource>?) {
        wl_data_device_send_selection(target, id)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> WlDataDeviceRequests? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? WlDataDeviceRequests
    }

    private static let startDrag_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { _, res, source, origin, icon, serial in
        guard let res, let h = handler(res) else { return }
        h.startDrag(res, source: source, origin: origin, icon: icon, serial: serial)
    }
    private static let setSelection_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { _, res, source, serial in
        guard let res, let h = handler(res) else { return }
        h.setSelection(res, source: source, serial: serial)
    }
    private static let release_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.release(res) } else { wl_resource_destroy(res) }
    }
}
