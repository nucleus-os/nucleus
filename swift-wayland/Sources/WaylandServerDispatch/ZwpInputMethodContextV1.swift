// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for zwp_input_method_context_v1: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

public import WaylandServerC
public import WaylandServer

public protocol ZwpInputMethodContextV1Requests: AnyObject {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
    func commitString(_ resource: UnsafeMutablePointer<wl_resource>, serial: UInt32, text: UnsafePointer<CChar>?)
    func preeditString(_ resource: UnsafeMutablePointer<wl_resource>, serial: UInt32, text: UnsafePointer<CChar>?, commit: UnsafePointer<CChar>?)
    func preeditStyling(_ resource: UnsafeMutablePointer<wl_resource>, index: UInt32, length: UInt32, style: UInt32)
    func preeditCursor(_ resource: UnsafeMutablePointer<wl_resource>, index: Int32)
    func deleteSurroundingText(_ resource: UnsafeMutablePointer<wl_resource>, index: Int32, length: UInt32)
    func cursorPosition(_ resource: UnsafeMutablePointer<wl_resource>, index: Int32, anchor: Int32)
    func modifiersMap(_ resource: UnsafeMutablePointer<wl_resource>, map: UnsafeMutablePointer<wl_array>?)
    func keysym(_ resource: UnsafeMutablePointer<wl_resource>, serial: UInt32, time: UInt32, sym: UInt32, state: UInt32, modifiers: UInt32)
    func grabKeyboard(_ resource: UnsafeMutablePointer<wl_resource>, keyboard: WlNewId)
    func key(_ resource: UnsafeMutablePointer<wl_resource>, serial: UInt32, time: UInt32, key: UInt32, state: UInt32)
    func modifiers(_ resource: UnsafeMutablePointer<wl_resource>, serial: UInt32, mods_depressed: UInt32, mods_latched: UInt32, mods_locked: UInt32, group: UInt32)
    func language(_ resource: UnsafeMutablePointer<wl_resource>, serial: UInt32, language: UnsafePointer<CChar>?)
    func textDirection(_ resource: UnsafeMutablePointer<wl_resource>, serial: UInt32, direction: UInt32)
}

public extension ZwpInputMethodContextV1Requests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum ZwpInputMethodContextV1Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_zwp_input_method_context_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_zwp_input_method_context_v1_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_zwp_input_method_context_v1_requests.self, capacity: 1)
        vt.pointee.destroy = destroy_impl
        vt.pointee.commit_string = commitString_impl
        vt.pointee.preedit_string = preeditString_impl
        vt.pointee.preedit_styling = preeditStyling_impl
        vt.pointee.preedit_cursor = preeditCursor_impl
        vt.pointee.delete_surrounding_text = deleteSurroundingText_impl
        vt.pointee.cursor_position = cursorPosition_impl
        vt.pointee.modifiers_map = modifiersMap_impl
        vt.pointee.keysym = keysym_impl
        vt.pointee.grab_keyboard = grabKeyboard_impl
        vt.pointee.key = key_impl
        vt.pointee.modifiers = modifiers_impl
        vt.pointee.language = language_impl
        vt.pointee.text_direction = textDirection_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendSurroundingText(_ target: UnsafeMutablePointer<wl_resource>, text: UnsafePointer<CChar>?, cursor: UInt32, anchor: UInt32) {
        zwp_input_method_context_v1_send_surrounding_text(target, text, cursor, anchor)
    }
    public static func sendReset(_ target: UnsafeMutablePointer<wl_resource>) {
        zwp_input_method_context_v1_send_reset(target)
    }
    public static func sendContentType(_ target: UnsafeMutablePointer<wl_resource>, hint: UInt32, purpose: UInt32) {
        zwp_input_method_context_v1_send_content_type(target, hint, purpose)
    }
    public static func sendInvokeAction(_ target: UnsafeMutablePointer<wl_resource>, button: UInt32, index: UInt32) {
        zwp_input_method_context_v1_send_invoke_action(target, button, index)
    }
    public static func sendCommitState(_ target: UnsafeMutablePointer<wl_resource>, serial: UInt32) {
        zwp_input_method_context_v1_send_commit_state(target, serial)
    }
    public static func sendPreferredLanguage(_ target: UnsafeMutablePointer<wl_resource>, language: UnsafePointer<CChar>?) {
        zwp_input_method_context_v1_send_preferred_language(target, language)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> (any ZwpInputMethodContextV1Requests)? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? any ZwpInputMethodContextV1Requests
    }

    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
    private static let commitString_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UnsafePointer<CChar>?) -> Void = { _, res, serial, text in
        guard let res, let h = handler(res) else { return }
        h.commitString(res, serial: serial, text: text)
    }
    private static let preeditString_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UnsafePointer<CChar>?, UnsafePointer<CChar>?) -> Void = { _, res, serial, text, commit in
        guard let res, let h = handler(res) else { return }
        h.preeditString(res, serial: serial, text: text, commit: commit)
    }
    private static let preeditStyling_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UInt32, UInt32) -> Void = { _, res, index, length, style in
        guard let res, let h = handler(res) else { return }
        h.preeditStyling(res, index: index, length: length, style: style)
    }
    private static let preeditCursor_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, Int32) -> Void = { _, res, index in
        guard let res, let h = handler(res) else { return }
        h.preeditCursor(res, index: index)
    }
    private static let deleteSurroundingText_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, Int32, UInt32) -> Void = { _, res, index, length in
        guard let res, let h = handler(res) else { return }
        h.deleteSurroundingText(res, index: index, length: length)
    }
    private static let cursorPosition_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, Int32, Int32) -> Void = { _, res, index, anchor in
        guard let res, let h = handler(res) else { return }
        h.cursorPosition(res, index: index, anchor: anchor)
    }
    private static let modifiersMap_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_array>?) -> Void = { _, res, map in
        guard let res, let h = handler(res) else { return }
        h.modifiersMap(res, map: map)
    }
    private static let keysym_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UInt32, UInt32, UInt32, UInt32) -> Void = { _, res, serial, time, sym, state, modifiers in
        guard let res, let h = handler(res) else { return }
        h.keysym(res, serial: serial, time: time, sym: sym, state: state, modifiers: modifiers)
    }
    private static let grabKeyboard_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { client, res, keyboard in
        guard let res, let client, let h = handler(res) else { return }
        h.grabKeyboard(res, keyboard: WlNewId(client: client, id: keyboard, version: Swift::min(wl_resource_get_version(res), Int32(10)), interface: swift_wayland_iface_wl_keyboard()))
    }
    private static let key_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UInt32, UInt32, UInt32) -> Void = { _, res, serial, time, key, state in
        guard let res, let h = handler(res) else { return }
        h.key(res, serial: serial, time: time, key: key, state: state)
    }
    private static let modifiers_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UInt32, UInt32, UInt32, UInt32) -> Void = { _, res, serial, mods_depressed, mods_latched, mods_locked, group in
        guard let res, let h = handler(res) else { return }
        h.modifiers(res, serial: serial, mods_depressed: mods_depressed, mods_latched: mods_latched, mods_locked: mods_locked, group: group)
    }
    private static let language_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UnsafePointer<CChar>?) -> Void = { _, res, serial, language in
        guard let res, let h = handler(res) else { return }
        h.language(res, serial: serial, language: language)
    }
    private static let textDirection_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UInt32) -> Void = { _, res, serial, direction in
        guard let res, let h = handler(res) else { return }
        h.textDirection(res, serial: serial, direction: direction)
    }
}
