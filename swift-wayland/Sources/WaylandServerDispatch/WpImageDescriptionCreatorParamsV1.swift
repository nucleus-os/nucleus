// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for wp_image_description_creator_params_v1: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

import WaylandServerC
import WaylandServer

public protocol WpImageDescriptionCreatorParamsV1Requests: AnyObject {
    func create(_ resource: UnsafeMutablePointer<wl_resource>, image_description: WlNewId)
    func setTfNamed(_ resource: UnsafeMutablePointer<wl_resource>, tf: UInt32)
    func setTfPower(_ resource: UnsafeMutablePointer<wl_resource>, eexp: UInt32)
    func setPrimariesNamed(_ resource: UnsafeMutablePointer<wl_resource>, primaries: UInt32)
    func setPrimaries(_ resource: UnsafeMutablePointer<wl_resource>, r_x: Int32, r_y: Int32, g_x: Int32, g_y: Int32, b_x: Int32, b_y: Int32, w_x: Int32, w_y: Int32)
    func setLuminances(_ resource: UnsafeMutablePointer<wl_resource>, min_lum: UInt32, max_lum: UInt32, reference_lum: UInt32)
    func setMasteringDisplayPrimaries(_ resource: UnsafeMutablePointer<wl_resource>, r_x: Int32, r_y: Int32, g_x: Int32, g_y: Int32, b_x: Int32, b_y: Int32, w_x: Int32, w_y: Int32)
    func setMasteringLuminance(_ resource: UnsafeMutablePointer<wl_resource>, min_lum: UInt32, max_lum: UInt32)
    func setMaxCll(_ resource: UnsafeMutablePointer<wl_resource>, max_cll: UInt32)
    func setMaxFall(_ resource: UnsafeMutablePointer<wl_resource>, max_fall: UInt32)
}

public enum WpImageDescriptionCreatorParamsV1Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_wp_image_description_creator_params_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_wp_image_description_creator_params_v1_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_wp_image_description_creator_params_v1_requests.self, capacity: 1)
        vt.pointee.create = create_impl
        vt.pointee.set_tf_named = setTfNamed_impl
        vt.pointee.set_tf_power = setTfPower_impl
        vt.pointee.set_primaries_named = setPrimariesNamed_impl
        vt.pointee.set_primaries = setPrimaries_impl
        vt.pointee.set_luminances = setLuminances_impl
        vt.pointee.set_mastering_display_primaries = setMasteringDisplayPrimaries_impl
        vt.pointee.set_mastering_luminance = setMasteringLuminance_impl
        vt.pointee.set_max_cll = setMaxCll_impl
        vt.pointee.set_max_fall = setMaxFall_impl
        return UnsafeRawPointer(raw)
    }()

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> WpImageDescriptionCreatorParamsV1Requests? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? WpImageDescriptionCreatorParamsV1Requests
    }

    private static let create_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { client, res, image_description in
        guard let res, let client, let h = handler(res) else { return }
        h.create(res, image_description: WlNewId(client: client, id: image_description, version: Swift.min(wl_resource_get_version(res), Int32(2)), interface: swift_wayland_iface_wp_image_description_v1()))
        wl_resource_destroy(res)
    }
    private static let setTfNamed_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { _, res, tf in
        guard let res, let h = handler(res) else { return }
        h.setTfNamed(res, tf: tf)
    }
    private static let setTfPower_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { _, res, eexp in
        guard let res, let h = handler(res) else { return }
        h.setTfPower(res, eexp: eexp)
    }
    private static let setPrimariesNamed_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { _, res, primaries in
        guard let res, let h = handler(res) else { return }
        h.setPrimariesNamed(res, primaries: primaries)
    }
    private static let setPrimaries_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, Int32, Int32, Int32, Int32, Int32, Int32, Int32, Int32) -> Void = { _, res, r_x, r_y, g_x, g_y, b_x, b_y, w_x, w_y in
        guard let res, let h = handler(res) else { return }
        h.setPrimaries(res, r_x: r_x, r_y: r_y, g_x: g_x, g_y: g_y, b_x: b_x, b_y: b_y, w_x: w_x, w_y: w_y)
    }
    private static let setLuminances_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UInt32, UInt32) -> Void = { _, res, min_lum, max_lum, reference_lum in
        guard let res, let h = handler(res) else { return }
        h.setLuminances(res, min_lum: min_lum, max_lum: max_lum, reference_lum: reference_lum)
    }
    private static let setMasteringDisplayPrimaries_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, Int32, Int32, Int32, Int32, Int32, Int32, Int32, Int32) -> Void = { _, res, r_x, r_y, g_x, g_y, b_x, b_y, w_x, w_y in
        guard let res, let h = handler(res) else { return }
        h.setMasteringDisplayPrimaries(res, r_x: r_x, r_y: r_y, g_x: g_x, g_y: g_y, b_x: b_x, b_y: b_y, w_x: w_x, w_y: w_y)
    }
    private static let setMasteringLuminance_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UInt32) -> Void = { _, res, min_lum, max_lum in
        guard let res, let h = handler(res) else { return }
        h.setMasteringLuminance(res, min_lum: min_lum, max_lum: max_lum)
    }
    private static let setMaxCll_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { _, res, max_cll in
        guard let res, let h = handler(res) else { return }
        h.setMaxCll(res, max_cll: max_cll)
    }
    private static let setMaxFall_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { _, res, max_fall in
        guard let res, let h = handler(res) else { return }
        h.setMaxFall(res, max_fall: max_fall)
    }
}
