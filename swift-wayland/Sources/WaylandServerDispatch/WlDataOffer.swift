// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for wl_data_offer: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

public import WaylandServerC
import WaylandServer

public protocol WlDataOfferRequests: AnyObject {
    func accept(_ resource: UnsafeMutablePointer<wl_resource>, serial: UInt32, mime_type: UnsafePointer<CChar>?)
    func receive(_ resource: UnsafeMutablePointer<wl_resource>, mime_type: UnsafePointer<CChar>?, fd: Int32)
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
    func finish(_ resource: UnsafeMutablePointer<wl_resource>)
    func setActions(_ resource: UnsafeMutablePointer<wl_resource>, dnd_actions: UInt32, preferred_action: UInt32)
}

public extension WlDataOfferRequests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum WlDataOfferServer {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_wl_data_offer_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_wl_data_offer_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_wl_data_offer_requests.self, capacity: 1)
        vt.pointee.accept = accept_impl
        vt.pointee.receive = receive_impl
        vt.pointee.destroy = destroy_impl
        vt.pointee.finish = finish_impl
        vt.pointee.set_actions = setActions_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendOffer(_ target: UnsafeMutablePointer<wl_resource>, mime_type: UnsafePointer<CChar>?) {
        wl_data_offer_send_offer(target, mime_type)
    }
    public static func sendSourceActions(_ target: UnsafeMutablePointer<wl_resource>, source_actions: UInt32) {
        wl_data_offer_send_source_actions(target, source_actions)
    }
    public static func sendAction(_ target: UnsafeMutablePointer<wl_resource>, dnd_action: UInt32) {
        wl_data_offer_send_action(target, dnd_action)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> (any WlDataOfferRequests)? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? any WlDataOfferRequests
    }

    private static let accept_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UnsafePointer<CChar>?) -> Void = { _, res, serial, mime_type in
        guard let res, let h = handler(res) else { return }
        h.accept(res, serial: serial, mime_type: mime_type)
    }
    private static let receive_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafePointer<CChar>?, Int32) -> Void = { _, res, mime_type, fd in
        guard let res, let h = handler(res) else { return }
        h.receive(res, mime_type: mime_type, fd: fd)
    }
    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
    private static let finish_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res, let h = handler(res) else { return }
        h.finish(res)
    }
    private static let setActions_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UInt32) -> Void = { _, res, dnd_actions, preferred_action in
        guard let res, let h = handler(res) else { return }
        h.setActions(res, dnd_actions: dnd_actions, preferred_action: preferred_action)
    }
}
