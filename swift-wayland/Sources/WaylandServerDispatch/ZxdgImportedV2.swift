// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for zxdg_imported_v2: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

import WaylandServerC
import WaylandServer

public protocol ZxdgImportedV2Requests: AnyObject {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
    func setParentOf(_ resource: UnsafeMutablePointer<wl_resource>, surface: UnsafeMutablePointer<wl_resource>?)
}

public extension ZxdgImportedV2Requests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum ZxdgImportedV2Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_zxdg_imported_v2_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_zxdg_imported_v2_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_zxdg_imported_v2_requests.self, capacity: 1)
        vt.pointee.destroy = destroy_impl
        vt.pointee.set_parent_of = setParentOf_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendDestroyed(_ target: UnsafeMutablePointer<wl_resource>) {
        zxdg_imported_v2_send_destroyed(target)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> ZxdgImportedV2Requests? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? ZxdgImportedV2Requests
    }

    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
    private static let setParentOf_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res, surface in
        guard let res, let h = handler(res) else { return }
        h.setParentOf(res, surface: surface)
    }
}
