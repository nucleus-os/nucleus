// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for zwp_text_input_v1: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

import WaylandServerC
import WaylandServer

public protocol ZwpTextInputV1Requests: AnyObject {
    func activate(_ resource: UnsafeMutablePointer<wl_resource>, seat: UnsafeMutablePointer<wl_resource>?, surface: UnsafeMutablePointer<wl_resource>?)
    func deactivate(_ resource: UnsafeMutablePointer<wl_resource>, seat: UnsafeMutablePointer<wl_resource>?)
    func showInputPanel(_ resource: UnsafeMutablePointer<wl_resource>)
    func hideInputPanel(_ resource: UnsafeMutablePointer<wl_resource>)
    func reset(_ resource: UnsafeMutablePointer<wl_resource>)
    func setSurroundingText(_ resource: UnsafeMutablePointer<wl_resource>, text: UnsafePointer<CChar>?, cursor: UInt32, anchor: UInt32)
    func setContentType(_ resource: UnsafeMutablePointer<wl_resource>, hint: UInt32, purpose: UInt32)
    func setCursorRectangle(_ resource: UnsafeMutablePointer<wl_resource>, x: Int32, y: Int32, width: Int32, height: Int32)
    func setPreferredLanguage(_ resource: UnsafeMutablePointer<wl_resource>, language: UnsafePointer<CChar>?)
    func commitState(_ resource: UnsafeMutablePointer<wl_resource>, serial: UInt32)
    func invokeAction(_ resource: UnsafeMutablePointer<wl_resource>, button: UInt32, index: UInt32)
}

public enum ZwpTextInputV1Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_zwp_text_input_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_zwp_text_input_v1_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_zwp_text_input_v1_requests.self, capacity: 1)
        vt.pointee.activate = activate_impl
        vt.pointee.deactivate = deactivate_impl
        vt.pointee.show_input_panel = showInputPanel_impl
        vt.pointee.hide_input_panel = hideInputPanel_impl
        vt.pointee.reset = reset_impl
        vt.pointee.set_surrounding_text = setSurroundingText_impl
        vt.pointee.set_content_type = setContentType_impl
        vt.pointee.set_cursor_rectangle = setCursorRectangle_impl
        vt.pointee.set_preferred_language = setPreferredLanguage_impl
        vt.pointee.commit_state = commitState_impl
        vt.pointee.invoke_action = invokeAction_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendEnter(_ target: UnsafeMutablePointer<wl_resource>, surface: UnsafeMutablePointer<wl_resource>?) {
        zwp_text_input_v1_send_enter(target, surface)
    }
    public static func sendLeave(_ target: UnsafeMutablePointer<wl_resource>) {
        zwp_text_input_v1_send_leave(target)
    }
    public static func sendModifiersMap(_ target: UnsafeMutablePointer<wl_resource>, map: UnsafeMutablePointer<wl_array>?) {
        zwp_text_input_v1_send_modifiers_map(target, map)
    }
    public static func sendInputPanelState(_ target: UnsafeMutablePointer<wl_resource>, state: UInt32) {
        zwp_text_input_v1_send_input_panel_state(target, state)
    }
    public static func sendPreeditString(_ target: UnsafeMutablePointer<wl_resource>, serial: UInt32, text: UnsafePointer<CChar>?, commit: UnsafePointer<CChar>?) {
        zwp_text_input_v1_send_preedit_string(target, serial, text, commit)
    }
    public static func sendPreeditStyling(_ target: UnsafeMutablePointer<wl_resource>, index: UInt32, length: UInt32, style: UInt32) {
        zwp_text_input_v1_send_preedit_styling(target, index, length, style)
    }
    public static func sendPreeditCursor(_ target: UnsafeMutablePointer<wl_resource>, index: Int32) {
        zwp_text_input_v1_send_preedit_cursor(target, index)
    }
    public static func sendCommitString(_ target: UnsafeMutablePointer<wl_resource>, serial: UInt32, text: UnsafePointer<CChar>?) {
        zwp_text_input_v1_send_commit_string(target, serial, text)
    }
    public static func sendCursorPosition(_ target: UnsafeMutablePointer<wl_resource>, index: Int32, anchor: Int32) {
        zwp_text_input_v1_send_cursor_position(target, index, anchor)
    }
    public static func sendDeleteSurroundingText(_ target: UnsafeMutablePointer<wl_resource>, index: Int32, length: UInt32) {
        zwp_text_input_v1_send_delete_surrounding_text(target, index, length)
    }
    public static func sendKeysym(_ target: UnsafeMutablePointer<wl_resource>, serial: UInt32, time: UInt32, sym: UInt32, state: UInt32, modifiers: UInt32) {
        zwp_text_input_v1_send_keysym(target, serial, time, sym, state, modifiers)
    }
    public static func sendLanguage(_ target: UnsafeMutablePointer<wl_resource>, serial: UInt32, language: UnsafePointer<CChar>?) {
        zwp_text_input_v1_send_language(target, serial, language)
    }
    public static func sendTextDirection(_ target: UnsafeMutablePointer<wl_resource>, serial: UInt32, direction: UInt32) {
        zwp_text_input_v1_send_text_direction(target, serial, direction)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> ZwpTextInputV1Requests? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? ZwpTextInputV1Requests
    }

    private static let activate_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res, seat, surface in
        guard let res, let h = handler(res) else { return }
        h.activate(res, seat: seat, surface: surface)
    }
    private static let deactivate_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res, seat in
        guard let res, let h = handler(res) else { return }
        h.deactivate(res, seat: seat)
    }
    private static let showInputPanel_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res, let h = handler(res) else { return }
        h.showInputPanel(res)
    }
    private static let hideInputPanel_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res, let h = handler(res) else { return }
        h.hideInputPanel(res)
    }
    private static let reset_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res, let h = handler(res) else { return }
        h.reset(res)
    }
    private static let setSurroundingText_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafePointer<CChar>?, UInt32, UInt32) -> Void = { _, res, text, cursor, anchor in
        guard let res, let h = handler(res) else { return }
        h.setSurroundingText(res, text: text, cursor: cursor, anchor: anchor)
    }
    private static let setContentType_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UInt32) -> Void = { _, res, hint, purpose in
        guard let res, let h = handler(res) else { return }
        h.setContentType(res, hint: hint, purpose: purpose)
    }
    private static let setCursorRectangle_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, Int32, Int32, Int32, Int32) -> Void = { _, res, x, y, width, height in
        guard let res, let h = handler(res) else { return }
        h.setCursorRectangle(res, x: x, y: y, width: width, height: height)
    }
    private static let setPreferredLanguage_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafePointer<CChar>?) -> Void = { _, res, language in
        guard let res, let h = handler(res) else { return }
        h.setPreferredLanguage(res, language: language)
    }
    private static let commitState_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { _, res, serial in
        guard let res, let h = handler(res) else { return }
        h.commitState(res, serial: serial)
    }
    private static let invokeAction_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UInt32) -> Void = { _, res, button, index in
        guard let res, let h = handler(res) else { return }
        h.invokeAction(res, button: button, index: index)
    }
}
