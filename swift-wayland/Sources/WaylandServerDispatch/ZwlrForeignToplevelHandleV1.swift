// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for zwlr_foreign_toplevel_handle_v1: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

import WaylandServerC
import WaylandServer

public protocol ZwlrForeignToplevelHandleV1Requests: AnyObject {
    func setMaximized(_ resource: UnsafeMutablePointer<wl_resource>)
    func unsetMaximized(_ resource: UnsafeMutablePointer<wl_resource>)
    func setMinimized(_ resource: UnsafeMutablePointer<wl_resource>)
    func unsetMinimized(_ resource: UnsafeMutablePointer<wl_resource>)
    func activate(_ resource: UnsafeMutablePointer<wl_resource>, seat: UnsafeMutablePointer<wl_resource>?)
    func close(_ resource: UnsafeMutablePointer<wl_resource>)
    func setRectangle(_ resource: UnsafeMutablePointer<wl_resource>, surface: UnsafeMutablePointer<wl_resource>?, x: Int32, y: Int32, width: Int32, height: Int32)
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
    func setFullscreen(_ resource: UnsafeMutablePointer<wl_resource>, output: UnsafeMutablePointer<wl_resource>?)
    func unsetFullscreen(_ resource: UnsafeMutablePointer<wl_resource>)
}

public extension ZwlrForeignToplevelHandleV1Requests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum ZwlrForeignToplevelHandleV1Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_zwlr_foreign_toplevel_handle_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_zwlr_foreign_toplevel_handle_v1_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_zwlr_foreign_toplevel_handle_v1_requests.self, capacity: 1)
        vt.pointee.set_maximized = setMaximized_impl
        vt.pointee.unset_maximized = unsetMaximized_impl
        vt.pointee.set_minimized = setMinimized_impl
        vt.pointee.unset_minimized = unsetMinimized_impl
        vt.pointee.activate = activate_impl
        vt.pointee.close = close_impl
        vt.pointee.set_rectangle = setRectangle_impl
        vt.pointee.destroy = destroy_impl
        vt.pointee.set_fullscreen = setFullscreen_impl
        vt.pointee.unset_fullscreen = unsetFullscreen_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendTitle(_ target: UnsafeMutablePointer<wl_resource>, title: UnsafePointer<CChar>?) {
        zwlr_foreign_toplevel_handle_v1_send_title(target, title)
    }
    public static func sendAppId(_ target: UnsafeMutablePointer<wl_resource>, app_id: UnsafePointer<CChar>?) {
        zwlr_foreign_toplevel_handle_v1_send_app_id(target, app_id)
    }
    public static func sendOutputEnter(_ target: UnsafeMutablePointer<wl_resource>, output: UnsafeMutablePointer<wl_resource>?) {
        zwlr_foreign_toplevel_handle_v1_send_output_enter(target, output)
    }
    public static func sendOutputLeave(_ target: UnsafeMutablePointer<wl_resource>, output: UnsafeMutablePointer<wl_resource>?) {
        zwlr_foreign_toplevel_handle_v1_send_output_leave(target, output)
    }
    public static func sendState(_ target: UnsafeMutablePointer<wl_resource>, state: UnsafeMutablePointer<wl_array>?) {
        zwlr_foreign_toplevel_handle_v1_send_state(target, state)
    }
    public static func sendDone(_ target: UnsafeMutablePointer<wl_resource>) {
        zwlr_foreign_toplevel_handle_v1_send_done(target)
    }
    public static func sendClosed(_ target: UnsafeMutablePointer<wl_resource>) {
        zwlr_foreign_toplevel_handle_v1_send_closed(target)
    }
    public static func sendParent(_ target: UnsafeMutablePointer<wl_resource>, parent: UnsafeMutablePointer<wl_resource>?) {
        zwlr_foreign_toplevel_handle_v1_send_parent(target, parent)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> ZwlrForeignToplevelHandleV1Requests? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? ZwlrForeignToplevelHandleV1Requests
    }

    private static let setMaximized_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res, let h = handler(res) else { return }
        h.setMaximized(res)
    }
    private static let unsetMaximized_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res, let h = handler(res) else { return }
        h.unsetMaximized(res)
    }
    private static let setMinimized_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res, let h = handler(res) else { return }
        h.setMinimized(res)
    }
    private static let unsetMinimized_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res, let h = handler(res) else { return }
        h.unsetMinimized(res)
    }
    private static let activate_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res, seat in
        guard let res, let h = handler(res) else { return }
        h.activate(res, seat: seat)
    }
    private static let close_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res, let h = handler(res) else { return }
        h.close(res)
    }
    private static let setRectangle_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?, Int32, Int32, Int32, Int32) -> Void = { _, res, surface, x, y, width, height in
        guard let res, let h = handler(res) else { return }
        h.setRectangle(res, surface: surface, x: x, y: y, width: width, height: height)
    }
    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
    private static let setFullscreen_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res, output in
        guard let res, let h = handler(res) else { return }
        h.setFullscreen(res, output: output)
    }
    private static let unsetFullscreen_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res, let h = handler(res) else { return }
        h.unsetFullscreen(res)
    }
}
