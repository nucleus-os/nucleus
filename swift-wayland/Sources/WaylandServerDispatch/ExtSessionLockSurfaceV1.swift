// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for ext_session_lock_surface_v1: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

import WaylandServerC
import WaylandServer

public protocol ExtSessionLockSurfaceV1Requests: AnyObject {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
    func ackConfigure(_ resource: UnsafeMutablePointer<wl_resource>, serial: UInt32)
}

public extension ExtSessionLockSurfaceV1Requests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum ExtSessionLockSurfaceV1Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_ext_session_lock_surface_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_ext_session_lock_surface_v1_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_ext_session_lock_surface_v1_requests.self, capacity: 1)
        vt.pointee.destroy = destroy_impl
        vt.pointee.ack_configure = ackConfigure_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendConfigure(_ target: UnsafeMutablePointer<wl_resource>, serial: UInt32, width: UInt32, height: UInt32) {
        ext_session_lock_surface_v1_send_configure(target, serial, width, height)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> ExtSessionLockSurfaceV1Requests? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? ExtSessionLockSurfaceV1Requests
    }

    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
    private static let ackConfigure_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { _, res, serial in
        guard let res, let h = handler(res) else { return }
        h.ackConfigure(res, serial: serial)
    }
}
