// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for zwp_tablet_pad_dial_v2: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

public import WaylandServerC
import WaylandServer

public protocol ZwpTabletPadDialV2Requests: AnyObject {
    func setFeedback(_ resource: UnsafeMutablePointer<wl_resource>, description: UnsafePointer<CChar>?, serial: UInt32)
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
}

public extension ZwpTabletPadDialV2Requests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum ZwpTabletPadDialV2Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_zwp_tablet_pad_dial_v2_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_zwp_tablet_pad_dial_v2_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_zwp_tablet_pad_dial_v2_requests.self, capacity: 1)
        vt.pointee.set_feedback = setFeedback_impl
        vt.pointee.destroy = destroy_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendDelta(_ target: UnsafeMutablePointer<wl_resource>, value120: Int32) {
        zwp_tablet_pad_dial_v2_send_delta(target, value120)
    }
    public static func sendFrame(_ target: UnsafeMutablePointer<wl_resource>, time: UInt32) {
        zwp_tablet_pad_dial_v2_send_frame(target, time)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> (any ZwpTabletPadDialV2Requests)? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? any ZwpTabletPadDialV2Requests
    }

    private static let setFeedback_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafePointer<CChar>?, UInt32) -> Void = { _, res, description, serial in
        guard let res, let h = handler(res) else { return }
        h.setFeedback(res, description: description, serial: serial)
    }
    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
}
