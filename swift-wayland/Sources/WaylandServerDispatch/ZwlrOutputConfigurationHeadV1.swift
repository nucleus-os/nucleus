// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for zwlr_output_configuration_head_v1: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

import WaylandServerC
import WaylandServer

public protocol ZwlrOutputConfigurationHeadV1Requests: AnyObject {
    func setMode(_ resource: UnsafeMutablePointer<wl_resource>, mode: UnsafeMutablePointer<wl_resource>?)
    func setCustomMode(_ resource: UnsafeMutablePointer<wl_resource>, width: Int32, height: Int32, refresh: Int32)
    func setPosition(_ resource: UnsafeMutablePointer<wl_resource>, x: Int32, y: Int32)
    func setTransform(_ resource: UnsafeMutablePointer<wl_resource>, transform: Int32)
    func setScale(_ resource: UnsafeMutablePointer<wl_resource>, scale: Double)
    func setAdaptiveSync(_ resource: UnsafeMutablePointer<wl_resource>, state: UInt32)
}

public enum ZwlrOutputConfigurationHeadV1Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_zwlr_output_configuration_head_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_zwlr_output_configuration_head_v1_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_zwlr_output_configuration_head_v1_requests.self, capacity: 1)
        vt.pointee.set_mode = setMode_impl
        vt.pointee.set_custom_mode = setCustomMode_impl
        vt.pointee.set_position = setPosition_impl
        vt.pointee.set_transform = setTransform_impl
        vt.pointee.set_scale = setScale_impl
        vt.pointee.set_adaptive_sync = setAdaptiveSync_impl
        return UnsafeRawPointer(raw)
    }()

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> ZwlrOutputConfigurationHeadV1Requests? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? ZwlrOutputConfigurationHeadV1Requests
    }

    private static let setMode_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res, mode in
        guard let res, let h = handler(res) else { return }
        h.setMode(res, mode: mode)
    }
    private static let setCustomMode_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, Int32, Int32, Int32) -> Void = { _, res, width, height, refresh in
        guard let res, let h = handler(res) else { return }
        h.setCustomMode(res, width: width, height: height, refresh: refresh)
    }
    private static let setPosition_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, Int32, Int32) -> Void = { _, res, x, y in
        guard let res, let h = handler(res) else { return }
        h.setPosition(res, x: x, y: y)
    }
    private static let setTransform_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, Int32) -> Void = { _, res, transform in
        guard let res, let h = handler(res) else { return }
        h.setTransform(res, transform: transform)
    }
    private static let setScale_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, wl_fixed_t) -> Void = { _, res, scale in
        guard let res, let h = handler(res) else { return }
        h.setScale(res, scale: swift_wayland_fixed_to_double(scale))
    }
    private static let setAdaptiveSync_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { _, res, state in
        guard let res, let h = handler(res) else { return }
        h.setAdaptiveSync(res, state: state)
    }
}
