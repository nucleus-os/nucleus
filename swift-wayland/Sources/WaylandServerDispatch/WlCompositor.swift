// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for wl_compositor: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

public import WaylandServerC
public import WaylandServer

public protocol WlCompositorRequests: AnyObject {
    func createSurface(_ resource: UnsafeMutablePointer<wl_resource>, id: WlNewId)
    func createRegion(_ resource: UnsafeMutablePointer<wl_resource>, id: WlNewId)
    func release(_ resource: UnsafeMutablePointer<wl_resource>)
}

public extension WlCompositorRequests {
    func release(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum WlCompositorServer {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_wl_compositor_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_wl_compositor_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_wl_compositor_requests.self, capacity: 1)
        vt.pointee.create_surface = createSurface_impl
        vt.pointee.create_region = createRegion_impl
        vt.pointee.release = release_impl
        return UnsafeRawPointer(raw)
    }()

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> (any WlCompositorRequests)? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? any WlCompositorRequests
    }

    private static let createSurface_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { client, res, id in
        guard let res, let client, let h = handler(res) else { return }
        h.createSurface(res, id: WlNewId(client: client, id: id, version: Swift::min(wl_resource_get_version(res), Int32(7)), interface: swift_wayland_iface_wl_surface()))
    }
    private static let createRegion_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { client, res, id in
        guard let res, let client, let h = handler(res) else { return }
        h.createRegion(res, id: WlNewId(client: client, id: id, version: Swift::min(wl_resource_get_version(res), Int32(7)), interface: swift_wayland_iface_wl_region()))
    }
    private static let release_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.release(res) } else { wl_resource_destroy(res) }
    }
}
