// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for ext_workspace_handle_v1: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

public import WaylandServerC
import WaylandServer

public protocol ExtWorkspaceHandleV1Requests: AnyObject {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
    func activate(_ resource: UnsafeMutablePointer<wl_resource>)
    func deactivate(_ resource: UnsafeMutablePointer<wl_resource>)
    func assign(_ resource: UnsafeMutablePointer<wl_resource>, workspace_group: UnsafeMutablePointer<wl_resource>?)
    func remove(_ resource: UnsafeMutablePointer<wl_resource>)
}

public extension ExtWorkspaceHandleV1Requests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum ExtWorkspaceHandleV1Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_ext_workspace_handle_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_ext_workspace_handle_v1_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_ext_workspace_handle_v1_requests.self, capacity: 1)
        vt.pointee.destroy = destroy_impl
        vt.pointee.activate = activate_impl
        vt.pointee.deactivate = deactivate_impl
        vt.pointee.assign = assign_impl
        vt.pointee.remove = remove_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendId(_ target: UnsafeMutablePointer<wl_resource>, id: UnsafePointer<CChar>?) {
        ext_workspace_handle_v1_send_id(target, id)
    }
    public static func sendName(_ target: UnsafeMutablePointer<wl_resource>, name: UnsafePointer<CChar>?) {
        ext_workspace_handle_v1_send_name(target, name)
    }
    public static func sendCoordinates(_ target: UnsafeMutablePointer<wl_resource>, coordinates: UnsafeMutablePointer<wl_array>?) {
        ext_workspace_handle_v1_send_coordinates(target, coordinates)
    }
    public static func sendState(_ target: UnsafeMutablePointer<wl_resource>, state: UInt32) {
        ext_workspace_handle_v1_send_state(target, state)
    }
    public static func sendCapabilities(_ target: UnsafeMutablePointer<wl_resource>, capabilities: UInt32) {
        ext_workspace_handle_v1_send_capabilities(target, capabilities)
    }
    public static func sendRemoved(_ target: UnsafeMutablePointer<wl_resource>) {
        ext_workspace_handle_v1_send_removed(target)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> (any ExtWorkspaceHandleV1Requests)? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? any ExtWorkspaceHandleV1Requests
    }

    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
    private static let activate_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res, let h = handler(res) else { return }
        h.activate(res)
    }
    private static let deactivate_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res, let h = handler(res) else { return }
        h.deactivate(res)
    }
    private static let assign_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res, workspace_group in
        guard let res, let h = handler(res) else { return }
        h.assign(res, workspace_group: workspace_group)
    }
    private static let remove_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res, let h = handler(res) else { return }
        h.remove(res)
    }
}
