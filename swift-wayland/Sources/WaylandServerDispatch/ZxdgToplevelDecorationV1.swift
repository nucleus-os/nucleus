// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for zxdg_toplevel_decoration_v1: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

import WaylandServerC
import WaylandServer

public protocol ZxdgToplevelDecorationV1Requests: AnyObject {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
    func setMode(_ resource: UnsafeMutablePointer<wl_resource>, mode: UInt32)
    func unsetMode(_ resource: UnsafeMutablePointer<wl_resource>)
}

public extension ZxdgToplevelDecorationV1Requests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum ZxdgToplevelDecorationV1Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_zxdg_toplevel_decoration_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_zxdg_toplevel_decoration_v1_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_zxdg_toplevel_decoration_v1_requests.self, capacity: 1)
        vt.pointee.destroy = destroy_impl
        vt.pointee.set_mode = setMode_impl
        vt.pointee.unset_mode = unsetMode_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendConfigure(_ target: UnsafeMutablePointer<wl_resource>, mode: UInt32) {
        zxdg_toplevel_decoration_v1_send_configure(target, mode)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> ZxdgToplevelDecorationV1Requests? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? ZxdgToplevelDecorationV1Requests
    }

    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
    private static let setMode_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { _, res, mode in
        guard let res, let h = handler(res) else { return }
        h.setMode(res, mode: mode)
    }
    private static let unsetMode_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res, let h = handler(res) else { return }
        h.unsetMode(res)
    }
}
