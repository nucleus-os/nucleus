// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for xdg_session_v1: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

public import WaylandServerC
public import WaylandServer

public protocol XdgSessionV1Requests: AnyObject {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
    func remove(_ resource: UnsafeMutablePointer<wl_resource>)
    func addToplevel(_ resource: UnsafeMutablePointer<wl_resource>, id: WlNewId, toplevel: UnsafeMutablePointer<wl_resource>?, name: UnsafePointer<CChar>?)
    func restoreToplevel(_ resource: UnsafeMutablePointer<wl_resource>, id: WlNewId, toplevel: UnsafeMutablePointer<wl_resource>?, name: UnsafePointer<CChar>?)
    func removeToplevel(_ resource: UnsafeMutablePointer<wl_resource>, name: UnsafePointer<CChar>?)
}

public extension XdgSessionV1Requests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
    func remove(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum XdgSessionV1Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_xdg_session_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_xdg_session_v1_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_xdg_session_v1_requests.self, capacity: 1)
        vt.pointee.destroy = destroy_impl
        vt.pointee.remove = remove_impl
        vt.pointee.add_toplevel = addToplevel_impl
        vt.pointee.restore_toplevel = restoreToplevel_impl
        vt.pointee.remove_toplevel = removeToplevel_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendCreated(_ target: UnsafeMutablePointer<wl_resource>, session_id: UnsafePointer<CChar>?) {
        xdg_session_v1_send_created(target, session_id)
    }
    public static func sendRestored(_ target: UnsafeMutablePointer<wl_resource>) {
        xdg_session_v1_send_restored(target)
    }
    public static func sendReplaced(_ target: UnsafeMutablePointer<wl_resource>) {
        xdg_session_v1_send_replaced(target)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> (any XdgSessionV1Requests)? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? any XdgSessionV1Requests
    }

    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
    private static let remove_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.remove(res) } else { wl_resource_destroy(res) }
    }
    private static let addToplevel_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UnsafeMutablePointer<wl_resource>?, UnsafePointer<CChar>?) -> Void = { client, res, id, toplevel, name in
        guard let res, let client, let h = handler(res) else { return }
        h.addToplevel(res, id: WlNewId(client: client, id: id, version: Swift::min(wl_resource_get_version(res), Int32(1)), interface: swift_wayland_iface_xdg_toplevel_session_v1()), toplevel: toplevel, name: name)
    }
    private static let restoreToplevel_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UnsafeMutablePointer<wl_resource>?, UnsafePointer<CChar>?) -> Void = { client, res, id, toplevel, name in
        guard let res, let client, let h = handler(res) else { return }
        h.restoreToplevel(res, id: WlNewId(client: client, id: id, version: Swift::min(wl_resource_get_version(res), Int32(1)), interface: swift_wayland_iface_xdg_toplevel_session_v1()), toplevel: toplevel, name: name)
    }
    private static let removeToplevel_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafePointer<CChar>?) -> Void = { _, res, name in
        guard let res, let h = handler(res) else { return }
        h.removeToplevel(res, name: name)
    }
}
