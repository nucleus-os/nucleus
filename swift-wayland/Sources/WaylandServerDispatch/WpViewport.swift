// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for wp_viewport: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

public import WaylandServerC
import WaylandServer

public protocol WpViewportRequests: AnyObject {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
    func setSource(_ resource: UnsafeMutablePointer<wl_resource>, x: Double, y: Double, width: Double, height: Double)
    func setDestination(_ resource: UnsafeMutablePointer<wl_resource>, width: Int32, height: Int32)
}

public extension WpViewportRequests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum WpViewportServer {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_wp_viewport_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_wp_viewport_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_wp_viewport_requests.self, capacity: 1)
        vt.pointee.destroy = destroy_impl
        vt.pointee.set_source = setSource_impl
        vt.pointee.set_destination = setDestination_impl
        return UnsafeRawPointer(raw)
    }()

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> (any WpViewportRequests)? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? any WpViewportRequests
    }

    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
    private static let setSource_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, wl_fixed_t, wl_fixed_t, wl_fixed_t, wl_fixed_t) -> Void = { _, res, x, y, width, height in
        guard let res, let h = handler(res) else { return }
        h.setSource(res, x: swift_wayland_fixed_to_double(x), y: swift_wayland_fixed_to_double(y), width: swift_wayland_fixed_to_double(width), height: swift_wayland_fixed_to_double(height))
    }
    private static let setDestination_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, Int32, Int32) -> Void = { _, res, width, height in
        guard let res, let h = handler(res) else { return }
        h.setDestination(res, width: width, height: height)
    }
}
