// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for zwp_tablet_tool_v1: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

public import WaylandServerC
import WaylandServer

public protocol ZwpTabletToolV1Requests: AnyObject {
    func setCursor(_ resource: UnsafeMutablePointer<wl_resource>, serial: UInt32, surface: UnsafeMutablePointer<wl_resource>?, hotspot_x: Int32, hotspot_y: Int32)
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
}

public extension ZwpTabletToolV1Requests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum ZwpTabletToolV1Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_zwp_tablet_tool_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_zwp_tablet_tool_v1_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_zwp_tablet_tool_v1_requests.self, capacity: 1)
        vt.pointee.set_cursor = setCursor_impl
        vt.pointee.destroy = destroy_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendType(_ target: UnsafeMutablePointer<wl_resource>, tool_type: UInt32) {
        zwp_tablet_tool_v1_send_type(target, tool_type)
    }
    public static func sendHardwareSerial(_ target: UnsafeMutablePointer<wl_resource>, hardware_serial_hi: UInt32, hardware_serial_lo: UInt32) {
        zwp_tablet_tool_v1_send_hardware_serial(target, hardware_serial_hi, hardware_serial_lo)
    }
    public static func sendHardwareIdWacom(_ target: UnsafeMutablePointer<wl_resource>, hardware_id_hi: UInt32, hardware_id_lo: UInt32) {
        zwp_tablet_tool_v1_send_hardware_id_wacom(target, hardware_id_hi, hardware_id_lo)
    }
    public static func sendCapability(_ target: UnsafeMutablePointer<wl_resource>, capability: UInt32) {
        zwp_tablet_tool_v1_send_capability(target, capability)
    }
    public static func sendDone(_ target: UnsafeMutablePointer<wl_resource>) {
        zwp_tablet_tool_v1_send_done(target)
    }
    public static func sendRemoved(_ target: UnsafeMutablePointer<wl_resource>) {
        zwp_tablet_tool_v1_send_removed(target)
    }
    public static func sendProximityIn(_ target: UnsafeMutablePointer<wl_resource>, serial: UInt32, tablet: UnsafeMutablePointer<wl_resource>?, surface: UnsafeMutablePointer<wl_resource>?) {
        zwp_tablet_tool_v1_send_proximity_in(target, serial, tablet, surface)
    }
    public static func sendProximityOut(_ target: UnsafeMutablePointer<wl_resource>) {
        zwp_tablet_tool_v1_send_proximity_out(target)
    }
    public static func sendDown(_ target: UnsafeMutablePointer<wl_resource>, serial: UInt32) {
        zwp_tablet_tool_v1_send_down(target, serial)
    }
    public static func sendUp(_ target: UnsafeMutablePointer<wl_resource>) {
        zwp_tablet_tool_v1_send_up(target)
    }
    public static func sendMotion(_ target: UnsafeMutablePointer<wl_resource>, x: Double, y: Double) {
        zwp_tablet_tool_v1_send_motion(target, swift_wayland_fixed_from_double(x), swift_wayland_fixed_from_double(y))
    }
    public static func sendPressure(_ target: UnsafeMutablePointer<wl_resource>, pressure: UInt32) {
        zwp_tablet_tool_v1_send_pressure(target, pressure)
    }
    public static func sendDistance(_ target: UnsafeMutablePointer<wl_resource>, distance: UInt32) {
        zwp_tablet_tool_v1_send_distance(target, distance)
    }
    public static func sendTilt(_ target: UnsafeMutablePointer<wl_resource>, tilt_x: Int32, tilt_y: Int32) {
        zwp_tablet_tool_v1_send_tilt(target, tilt_x, tilt_y)
    }
    public static func sendRotation(_ target: UnsafeMutablePointer<wl_resource>, degrees: Int32) {
        zwp_tablet_tool_v1_send_rotation(target, degrees)
    }
    public static func sendSlider(_ target: UnsafeMutablePointer<wl_resource>, position: Int32) {
        zwp_tablet_tool_v1_send_slider(target, position)
    }
    public static func sendWheel(_ target: UnsafeMutablePointer<wl_resource>, degrees: Int32, clicks: Int32) {
        zwp_tablet_tool_v1_send_wheel(target, degrees, clicks)
    }
    public static func sendButton(_ target: UnsafeMutablePointer<wl_resource>, serial: UInt32, button: UInt32, state: UInt32) {
        zwp_tablet_tool_v1_send_button(target, serial, button, state)
    }
    public static func sendFrame(_ target: UnsafeMutablePointer<wl_resource>, time: UInt32) {
        zwp_tablet_tool_v1_send_frame(target, time)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> (any ZwpTabletToolV1Requests)? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? any ZwpTabletToolV1Requests
    }

    private static let setCursor_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UnsafeMutablePointer<wl_resource>?, Int32, Int32) -> Void = { _, res, serial, surface, hotspot_x, hotspot_y in
        guard let res, let h = handler(res) else { return }
        h.setCursor(res, serial: serial, surface: surface, hotspot_x: hotspot_x, hotspot_y: hotspot_y)
    }
    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
}
