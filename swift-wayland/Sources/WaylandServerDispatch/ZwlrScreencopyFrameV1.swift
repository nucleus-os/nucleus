// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for zwlr_screencopy_frame_v1: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

public import WaylandServerC
import WaylandServer

public protocol ZwlrScreencopyFrameV1Requests: AnyObject {
    func copy(_ resource: UnsafeMutablePointer<wl_resource>, buffer: UnsafeMutablePointer<wl_resource>?)
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
    func copyWithDamage(_ resource: UnsafeMutablePointer<wl_resource>, buffer: UnsafeMutablePointer<wl_resource>?)
}

public extension ZwlrScreencopyFrameV1Requests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum ZwlrScreencopyFrameV1Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_zwlr_screencopy_frame_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_zwlr_screencopy_frame_v1_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_zwlr_screencopy_frame_v1_requests.self, capacity: 1)
        vt.pointee.copy = copy_impl
        vt.pointee.destroy = destroy_impl
        vt.pointee.copy_with_damage = copyWithDamage_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendBuffer(_ target: UnsafeMutablePointer<wl_resource>, format: UInt32, width: UInt32, height: UInt32, stride: UInt32) {
        zwlr_screencopy_frame_v1_send_buffer(target, format, width, height, stride)
    }
    public static func sendFlags(_ target: UnsafeMutablePointer<wl_resource>, flags: UInt32) {
        zwlr_screencopy_frame_v1_send_flags(target, flags)
    }
    public static func sendReady(_ target: UnsafeMutablePointer<wl_resource>, tv_sec_hi: UInt32, tv_sec_lo: UInt32, tv_nsec: UInt32) {
        zwlr_screencopy_frame_v1_send_ready(target, tv_sec_hi, tv_sec_lo, tv_nsec)
    }
    public static func sendFailed(_ target: UnsafeMutablePointer<wl_resource>) {
        zwlr_screencopy_frame_v1_send_failed(target)
    }
    public static func sendDamage(_ target: UnsafeMutablePointer<wl_resource>, x: UInt32, y: UInt32, width: UInt32, height: UInt32) {
        zwlr_screencopy_frame_v1_send_damage(target, x, y, width, height)
    }
    public static func sendLinuxDmabuf(_ target: UnsafeMutablePointer<wl_resource>, format: UInt32, width: UInt32, height: UInt32) {
        zwlr_screencopy_frame_v1_send_linux_dmabuf(target, format, width, height)
    }
    public static func sendBufferDone(_ target: UnsafeMutablePointer<wl_resource>) {
        zwlr_screencopy_frame_v1_send_buffer_done(target)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> (any ZwlrScreencopyFrameV1Requests)? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? any ZwlrScreencopyFrameV1Requests
    }

    private static let copy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res, buffer in
        guard let res, let h = handler(res) else { return }
        h.copy(res, buffer: buffer)
    }
    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
    private static let copyWithDamage_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res, buffer in
        guard let res, let h = handler(res) else { return }
        h.copyWithDamage(res, buffer: buffer)
    }
}
