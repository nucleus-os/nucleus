// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for zwp_fullscreen_shell_v1: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

public import WaylandServerC
public import WaylandServer

public protocol ZwpFullscreenShellV1Requests: AnyObject {
    func release(_ resource: UnsafeMutablePointer<wl_resource>)
    func presentSurface(_ resource: UnsafeMutablePointer<wl_resource>, surface: UnsafeMutablePointer<wl_resource>?, method: UInt32, output: UnsafeMutablePointer<wl_resource>?)
    func presentSurfaceForMode(_ resource: UnsafeMutablePointer<wl_resource>, surface: UnsafeMutablePointer<wl_resource>?, output: UnsafeMutablePointer<wl_resource>?, framerate: Int32, feedback: WlNewId)
}

public extension ZwpFullscreenShellV1Requests {
    func release(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum ZwpFullscreenShellV1Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_zwp_fullscreen_shell_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_zwp_fullscreen_shell_v1_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_zwp_fullscreen_shell_v1_requests.self, capacity: 1)
        vt.pointee.release = release_impl
        vt.pointee.present_surface = presentSurface_impl
        vt.pointee.present_surface_for_mode = presentSurfaceForMode_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendCapability(_ target: UnsafeMutablePointer<wl_resource>, capability: UInt32) {
        zwp_fullscreen_shell_v1_send_capability(target, capability)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> (any ZwpFullscreenShellV1Requests)? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? any ZwpFullscreenShellV1Requests
    }

    private static let release_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.release(res) } else { wl_resource_destroy(res) }
    }
    private static let presentSurface_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?, UInt32, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res, surface, method, output in
        guard let res, let h = handler(res) else { return }
        h.presentSurface(res, surface: surface, method: method, output: output)
    }
    private static let presentSurfaceForMode_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?, Int32, UInt32) -> Void = { client, res, surface, output, framerate, feedback in
        guard let res, let client, let h = handler(res) else { return }
        h.presentSurfaceForMode(res, surface: surface, output: output, framerate: framerate, feedback: WlNewId(client: client, id: feedback, version: Swift::min(wl_resource_get_version(res), Int32(1)), interface: swift_wayland_iface_zwp_fullscreen_shell_mode_feedback_v1()))
    }
}
