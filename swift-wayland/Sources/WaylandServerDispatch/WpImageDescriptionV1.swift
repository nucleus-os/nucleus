// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for wp_image_description_v1: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

import WaylandServerC
import WaylandServer

public protocol WpImageDescriptionV1Requests: AnyObject {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
    func getInformation(_ resource: UnsafeMutablePointer<wl_resource>, information: WlNewId)
}

public extension WpImageDescriptionV1Requests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum WpImageDescriptionV1Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_wp_image_description_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_wp_image_description_v1_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_wp_image_description_v1_requests.self, capacity: 1)
        vt.pointee.destroy = destroy_impl
        vt.pointee.get_information = getInformation_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendFailed(_ target: UnsafeMutablePointer<wl_resource>, cause: UInt32, msg: UnsafePointer<CChar>?) {
        wp_image_description_v1_send_failed(target, cause, msg)
    }
    public static func sendReady(_ target: UnsafeMutablePointer<wl_resource>, identity: UInt32) {
        wp_image_description_v1_send_ready(target, identity)
    }
    public static func sendReady2(_ target: UnsafeMutablePointer<wl_resource>, identity_hi: UInt32, identity_lo: UInt32) {
        wp_image_description_v1_send_ready2(target, identity_hi, identity_lo)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> WpImageDescriptionV1Requests? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? WpImageDescriptionV1Requests
    }

    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
    private static let getInformation_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { client, res, information in
        guard let res, let client, let h = handler(res) else { return }
        h.getInformation(res, information: WlNewId(client: client, id: information, version: Swift.min(wl_resource_get_version(res), Int32(2)), interface: swift_wayland_iface_wp_image_description_info_v1()))
    }
}
