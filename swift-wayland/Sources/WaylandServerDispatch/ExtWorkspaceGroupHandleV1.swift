// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for ext_workspace_group_handle_v1: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

import WaylandServerC
import WaylandServer

public protocol ExtWorkspaceGroupHandleV1Requests: AnyObject {
    func createWorkspace(_ resource: UnsafeMutablePointer<wl_resource>, workspace: UnsafePointer<CChar>?)
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
}

public extension ExtWorkspaceGroupHandleV1Requests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum ExtWorkspaceGroupHandleV1Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_ext_workspace_group_handle_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_ext_workspace_group_handle_v1_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_ext_workspace_group_handle_v1_requests.self, capacity: 1)
        vt.pointee.create_workspace = createWorkspace_impl
        vt.pointee.destroy = destroy_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendCapabilities(_ target: UnsafeMutablePointer<wl_resource>, capabilities: UInt32) {
        ext_workspace_group_handle_v1_send_capabilities(target, capabilities)
    }
    public static func sendOutputEnter(_ target: UnsafeMutablePointer<wl_resource>, output: UnsafeMutablePointer<wl_resource>?) {
        ext_workspace_group_handle_v1_send_output_enter(target, output)
    }
    public static func sendOutputLeave(_ target: UnsafeMutablePointer<wl_resource>, output: UnsafeMutablePointer<wl_resource>?) {
        ext_workspace_group_handle_v1_send_output_leave(target, output)
    }
    public static func sendWorkspaceEnter(_ target: UnsafeMutablePointer<wl_resource>, workspace: UnsafeMutablePointer<wl_resource>?) {
        ext_workspace_group_handle_v1_send_workspace_enter(target, workspace)
    }
    public static func sendWorkspaceLeave(_ target: UnsafeMutablePointer<wl_resource>, workspace: UnsafeMutablePointer<wl_resource>?) {
        ext_workspace_group_handle_v1_send_workspace_leave(target, workspace)
    }
    public static func sendRemoved(_ target: UnsafeMutablePointer<wl_resource>) {
        ext_workspace_group_handle_v1_send_removed(target)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> ExtWorkspaceGroupHandleV1Requests? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? ExtWorkspaceGroupHandleV1Requests
    }

    private static let createWorkspace_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafePointer<CChar>?) -> Void = { _, res, workspace in
        guard let res, let h = handler(res) else { return }
        h.createWorkspace(res, workspace: workspace)
    }
    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
}
