// Generated by SwiftWaylandGen. Do not edit.
// Typed server descriptor and dispatch for zwp_linux_dmabuf_feedback_v1.

public import WaylandServerC
public import WaylandServer
public import WaylandProtocolTypes
@MainActor public protocol ZwpLinuxDmabufFeedbackV1Requests: AnyObject {
    func destroy(_ request: WaylandRequest<ZwpLinuxDmabufFeedbackV1Server>)
}
public extension ZwpLinuxDmabufFeedbackV1Requests {
    func destroy(_ request: WaylandRequest<ZwpLinuxDmabufFeedbackV1Server>) {
        unsafe wl_resource_destroy(request.resource)
    }
}
public enum ZwpLinuxDmabufFeedbackV1Server: WaylandServerInterface {
    public nonisolated(unsafe) static let interface = unsafe swift_wayland_iface_zwp_linux_dmabuf_feedback_v1()
    public nonisolated static let maximumVersion: Int32 = 5
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_zwp_linux_dmabuf_feedback_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_zwp_linux_dmabuf_feedback_v1_requests>.alignment)
        unsafe raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = unsafe raw.bindMemory(to: swift_wayland_zwp_linux_dmabuf_feedback_v1_requests.self, capacity: 1)
        unsafe vt.pointee.destroy = destroy_impl
        return UnsafeRawPointer(raw)
    }()
    public nonisolated static func requestVtable() -> UnsafeRawPointer? {
        unsafe vtable
    }
    public static func sendDone(_ target: UnsafeMutablePointer<wl_resource>) {
        unsafe zwp_linux_dmabuf_feedback_v1_send_done(target)
    }
    public static func sendFormatTable(_ target: UnsafeMutablePointer<wl_resource>, fd: Int32, size: UInt32) {
        unsafe zwp_linux_dmabuf_feedback_v1_send_format_table(target, fd, size)
    }
    public static func sendMainDevice(_ target: UnsafeMutablePointer<wl_resource>, device: UnsafeMutablePointer<wl_array>?) {
        unsafe zwp_linux_dmabuf_feedback_v1_send_main_device(target, device)
    }
    public static func sendTrancheDone(_ target: UnsafeMutablePointer<wl_resource>) {
        unsafe zwp_linux_dmabuf_feedback_v1_send_tranche_done(target)
    }
    public static func sendTrancheTargetDevice(_ target: UnsafeMutablePointer<wl_resource>, device: UnsafeMutablePointer<wl_array>?) {
        unsafe zwp_linux_dmabuf_feedback_v1_send_tranche_target_device(target, device)
    }
    public static func sendTrancheFormats(_ target: UnsafeMutablePointer<wl_resource>, indices: UnsafeMutablePointer<wl_array>?) {
        unsafe zwp_linux_dmabuf_feedback_v1_send_tranche_formats(target, indices)
    }
    public static func sendTrancheFlags(_ target: UnsafeMutablePointer<wl_resource>, flags: ZwpLinuxDmabufFeedbackV1TrancheFlags) {
        unsafe zwp_linux_dmabuf_feedback_v1_send_tranche_flags(target, flags.rawValue)
    }
    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> any ZwpLinuxDmabufFeedbackV1Requests? {
        guard let ud = unsafe wl_resource_get_user_data(res) else {
            return nil
        }
        return unsafe Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? any ZwpLinuxDmabufFeedbackV1Requests
    }
    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res = unsafe res else {
            return
        }
        if let h = unsafe handler(res) {
            nonisolated(unsafe) let requestHandler = h
            nonisolated(unsafe) let requestResource = unsafe res
            MainActor.assumeIsolated {
                unsafe requestHandler.destroy(WaylandRequest<ZwpLinuxDmabufFeedbackV1Server>(requestResource))
            }
        } else {
            unsafe wl_resource_destroy(res)
        }
    }
}
public extension WaylandResourceHandle where Interface == ZwpLinuxDmabufFeedbackV1Server {
    @discardableResult
    func sendDone() -> Bool {
        guard let target = unsafe resource else {
            return false
        }
        unsafe zwp_linux_dmabuf_feedback_v1_send_done(target)
        return true
    }
    @discardableResult
    func sendFormatTable(fd: Int32, size: UInt32) -> Bool {
        guard let target = unsafe resource else {
            return false
        }
        unsafe zwp_linux_dmabuf_feedback_v1_send_format_table(target, fd, size)
        return true
    }
    @discardableResult
    func sendMainDevice<DeviceElement>(device: [DeviceElement]) -> Bool {
        guard let target = unsafe resource else {
            return false
        }
        return unsafe withWaylandEventArray(device) { _event_deviceArray in
            unsafe zwp_linux_dmabuf_feedback_v1_send_main_device(target, _event_deviceArray)
            return true
        }
    }
    @discardableResult
    func sendTrancheDone() -> Bool {
        guard let target = unsafe resource else {
            return false
        }
        unsafe zwp_linux_dmabuf_feedback_v1_send_tranche_done(target)
        return true
    }
    @discardableResult
    func sendTrancheTargetDevice<DeviceElement>(device: [DeviceElement]) -> Bool {
        guard let target = unsafe resource else {
            return false
        }
        return unsafe withWaylandEventArray(device) { _event_deviceArray in
            unsafe zwp_linux_dmabuf_feedback_v1_send_tranche_target_device(target, _event_deviceArray)
            return true
        }
    }
    @discardableResult
    func sendTrancheFormats<IndicesElement>(indices: [IndicesElement]) -> Bool {
        guard let target = unsafe resource else {
            return false
        }
        return unsafe withWaylandEventArray(indices) { _event_indicesArray in
            unsafe zwp_linux_dmabuf_feedback_v1_send_tranche_formats(target, _event_indicesArray)
            return true
        }
    }
    @discardableResult
    func sendTrancheFlags(flags: ZwpLinuxDmabufFeedbackV1TrancheFlags) -> Bool {
        guard let target = unsafe resource else {
            return false
        }
        unsafe zwp_linux_dmabuf_feedback_v1_send_tranche_flags(target, flags.rawValue)
        return true
    }
}
public extension WlNewId where Interface == ZwpLinuxDmabufFeedbackV1Server {
    @discardableResult
    @MainActor
    func create<Owner: AnyObject>(
        owner: (WaylandResourceHandle<ZwpLinuxDmabufFeedbackV1Server>) -> Owner?,
        installed: (Owner) -> Void = { _ in
        }
    ) -> Owner? {
        unsafe _create(vtable: ZwpLinuxDmabufFeedbackV1Server.requestVtable(), owner: owner, installed: installed)
    }
}
public extension ZwpLinuxDmabufFeedbackV1Server {
    @discardableResult
    @MainActor
    static func createResource<Owner: AnyObject>(
        client: OpaquePointer,
        version: Int32,
        id: UInt32 = 0,
        owner: (WaylandResourceHandle<ZwpLinuxDmabufFeedbackV1Server>) -> Owner?,
        installed: (Owner) -> Void = { _ in
        }
    ) -> Owner? {
        unsafe WaylandResource.create(
            client: client, interface: ZwpLinuxDmabufFeedbackV1Server.self,
            version: Swift.min(version, maximumVersion), id: id,
            vtable: requestVtable(), owner: owner,
            installed: installed)
    }
    @MainActor
    static func global<Implementation: AnyObject, Owner: AnyObject>(
        implementation: Implementation,
        advertisedVersion: Int32 = maximumVersion,
        owner: @escaping (Implementation, WaylandResourceHandle<ZwpLinuxDmabufFeedbackV1Server>) -> Owner?,
        installed: @escaping (Implementation, Owner, WaylandResourceHandle<ZwpLinuxDmabufFeedbackV1Server>) -> Void = { _, _, _ in
        }
    ) -> WaylandGlobalSpecification<ZwpLinuxDmabufFeedbackV1Server> {
        unsafe WaylandGlobalSpecification(
            implementation: implementation,
            advertisedVersion: advertisedVersion,
            vtable: requestVtable(), owner: owner,
            installed: installed)
    }
}