// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for xdg_surface: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

public import WaylandServerC
public import WaylandServer

public protocol XdgSurfaceRequests: AnyObject {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
    func getToplevel(_ resource: UnsafeMutablePointer<wl_resource>, id: WlNewId)
    func getPopup(_ resource: UnsafeMutablePointer<wl_resource>, id: WlNewId, parent: UnsafeMutablePointer<wl_resource>?, positioner: UnsafeMutablePointer<wl_resource>?)
    func setWindowGeometry(_ resource: UnsafeMutablePointer<wl_resource>, x: Int32, y: Int32, width: Int32, height: Int32)
    func ackConfigure(_ resource: UnsafeMutablePointer<wl_resource>, serial: UInt32)
}

public extension XdgSurfaceRequests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum XdgSurfaceServer {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_xdg_surface_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_xdg_surface_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_xdg_surface_requests.self, capacity: 1)
        vt.pointee.destroy = destroy_impl
        vt.pointee.get_toplevel = getToplevel_impl
        vt.pointee.get_popup = getPopup_impl
        vt.pointee.set_window_geometry = setWindowGeometry_impl
        vt.pointee.ack_configure = ackConfigure_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendConfigure(_ target: UnsafeMutablePointer<wl_resource>, serial: UInt32) {
        xdg_surface_send_configure(target, serial)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> (any XdgSurfaceRequests)? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? any XdgSurfaceRequests
    }

    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
    private static let getToplevel_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { client, res, id in
        guard let res, let client, let h = handler(res) else { return }
        h.getToplevel(res, id: WlNewId(client: client, id: id, version: Swift::min(wl_resource_get_version(res), Int32(7)), interface: swift_wayland_iface_xdg_toplevel()))
    }
    private static let getPopup_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?) -> Void = { client, res, id, parent, positioner in
        guard let res, let client, let h = handler(res) else { return }
        h.getPopup(res, id: WlNewId(client: client, id: id, version: Swift::min(wl_resource_get_version(res), Int32(7)), interface: swift_wayland_iface_xdg_popup()), parent: parent, positioner: positioner)
    }
    private static let setWindowGeometry_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, Int32, Int32, Int32, Int32) -> Void = { _, res, x, y, width, height in
        guard let res, let h = handler(res) else { return }
        h.setWindowGeometry(res, x: x, y: y, width: width, height: height)
    }
    private static let ackConfigure_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { _, res, serial in
        guard let res, let h = handler(res) else { return }
        h.ackConfigure(res, serial: serial)
    }
}
