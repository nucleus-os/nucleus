// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for xdg_activation_token_v1: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

public import WaylandServerC
import WaylandServer

public protocol XdgActivationTokenV1Requests: AnyObject {
    func setSerial(_ resource: UnsafeMutablePointer<wl_resource>, serial: UInt32, seat: UnsafeMutablePointer<wl_resource>?)
    func setAppId(_ resource: UnsafeMutablePointer<wl_resource>, app_id: UnsafePointer<CChar>?)
    func setSurface(_ resource: UnsafeMutablePointer<wl_resource>, surface: UnsafeMutablePointer<wl_resource>?)
    func commit(_ resource: UnsafeMutablePointer<wl_resource>)
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
}

public extension XdgActivationTokenV1Requests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum XdgActivationTokenV1Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_xdg_activation_token_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_xdg_activation_token_v1_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_xdg_activation_token_v1_requests.self, capacity: 1)
        vt.pointee.set_serial = setSerial_impl
        vt.pointee.set_app_id = setAppId_impl
        vt.pointee.set_surface = setSurface_impl
        vt.pointee.commit = commit_impl
        vt.pointee.destroy = destroy_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendDone(_ target: UnsafeMutablePointer<wl_resource>, token: UnsafePointer<CChar>?) {
        xdg_activation_token_v1_send_done(target, token)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> (any XdgActivationTokenV1Requests)? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? any XdgActivationTokenV1Requests
    }

    private static let setSerial_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res, serial, seat in
        guard let res, let h = handler(res) else { return }
        h.setSerial(res, serial: serial, seat: seat)
    }
    private static let setAppId_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafePointer<CChar>?) -> Void = { _, res, app_id in
        guard let res, let h = handler(res) else { return }
        h.setAppId(res, app_id: app_id)
    }
    private static let setSurface_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res, surface in
        guard let res, let h = handler(res) else { return }
        h.setSurface(res, surface: surface)
    }
    private static let commit_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res, let h = handler(res) else { return }
        h.commit(res)
    }
    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
}
