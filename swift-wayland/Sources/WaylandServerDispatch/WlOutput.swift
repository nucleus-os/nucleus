// Generated by SwiftWaylandGen. Do not edit.
// Typed server descriptor and dispatch for wl_output.

public import WaylandServerC
public import WaylandServer
public import WaylandProtocolTypes
@MainActor public protocol WlOutputRequests: AnyObject {
    func release(_ request: WaylandRequest<WlOutputServer>)
}
public extension WlOutputRequests {
    func release(_ request: WaylandRequest<WlOutputServer>) {
        unsafe wl_resource_destroy(request.resource)
    }
}
public enum WlOutputServer: WaylandServerInterface {
    public nonisolated(unsafe) static let interface = unsafe swift_wayland_iface_wl_output()
    public nonisolated static let maximumVersion: Int32 = 4
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_wl_output_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_wl_output_requests>.alignment)
        unsafe raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = unsafe raw.bindMemory(to: swift_wayland_wl_output_requests.self, capacity: 1)
        unsafe vt.pointee.release = release_impl
        return UnsafeRawPointer(raw)
    }()
    public nonisolated static func requestVtable() -> UnsafeRawPointer? {
        unsafe vtable
    }
    public static func sendGeometry(_ target: UnsafeMutablePointer<wl_resource>, x: Int32, y: Int32, physical_width: Int32, physical_height: Int32, subpixel: WlOutputSubpixel, make: UnsafePointer<CChar>?, model: UnsafePointer<CChar>?, transform: WlOutputTransform) {
        unsafe wl_output_send_geometry(target, x, y, physical_width, physical_height, Int32(bitPattern: subpixel.rawValue), make, model, Int32(bitPattern: transform.rawValue))
    }
    public static func sendMode(_ target: UnsafeMutablePointer<wl_resource>, flags: WlOutputMode, width: Int32, height: Int32, refresh: Int32) {
        unsafe wl_output_send_mode(target, flags.rawValue, width, height, refresh)
    }
    public static func sendDone(_ target: UnsafeMutablePointer<wl_resource>) {
        unsafe wl_output_send_done(target)
    }
    public static func sendScale(_ target: UnsafeMutablePointer<wl_resource>, factor: Int32) {
        unsafe wl_output_send_scale(target, factor)
    }
    public static func sendName(_ target: UnsafeMutablePointer<wl_resource>, name: UnsafePointer<CChar>?) {
        unsafe wl_output_send_name(target, name)
    }
    public static func sendDescription(_ target: UnsafeMutablePointer<wl_resource>, description: UnsafePointer<CChar>?) {
        unsafe wl_output_send_description(target, description)
    }
    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> any WlOutputRequests? {
        guard let ud = unsafe wl_resource_get_user_data(res) else {
            return nil
        }
        return unsafe Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? any WlOutputRequests
    }
    private static let release_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res = unsafe res else {
            return
        }
        if let h = unsafe handler(res) {
            nonisolated(unsafe) let requestHandler = h
            nonisolated(unsafe) let requestResource = unsafe res
            MainActor.assumeIsolated {
                unsafe requestHandler.release(WaylandRequest<WlOutputServer>(requestResource))
            }
        } else {
            unsafe wl_resource_destroy(res)
        }
    }
}
public extension WaylandResourceHandle where Interface == WlOutputServer {
    var supportsDone: Bool {
        guard let version else {
            return false
        }
        return version >= 2
    }
    var supportsScale: Bool {
        guard let version else {
            return false
        }
        return version >= 2
    }
    var supportsName: Bool {
        guard let version else {
            return false
        }
        return version >= 4
    }
    var supportsDescription: Bool {
        guard let version else {
            return false
        }
        return version >= 4
    }
    @discardableResult
    func sendGeometry(x: Int32, y: Int32, physical_width: Int32, physical_height: Int32, subpixel: WlOutputSubpixel, make: String, model: String, transform: WlOutputTransform) -> Bool {
        guard let target = unsafe resource else {
            return false
        }
        return unsafe withWaylandEventCString(make) { _event_makeCString in
            return unsafe withWaylandEventCString(model) { _event_modelCString in
                unsafe wl_output_send_geometry(target, x, y, physical_width, physical_height, Int32(bitPattern: subpixel.rawValue), _event_makeCString, _event_modelCString, Int32(bitPattern: transform.rawValue))
                return true
            }
        }
    }
    @discardableResult
    func sendMode(flags: WlOutputMode, width: Int32, height: Int32, refresh: Int32) -> Bool {
        guard let target = unsafe resource else {
            return false
        }
        unsafe wl_output_send_mode(target, flags.rawValue, width, height, refresh)
        return true
    }
    @discardableResult
    func sendDone() -> Bool {
        guard let target = unsafe resource else {
            return false
        }
        precondition(supportsDone, "wl_output.done requires version 2")
        unsafe wl_output_send_done(target)
        return true
    }
    @discardableResult
    func sendScale(factor: Int32) -> Bool {
        guard let target = unsafe resource else {
            return false
        }
        precondition(supportsScale, "wl_output.scale requires version 2")
        unsafe wl_output_send_scale(target, factor)
        return true
    }
    @discardableResult
    func sendName(name: String) -> Bool {
        guard let target = unsafe resource else {
            return false
        }
        precondition(supportsName, "wl_output.name requires version 4")
        return unsafe withWaylandEventCString(name) { _event_nameCString in
            unsafe wl_output_send_name(target, _event_nameCString)
            return true
        }
    }
    @discardableResult
    func sendDescription(description: String) -> Bool {
        guard let target = unsafe resource else {
            return false
        }
        precondition(supportsDescription, "wl_output.description requires version 4")
        return unsafe withWaylandEventCString(description) { _event_descriptionCString in
            unsafe wl_output_send_description(target, _event_descriptionCString)
            return true
        }
    }
}
public extension WlNewId where Interface == WlOutputServer {
    @discardableResult
    @MainActor
    func create<Owner: AnyObject>(
        owner: (WaylandResourceHandle<WlOutputServer>) -> Owner?,
        installed: (Owner) -> Void = { _ in
        }
    ) -> Owner? {
        unsafe _create(vtable: WlOutputServer.requestVtable(), owner: owner, installed: installed)
    }
}
public extension WlOutputServer {
    @discardableResult
    @MainActor
    static func createResource<Owner: AnyObject>(
        client: OpaquePointer,
        version: Int32,
        id: UInt32 = 0,
        owner: (WaylandResourceHandle<WlOutputServer>) -> Owner?,
        installed: (Owner) -> Void = { _ in
        }
    ) -> Owner? {
        unsafe WaylandResource.create(
            client: client, interface: WlOutputServer.self,
            version: Swift.min(version, maximumVersion), id: id,
            vtable: requestVtable(), owner: owner,
            installed: installed)
    }
    @MainActor
    static func global<Implementation: AnyObject, Owner: AnyObject>(
        implementation: Implementation,
        advertisedVersion: Int32 = maximumVersion,
        owner: @escaping (Implementation, WaylandResourceHandle<WlOutputServer>) -> Owner?,
        installed: @escaping (Implementation, Owner, WaylandResourceHandle<WlOutputServer>) -> Void = { _, _, _ in
        }
    ) -> WaylandGlobalSpecification<WlOutputServer> {
        unsafe WaylandGlobalSpecification(
            implementation: implementation,
            advertisedVersion: advertisedVersion,
            vtable: requestVtable(), owner: owner,
            installed: installed)
    }
}