// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for wp_security_context_v1: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

import WaylandServerC
import WaylandServer

public protocol WpSecurityContextV1Requests: AnyObject {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
    func setSandboxEngine(_ resource: UnsafeMutablePointer<wl_resource>, name: UnsafePointer<CChar>?)
    func setAppId(_ resource: UnsafeMutablePointer<wl_resource>, app_id: UnsafePointer<CChar>?)
    func setInstanceId(_ resource: UnsafeMutablePointer<wl_resource>, instance_id: UnsafePointer<CChar>?)
    func commit(_ resource: UnsafeMutablePointer<wl_resource>)
}

public extension WpSecurityContextV1Requests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum WpSecurityContextV1Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_wp_security_context_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_wp_security_context_v1_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_wp_security_context_v1_requests.self, capacity: 1)
        vt.pointee.destroy = destroy_impl
        vt.pointee.set_sandbox_engine = setSandboxEngine_impl
        vt.pointee.set_app_id = setAppId_impl
        vt.pointee.set_instance_id = setInstanceId_impl
        vt.pointee.commit = commit_impl
        return UnsafeRawPointer(raw)
    }()

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> WpSecurityContextV1Requests? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? WpSecurityContextV1Requests
    }

    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
    private static let setSandboxEngine_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafePointer<CChar>?) -> Void = { _, res, name in
        guard let res, let h = handler(res) else { return }
        h.setSandboxEngine(res, name: name)
    }
    private static let setAppId_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafePointer<CChar>?) -> Void = { _, res, app_id in
        guard let res, let h = handler(res) else { return }
        h.setAppId(res, app_id: app_id)
    }
    private static let setInstanceId_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafePointer<CChar>?) -> Void = { _, res, instance_id in
        guard let res, let h = handler(res) else { return }
        h.setInstanceId(res, instance_id: instance_id)
    }
    private static let commit_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res, let h = handler(res) else { return }
        h.commit(res)
    }
}
