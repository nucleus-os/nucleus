// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for ext_image_copy_capture_frame_v1: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

import WaylandServerC
import WaylandServer

public protocol ExtImageCopyCaptureFrameV1Requests: AnyObject {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
    func attachBuffer(_ resource: UnsafeMutablePointer<wl_resource>, buffer: UnsafeMutablePointer<wl_resource>?)
    func damageBuffer(_ resource: UnsafeMutablePointer<wl_resource>, x: Int32, y: Int32, width: Int32, height: Int32)
    func capture(_ resource: UnsafeMutablePointer<wl_resource>)
}

public extension ExtImageCopyCaptureFrameV1Requests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum ExtImageCopyCaptureFrameV1Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_ext_image_copy_capture_frame_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_ext_image_copy_capture_frame_v1_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_ext_image_copy_capture_frame_v1_requests.self, capacity: 1)
        vt.pointee.destroy = destroy_impl
        vt.pointee.attach_buffer = attachBuffer_impl
        vt.pointee.damage_buffer = damageBuffer_impl
        vt.pointee.capture = capture_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendTransform(_ target: UnsafeMutablePointer<wl_resource>, transform: UInt32) {
        ext_image_copy_capture_frame_v1_send_transform(target, transform)
    }
    public static func sendDamage(_ target: UnsafeMutablePointer<wl_resource>, x: Int32, y: Int32, width: Int32, height: Int32) {
        ext_image_copy_capture_frame_v1_send_damage(target, x, y, width, height)
    }
    public static func sendPresentationTime(_ target: UnsafeMutablePointer<wl_resource>, tv_sec_hi: UInt32, tv_sec_lo: UInt32, tv_nsec: UInt32) {
        ext_image_copy_capture_frame_v1_send_presentation_time(target, tv_sec_hi, tv_sec_lo, tv_nsec)
    }
    public static func sendReady(_ target: UnsafeMutablePointer<wl_resource>) {
        ext_image_copy_capture_frame_v1_send_ready(target)
    }
    public static func sendFailed(_ target: UnsafeMutablePointer<wl_resource>, reason: UInt32) {
        ext_image_copy_capture_frame_v1_send_failed(target, reason)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> ExtImageCopyCaptureFrameV1Requests? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? ExtImageCopyCaptureFrameV1Requests
    }

    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
    private static let attachBuffer_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res, buffer in
        guard let res, let h = handler(res) else { return }
        h.attachBuffer(res, buffer: buffer)
    }
    private static let damageBuffer_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, Int32, Int32, Int32, Int32) -> Void = { _, res, x, y, width, height in
        guard let res, let h = handler(res) else { return }
        h.damageBuffer(res, x: x, y: y, width: width, height: height)
    }
    private static let capture_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res, let h = handler(res) else { return }
        h.capture(res)
    }
}
