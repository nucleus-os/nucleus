// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for wl_subsurface: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

public import WaylandServerC
import WaylandServer

public protocol WlSubsurfaceRequests: AnyObject {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
    func setPosition(_ resource: UnsafeMutablePointer<wl_resource>, x: Int32, y: Int32)
    func placeAbove(_ resource: UnsafeMutablePointer<wl_resource>, sibling: UnsafeMutablePointer<wl_resource>?)
    func placeBelow(_ resource: UnsafeMutablePointer<wl_resource>, sibling: UnsafeMutablePointer<wl_resource>?)
    func setSync(_ resource: UnsafeMutablePointer<wl_resource>)
    func setDesync(_ resource: UnsafeMutablePointer<wl_resource>)
}

public extension WlSubsurfaceRequests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum WlSubsurfaceServer {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_wl_subsurface_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_wl_subsurface_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_wl_subsurface_requests.self, capacity: 1)
        vt.pointee.destroy = destroy_impl
        vt.pointee.set_position = setPosition_impl
        vt.pointee.place_above = placeAbove_impl
        vt.pointee.place_below = placeBelow_impl
        vt.pointee.set_sync = setSync_impl
        vt.pointee.set_desync = setDesync_impl
        return UnsafeRawPointer(raw)
    }()

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> (any WlSubsurfaceRequests)? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? any WlSubsurfaceRequests
    }

    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
    private static let setPosition_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, Int32, Int32) -> Void = { _, res, x, y in
        guard let res, let h = handler(res) else { return }
        h.setPosition(res, x: x, y: y)
    }
    private static let placeAbove_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res, sibling in
        guard let res, let h = handler(res) else { return }
        h.placeAbove(res, sibling: sibling)
    }
    private static let placeBelow_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res, sibling in
        guard let res, let h = handler(res) else { return }
        h.placeBelow(res, sibling: sibling)
    }
    private static let setSync_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res, let h = handler(res) else { return }
        h.setSync(res)
    }
    private static let setDesync_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res, let h = handler(res) else { return }
        h.setDesync(res)
    }
}
