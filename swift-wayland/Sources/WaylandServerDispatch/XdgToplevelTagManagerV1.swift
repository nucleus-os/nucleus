// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for xdg_toplevel_tag_manager_v1: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

import WaylandServerC
import WaylandServer

public protocol XdgToplevelTagManagerV1Requests: AnyObject {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
    func setToplevelTag(_ resource: UnsafeMutablePointer<wl_resource>, toplevel: UnsafeMutablePointer<wl_resource>?, tag: UnsafePointer<CChar>?)
    func setToplevelDescription(_ resource: UnsafeMutablePointer<wl_resource>, toplevel: UnsafeMutablePointer<wl_resource>?, description: UnsafePointer<CChar>?)
}

public extension XdgToplevelTagManagerV1Requests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum XdgToplevelTagManagerV1Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_xdg_toplevel_tag_manager_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_xdg_toplevel_tag_manager_v1_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_xdg_toplevel_tag_manager_v1_requests.self, capacity: 1)
        vt.pointee.destroy = destroy_impl
        vt.pointee.set_toplevel_tag = setToplevelTag_impl
        vt.pointee.set_toplevel_description = setToplevelDescription_impl
        return UnsafeRawPointer(raw)
    }()

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> XdgToplevelTagManagerV1Requests? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? XdgToplevelTagManagerV1Requests
    }

    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
    private static let setToplevelTag_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?, UnsafePointer<CChar>?) -> Void = { _, res, toplevel, tag in
        guard let res, let h = handler(res) else { return }
        h.setToplevelTag(res, toplevel: toplevel, tag: tag)
    }
    private static let setToplevelDescription_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?, UnsafePointer<CChar>?) -> Void = { _, res, toplevel, description in
        guard let res, let h = handler(res) else { return }
        h.setToplevelDescription(res, toplevel: toplevel, description: description)
    }
}
