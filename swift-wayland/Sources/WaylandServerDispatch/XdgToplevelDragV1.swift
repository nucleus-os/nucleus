// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for xdg_toplevel_drag_v1: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

public import WaylandServerC
import WaylandServer

public protocol XdgToplevelDragV1Requests: AnyObject {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
    func attach(_ resource: UnsafeMutablePointer<wl_resource>, toplevel: UnsafeMutablePointer<wl_resource>?, x_offset: Int32, y_offset: Int32)
}

public extension XdgToplevelDragV1Requests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum XdgToplevelDragV1Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_xdg_toplevel_drag_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_xdg_toplevel_drag_v1_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_xdg_toplevel_drag_v1_requests.self, capacity: 1)
        vt.pointee.destroy = destroy_impl
        vt.pointee.attach = attach_impl
        return UnsafeRawPointer(raw)
    }()

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> (any XdgToplevelDragV1Requests)? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? any XdgToplevelDragV1Requests
    }

    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
    private static let attach_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?, Int32, Int32) -> Void = { _, res, toplevel, x_offset, y_offset in
        guard let res, let h = handler(res) else { return }
        h.attach(res, toplevel: toplevel, x_offset: x_offset, y_offset: y_offset)
    }
}
