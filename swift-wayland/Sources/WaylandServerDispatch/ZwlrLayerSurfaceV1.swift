// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for zwlr_layer_surface_v1: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

import WaylandServerC
import WaylandServer

public protocol ZwlrLayerSurfaceV1Requests: AnyObject {
    func setSize(_ resource: UnsafeMutablePointer<wl_resource>, width: UInt32, height: UInt32)
    func setAnchor(_ resource: UnsafeMutablePointer<wl_resource>, anchor: UInt32)
    func setExclusiveZone(_ resource: UnsafeMutablePointer<wl_resource>, zone: Int32)
    func setMargin(_ resource: UnsafeMutablePointer<wl_resource>, top: Int32, right: Int32, bottom: Int32, left: Int32)
    func setKeyboardInteractivity(_ resource: UnsafeMutablePointer<wl_resource>, keyboard_interactivity: UInt32)
    func getPopup(_ resource: UnsafeMutablePointer<wl_resource>, popup: UnsafeMutablePointer<wl_resource>?)
    func ackConfigure(_ resource: UnsafeMutablePointer<wl_resource>, serial: UInt32)
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
    func setLayer(_ resource: UnsafeMutablePointer<wl_resource>, layer: UInt32)
    func setExclusiveEdge(_ resource: UnsafeMutablePointer<wl_resource>, edge: UInt32)
}

public extension ZwlrLayerSurfaceV1Requests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum ZwlrLayerSurfaceV1Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_zwlr_layer_surface_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_zwlr_layer_surface_v1_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_zwlr_layer_surface_v1_requests.self, capacity: 1)
        vt.pointee.set_size = setSize_impl
        vt.pointee.set_anchor = setAnchor_impl
        vt.pointee.set_exclusive_zone = setExclusiveZone_impl
        vt.pointee.set_margin = setMargin_impl
        vt.pointee.set_keyboard_interactivity = setKeyboardInteractivity_impl
        vt.pointee.get_popup = getPopup_impl
        vt.pointee.ack_configure = ackConfigure_impl
        vt.pointee.destroy = destroy_impl
        vt.pointee.set_layer = setLayer_impl
        vt.pointee.set_exclusive_edge = setExclusiveEdge_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendConfigure(_ target: UnsafeMutablePointer<wl_resource>, serial: UInt32, width: UInt32, height: UInt32) {
        zwlr_layer_surface_v1_send_configure(target, serial, width, height)
    }
    public static func sendClosed(_ target: UnsafeMutablePointer<wl_resource>) {
        zwlr_layer_surface_v1_send_closed(target)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> ZwlrLayerSurfaceV1Requests? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? ZwlrLayerSurfaceV1Requests
    }

    private static let setSize_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UInt32) -> Void = { _, res, width, height in
        guard let res, let h = handler(res) else { return }
        h.setSize(res, width: width, height: height)
    }
    private static let setAnchor_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { _, res, anchor in
        guard let res, let h = handler(res) else { return }
        h.setAnchor(res, anchor: anchor)
    }
    private static let setExclusiveZone_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, Int32) -> Void = { _, res, zone in
        guard let res, let h = handler(res) else { return }
        h.setExclusiveZone(res, zone: zone)
    }
    private static let setMargin_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, Int32, Int32, Int32, Int32) -> Void = { _, res, top, right, bottom, left in
        guard let res, let h = handler(res) else { return }
        h.setMargin(res, top: top, right: right, bottom: bottom, left: left)
    }
    private static let setKeyboardInteractivity_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { _, res, keyboard_interactivity in
        guard let res, let h = handler(res) else { return }
        h.setKeyboardInteractivity(res, keyboard_interactivity: keyboard_interactivity)
    }
    private static let getPopup_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res, popup in
        guard let res, let h = handler(res) else { return }
        h.getPopup(res, popup: popup)
    }
    private static let ackConfigure_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { _, res, serial in
        guard let res, let h = handler(res) else { return }
        h.ackConfigure(res, serial: serial)
    }
    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
    private static let setLayer_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { _, res, layer in
        guard let res, let h = handler(res) else { return }
        h.setLayer(res, layer: layer)
    }
    private static let setExclusiveEdge_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { _, res, edge in
        guard let res, let h = handler(res) else { return }
        h.setExclusiveEdge(res, edge: edge)
    }
}
