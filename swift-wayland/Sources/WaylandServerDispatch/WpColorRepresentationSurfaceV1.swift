// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for wp_color_representation_surface_v1: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

import WaylandServerC
import WaylandServer

public protocol WpColorRepresentationSurfaceV1Requests: AnyObject {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
    func setAlphaMode(_ resource: UnsafeMutablePointer<wl_resource>, alpha_mode: UInt32)
    func setCoefficientsAndRange(_ resource: UnsafeMutablePointer<wl_resource>, coefficients: UInt32, range: UInt32)
    func setChromaLocation(_ resource: UnsafeMutablePointer<wl_resource>, chroma_location: UInt32)
}

public extension WpColorRepresentationSurfaceV1Requests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum WpColorRepresentationSurfaceV1Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_wp_color_representation_surface_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_wp_color_representation_surface_v1_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_wp_color_representation_surface_v1_requests.self, capacity: 1)
        vt.pointee.destroy = destroy_impl
        vt.pointee.set_alpha_mode = setAlphaMode_impl
        vt.pointee.set_coefficients_and_range = setCoefficientsAndRange_impl
        vt.pointee.set_chroma_location = setChromaLocation_impl
        return UnsafeRawPointer(raw)
    }()

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> WpColorRepresentationSurfaceV1Requests? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? WpColorRepresentationSurfaceV1Requests
    }

    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
    private static let setAlphaMode_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { _, res, alpha_mode in
        guard let res, let h = handler(res) else { return }
        h.setAlphaMode(res, alpha_mode: alpha_mode)
    }
    private static let setCoefficientsAndRange_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UInt32) -> Void = { _, res, coefficients, range in
        guard let res, let h = handler(res) else { return }
        h.setCoefficientsAndRange(res, coefficients: coefficients, range: range)
    }
    private static let setChromaLocation_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { _, res, chroma_location in
        guard let res, let h = handler(res) else { return }
        h.setChromaLocation(res, chroma_location: chroma_location)
    }
}
