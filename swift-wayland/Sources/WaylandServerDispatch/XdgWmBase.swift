// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for xdg_wm_base: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

public import WaylandServerC
public import WaylandServer

public protocol XdgWmBaseRequests: AnyObject {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
    func createPositioner(_ resource: UnsafeMutablePointer<wl_resource>, id: WlNewId)
    func getXdgSurface(_ resource: UnsafeMutablePointer<wl_resource>, id: WlNewId, surface: UnsafeMutablePointer<wl_resource>?)
    func pong(_ resource: UnsafeMutablePointer<wl_resource>, serial: UInt32)
}

public extension XdgWmBaseRequests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum XdgWmBaseServer {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_xdg_wm_base_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_xdg_wm_base_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_xdg_wm_base_requests.self, capacity: 1)
        vt.pointee.destroy = destroy_impl
        vt.pointee.create_positioner = createPositioner_impl
        vt.pointee.get_xdg_surface = getXdgSurface_impl
        vt.pointee.pong = pong_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendPing(_ target: UnsafeMutablePointer<wl_resource>, serial: UInt32) {
        xdg_wm_base_send_ping(target, serial)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> (any XdgWmBaseRequests)? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? any XdgWmBaseRequests
    }

    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
    private static let createPositioner_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { client, res, id in
        guard let res, let client, let h = handler(res) else { return }
        h.createPositioner(res, id: WlNewId(client: client, id: id, version: Swift::min(wl_resource_get_version(res), Int32(7)), interface: swift_wayland_iface_xdg_positioner()))
    }
    private static let getXdgSurface_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UnsafeMutablePointer<wl_resource>?) -> Void = { client, res, id, surface in
        guard let res, let client, let h = handler(res) else { return }
        h.getXdgSurface(res, id: WlNewId(client: client, id: id, version: Swift::min(wl_resource_get_version(res), Int32(7)), interface: swift_wayland_iface_xdg_surface()), surface: surface)
    }
    private static let pong_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32) -> Void = { _, res, serial in
        guard let res, let h = handler(res) else { return }
        h.pong(res, serial: serial)
    }
}
