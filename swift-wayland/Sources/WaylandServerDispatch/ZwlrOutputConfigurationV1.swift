// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for zwlr_output_configuration_v1: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

public import WaylandServerC
public import WaylandServer

public protocol ZwlrOutputConfigurationV1Requests: AnyObject {
    func enableHead(_ resource: UnsafeMutablePointer<wl_resource>, id: WlNewId, head: UnsafeMutablePointer<wl_resource>?)
    func disableHead(_ resource: UnsafeMutablePointer<wl_resource>, head: UnsafeMutablePointer<wl_resource>?)
    func apply(_ resource: UnsafeMutablePointer<wl_resource>)
    func test(_ resource: UnsafeMutablePointer<wl_resource>)
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
}

public extension ZwlrOutputConfigurationV1Requests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum ZwlrOutputConfigurationV1Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_zwlr_output_configuration_v1_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_zwlr_output_configuration_v1_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_zwlr_output_configuration_v1_requests.self, capacity: 1)
        vt.pointee.enable_head = enableHead_impl
        vt.pointee.disable_head = disableHead_impl
        vt.pointee.apply = apply_impl
        vt.pointee.test = test_impl
        vt.pointee.destroy = destroy_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendSucceeded(_ target: UnsafeMutablePointer<wl_resource>) {
        zwlr_output_configuration_v1_send_succeeded(target)
    }
    public static func sendFailed(_ target: UnsafeMutablePointer<wl_resource>) {
        zwlr_output_configuration_v1_send_failed(target)
    }
    public static func sendCancelled(_ target: UnsafeMutablePointer<wl_resource>) {
        zwlr_output_configuration_v1_send_cancelled(target)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> (any ZwlrOutputConfigurationV1Requests)? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? any ZwlrOutputConfigurationV1Requests
    }

    private static let enableHead_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UnsafeMutablePointer<wl_resource>?) -> Void = { client, res, id, head in
        guard let res, let client, let h = handler(res) else { return }
        h.enableHead(res, id: WlNewId(client: client, id: id, version: Swift::min(wl_resource_get_version(res), Int32(4)), interface: swift_wayland_iface_zwlr_output_configuration_head_v1()), head: head)
    }
    private static let disableHead_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res, head in
        guard let res, let h = handler(res) else { return }
        h.disableHead(res, head: head)
    }
    private static let apply_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res, let h = handler(res) else { return }
        h.apply(res)
    }
    private static let test_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res, let h = handler(res) else { return }
        h.test(res)
    }
    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
}
