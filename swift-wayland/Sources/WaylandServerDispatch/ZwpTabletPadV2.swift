// Generated by SwiftWaylandGen. Do not edit.
//
// Typed server dispatch for zwp_tablet_pad_v2: a handler protocol (one method per request), the
// request vtable + owner recovery + arg marshalling, and typed event senders.

import WaylandServerC
import WaylandServer

public protocol ZwpTabletPadV2Requests: AnyObject {
    func setFeedback(_ resource: UnsafeMutablePointer<wl_resource>, button: UInt32, description: UnsafePointer<CChar>?, serial: UInt32)
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>)
}

public extension ZwpTabletPadV2Requests {
    func destroy(_ resource: UnsafeMutablePointer<wl_resource>) { wl_resource_destroy(resource) }
}

public enum ZwpTabletPadV2Server {
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_zwp_tablet_pad_v2_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_zwp_tablet_pad_v2_requests>.alignment)
        raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = raw.bindMemory(to: swift_wayland_zwp_tablet_pad_v2_requests.self, capacity: 1)
        vt.pointee.set_feedback = setFeedback_impl
        vt.pointee.destroy = destroy_impl
        return UnsafeRawPointer(raw)
    }()

    public static func sendGroup(_ target: UnsafeMutablePointer<wl_resource>, pad_group: UnsafeMutablePointer<wl_resource>?) {
        zwp_tablet_pad_v2_send_group(target, pad_group)
    }
    public static func sendPath(_ target: UnsafeMutablePointer<wl_resource>, path: UnsafePointer<CChar>?) {
        zwp_tablet_pad_v2_send_path(target, path)
    }
    public static func sendButtons(_ target: UnsafeMutablePointer<wl_resource>, buttons: UInt32) {
        zwp_tablet_pad_v2_send_buttons(target, buttons)
    }
    public static func sendDone(_ target: UnsafeMutablePointer<wl_resource>) {
        zwp_tablet_pad_v2_send_done(target)
    }
    public static func sendButton(_ target: UnsafeMutablePointer<wl_resource>, time: UInt32, button: UInt32, state: UInt32) {
        zwp_tablet_pad_v2_send_button(target, time, button, state)
    }
    public static func sendEnter(_ target: UnsafeMutablePointer<wl_resource>, serial: UInt32, tablet: UnsafeMutablePointer<wl_resource>?, surface: UnsafeMutablePointer<wl_resource>?) {
        zwp_tablet_pad_v2_send_enter(target, serial, tablet, surface)
    }
    public static func sendLeave(_ target: UnsafeMutablePointer<wl_resource>, serial: UInt32, surface: UnsafeMutablePointer<wl_resource>?) {
        zwp_tablet_pad_v2_send_leave(target, serial, surface)
    }
    public static func sendRemoved(_ target: UnsafeMutablePointer<wl_resource>) {
        zwp_tablet_pad_v2_send_removed(target)
    }

    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> ZwpTabletPadV2Requests? {
        guard let ud = wl_resource_get_user_data(res) else { return nil }
        return Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? ZwpTabletPadV2Requests
    }

    private static let setFeedback_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UnsafePointer<CChar>?, UInt32) -> Void = { _, res, button, description, serial in
        guard let res, let h = handler(res) else { return }
        h.setFeedback(res, button: button, description: description, serial: serial)
    }
    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res else { return }
        if let h = handler(res) { h.destroy(res) } else { wl_resource_destroy(res) }
    }
}
