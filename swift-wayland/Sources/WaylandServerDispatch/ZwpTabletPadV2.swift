// Generated by SwiftWaylandGen. Do not edit.
// Typed server descriptor and dispatch for zwp_tablet_pad_v2.

public import WaylandServerC
public import WaylandServer
public import WaylandProtocolTypes
@MainActor public protocol ZwpTabletPadV2Requests: AnyObject {
    func setFeedback(_ request: WaylandRequest<ZwpTabletPadV2Server>, button: UInt32, description: String, serial: UInt32)
    func destroy(_ request: WaylandRequest<ZwpTabletPadV2Server>)
}
public extension ZwpTabletPadV2Requests {
    func destroy(_ request: WaylandRequest<ZwpTabletPadV2Server>) {
        unsafe wl_resource_destroy(request.resource)
    }
}
public enum ZwpTabletPadV2Server: WaylandServerInterface {
    public nonisolated(unsafe) static let interface = unsafe swift_wayland_iface_zwp_tablet_pad_v2()
    public nonisolated static let maximumVersion: Int32 = 2
    public nonisolated(unsafe) static let vtable: UnsafeRawPointer = {
        let size = MemoryLayout<swift_wayland_zwp_tablet_pad_v2_requests>.stride
        let raw = UnsafeMutableRawPointer.allocate(
            byteCount: size, alignment: MemoryLayout<swift_wayland_zwp_tablet_pad_v2_requests>.alignment)
        unsafe raw.initializeMemory(as: UInt8.self, repeating: 0, count: size)
        let vt = unsafe raw.bindMemory(to: swift_wayland_zwp_tablet_pad_v2_requests.self, capacity: 1)
        unsafe vt.pointee.set_feedback = setFeedback_impl
        unsafe vt.pointee.destroy = destroy_impl
        return UnsafeRawPointer(raw)
    }()
    public nonisolated static func requestVtable() -> UnsafeRawPointer? {
        unsafe vtable
    }
    public static func sendGroup(_ target: UnsafeMutablePointer<wl_resource>, pad_group: UnsafeMutablePointer<wl_resource>?) {
        unsafe zwp_tablet_pad_v2_send_group(target, pad_group)
    }
    public static func sendPath(_ target: UnsafeMutablePointer<wl_resource>, path: UnsafePointer<CChar>?) {
        unsafe zwp_tablet_pad_v2_send_path(target, path)
    }
    public static func sendButtons(_ target: UnsafeMutablePointer<wl_resource>, buttons: UInt32) {
        unsafe zwp_tablet_pad_v2_send_buttons(target, buttons)
    }
    public static func sendDone(_ target: UnsafeMutablePointer<wl_resource>) {
        unsafe zwp_tablet_pad_v2_send_done(target)
    }
    public static func sendButton(_ target: UnsafeMutablePointer<wl_resource>, time: UInt32, button: UInt32, state: ZwpTabletPadV2ButtonState) {
        unsafe zwp_tablet_pad_v2_send_button(target, time, button, state.rawValue)
    }
    public static func sendEnter(_ target: UnsafeMutablePointer<wl_resource>, serial: UInt32, tablet: UnsafeMutablePointer<wl_resource>?, surface: UnsafeMutablePointer<wl_resource>?) {
        unsafe zwp_tablet_pad_v2_send_enter(target, serial, tablet, surface)
    }
    public static func sendLeave(_ target: UnsafeMutablePointer<wl_resource>, serial: UInt32, surface: UnsafeMutablePointer<wl_resource>?) {
        unsafe zwp_tablet_pad_v2_send_leave(target, serial, surface)
    }
    public static func sendRemoved(_ target: UnsafeMutablePointer<wl_resource>) {
        unsafe zwp_tablet_pad_v2_send_removed(target)
    }
    private static func handler(_ res: UnsafeMutablePointer<wl_resource>) -> any ZwpTabletPadV2Requests? {
        guard let ud = unsafe wl_resource_get_user_data(res) else {
            return nil
        }
        return unsafe Unmanaged<AnyObject>.fromOpaque(ud).takeUnretainedValue() as? any ZwpTabletPadV2Requests
    }
    private static let setFeedback_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?, UInt32, UnsafePointer<CChar>?, UInt32) -> Void = { _, res, button, description, serial in
        guard let res = unsafe res, let h = unsafe handler(res) else {
            return
        }
        nonisolated(unsafe) let requestHandler = h
        nonisolated(unsafe) let requestResource = unsafe res
        nonisolated(unsafe) let _request_description = unsafe description
        MainActor.assumeIsolated {
            unsafe requestHandler.setFeedback(WaylandRequest<ZwpTabletPadV2Server>(requestResource), button: button, description: unsafe String(cString: _request_description!), serial: serial)
        }
    }
    private static let destroy_impl: @convention(c) (OpaquePointer?, UnsafeMutablePointer<wl_resource>?) -> Void = { _, res in
        guard let res = unsafe res else {
            return
        }
        if let h = unsafe handler(res) {
            nonisolated(unsafe) let requestHandler = h
            nonisolated(unsafe) let requestResource = unsafe res
            MainActor.assumeIsolated {
                unsafe requestHandler.destroy(WaylandRequest<ZwpTabletPadV2Server>(requestResource))
            }
        } else {
            unsafe wl_resource_destroy(res)
        }
    }
}
public extension WaylandResourceHandle where Interface == ZwpTabletPadV2Server {
    @discardableResult
    func sendGroup(pad_group: WaylandResourceHandle<ZwpTabletPadGroupV2Server>) -> Bool {
        guard let target = unsafe resource else {
            return false
        }
        guard let _event_pad_groupResource = unsafe pad_group.resource else {
            return false
        }
        unsafe zwp_tablet_pad_v2_send_group(target, _event_pad_groupResource)
        return true
    }
    @discardableResult
    func sendPath(path: String) -> Bool {
        guard let target = unsafe resource else {
            return false
        }
        return unsafe withWaylandEventCString(path) { _event_pathCString in
            unsafe zwp_tablet_pad_v2_send_path(target, _event_pathCString)
            return true
        }
    }
    @discardableResult
    func sendButtons(buttons: UInt32) -> Bool {
        guard let target = unsafe resource else {
            return false
        }
        unsafe zwp_tablet_pad_v2_send_buttons(target, buttons)
        return true
    }
    @discardableResult
    func sendDone() -> Bool {
        guard let target = unsafe resource else {
            return false
        }
        unsafe zwp_tablet_pad_v2_send_done(target)
        return true
    }
    @discardableResult
    func sendButton(time: UInt32, button: UInt32, state: ZwpTabletPadV2ButtonState) -> Bool {
        guard let target = unsafe resource else {
            return false
        }
        unsafe zwp_tablet_pad_v2_send_button(target, time, button, state.rawValue)
        return true
    }
    @discardableResult
    func sendEnter(serial: UInt32, tablet: WaylandResourceHandle<ZwpTabletV2Server>, surface: WaylandResourceHandle<WlSurfaceServer>) -> Bool {
        guard let target = unsafe resource else {
            return false
        }
        guard let _event_tabletResource = unsafe tablet.resource else {
            return false
        }
        guard let _event_surfaceResource = unsafe surface.resource else {
            return false
        }
        unsafe zwp_tablet_pad_v2_send_enter(target, serial, _event_tabletResource, _event_surfaceResource)
        return true
    }
    @discardableResult
    func sendLeave(serial: UInt32, surface: WaylandResourceHandle<WlSurfaceServer>) -> Bool {
        guard let target = unsafe resource else {
            return false
        }
        guard let _event_surfaceResource = unsafe surface.resource else {
            return false
        }
        unsafe zwp_tablet_pad_v2_send_leave(target, serial, _event_surfaceResource)
        return true
    }
    @discardableResult
    func sendRemoved() -> Bool {
        guard let target = unsafe resource else {
            return false
        }
        unsafe zwp_tablet_pad_v2_send_removed(target)
        return true
    }
}
public extension WlNewId where Interface == ZwpTabletPadV2Server {
    @discardableResult
    @MainActor
    func create<Owner: ZwpTabletPadV2Requests>(
        owner: (WaylandResourceHandle<ZwpTabletPadV2Server>) -> Owner?,
        installed: (Owner) -> Void = { _ in
        }
    ) -> Owner? {
        unsafe _create(vtable: ZwpTabletPadV2Server.requestVtable(), owner: owner, installed: installed)
    }
}
public extension ZwpTabletPadV2Server {
    @discardableResult
    @MainActor
    static func createResource<Owner: ZwpTabletPadV2Requests>(
        client: OpaquePointer,
        version: Int32,
        id: UInt32 = 0,
        owner: (WaylandResourceHandle<ZwpTabletPadV2Server>) -> Owner?,
        installed: (Owner) -> Void = { _ in
        }
    ) -> Owner? {
        unsafe WaylandResource.create(
            client: client, interface: ZwpTabletPadV2Server.self,
            version: Swift.min(version, maximumVersion), id: id,
            vtable: requestVtable(), owner: owner,
            installed: installed)
    }
    @MainActor
    static func global<Implementation: AnyObject, Owner: ZwpTabletPadV2Requests>(
        implementation: Implementation,
        advertisedVersion: Int32 = maximumVersion,
        owner: @escaping (Implementation, WaylandResourceHandle<ZwpTabletPadV2Server>) -> Owner?,
        installed: @escaping (Implementation, Owner, WaylandResourceHandle<ZwpTabletPadV2Server>) -> Void = { _, _, _ in
        }
    ) -> WaylandGlobalSpecification<ZwpTabletPadV2Server> {
        unsafe WaylandGlobalSpecification(
            implementation: implementation,
            advertisedVersion: advertisedVersion,
            vtable: requestVtable(), owner: owner,
            installed: installed)
    }
}