// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zwp_relative_pointer_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol ZwpRelativePointerV1Events: AnyObject {
    func relativeMotion(_ proxy: OpaquePointer, utime_hi: UInt32, utime_lo: UInt32, dx: Double, dy: Double, dx_unaccel: Double, dy_unaccel: Double)
}

public enum ZwpRelativePointerV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zwp_relative_pointer_v1_listener> = {
        let p = UnsafeMutablePointer<zwp_relative_pointer_v1_listener>.allocate(capacity: 1)
        p.initialize(to: zwp_relative_pointer_v1_listener())
        p.pointee.relative_motion = relativeMotion_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zwp_relative_pointer_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> ZwpRelativePointerV1Events? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? ZwpRelativePointerV1Events
    }

    private static let relativeMotion_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32, wl_fixed_t, wl_fixed_t, wl_fixed_t, wl_fixed_t) -> Void = { data, proxy, utime_hi, utime_lo, dx, dy, dx_unaccel, dy_unaccel in
        guard let data, let proxy, let h = handler(data) else { return }
        h.relativeMotion(proxy, utime_hi: utime_hi, utime_lo: utime_lo, dx: swift_wayland_fixed_to_double(dx), dy: swift_wayland_fixed_to_double(dy), dx_unaccel: swift_wayland_fixed_to_double(dx_unaccel), dy_unaccel: swift_wayland_fixed_to_double(dy_unaccel))
    }
}
