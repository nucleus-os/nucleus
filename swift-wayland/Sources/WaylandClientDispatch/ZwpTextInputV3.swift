// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zwp_text_input_v3: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ZwpTextInputV3Events: AnyObject {
    func enter(_ proxy: OpaquePointer, surface: OpaquePointer?)
    func leave(_ proxy: OpaquePointer, surface: OpaquePointer?)
    func preeditString(_ proxy: OpaquePointer, text: UnsafePointer<CChar>?, cursor_begin: Int32, cursor_end: Int32)
    func commitString(_ proxy: OpaquePointer, text: UnsafePointer<CChar>?)
    func deleteSurroundingText(_ proxy: OpaquePointer, before_length: UInt32, after_length: UInt32)
    func done(_ proxy: OpaquePointer, serial: UInt32)
    func action(_ proxy: OpaquePointer, action: UInt32, serial: UInt32)
    func language(_ proxy: OpaquePointer, language: UnsafePointer<CChar>?)
    func preeditHint(_ proxy: OpaquePointer, start: UInt32, end: UInt32, hint: UInt32)
}

public enum ZwpTextInputV3Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zwp_text_input_v3_listener> = {
        let p = UnsafeMutablePointer<zwp_text_input_v3_listener>.allocate(capacity: 1)
        p.initialize(to: zwp_text_input_v3_listener())
        p.pointee.enter = enter_impl
        p.pointee.leave = leave_impl
        p.pointee.preedit_string = preeditString_impl
        p.pointee.commit_string = commitString_impl
        p.pointee.delete_surrounding_text = deleteSurroundingText_impl
        p.pointee.done = done_impl
        p.pointee.action = action_impl
        p.pointee.language = language_impl
        p.pointee.preedit_hint = preeditHint_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zwp_text_input_v3_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ZwpTextInputV3Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ZwpTextInputV3Events
    }

    private static let enter_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, surface in
        guard let data, let proxy, let h = handler(data) else { return }
        h.enter(proxy, surface: surface)
    }
    private static let leave_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, surface in
        guard let data, let proxy, let h = handler(data) else { return }
        h.leave(proxy, surface: surface)
    }
    private static let preeditString_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?, Int32, Int32) -> Void = { data, proxy, text, cursor_begin, cursor_end in
        guard let data, let proxy, let h = handler(data) else { return }
        h.preeditString(proxy, text: text, cursor_begin: cursor_begin, cursor_end: cursor_end)
    }
    private static let commitString_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, text in
        guard let data, let proxy, let h = handler(data) else { return }
        h.commitString(proxy, text: text)
    }
    private static let deleteSurroundingText_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32) -> Void = { data, proxy, before_length, after_length in
        guard let data, let proxy, let h = handler(data) else { return }
        h.deleteSurroundingText(proxy, before_length: before_length, after_length: after_length)
    }
    private static let done_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, serial in
        guard let data, let proxy, let h = handler(data) else { return }
        h.done(proxy, serial: serial)
    }
    private static let action_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32) -> Void = { data, proxy, action, serial in
        guard let data, let proxy, let h = handler(data) else { return }
        h.action(proxy, action: action, serial: serial)
    }
    private static let language_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, language in
        guard let data, let proxy, let h = handler(data) else { return }
        h.language(proxy, language: language)
    }
    private static let preeditHint_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32, UInt32) -> Void = { data, proxy, start, end, hint in
        guard let data, let proxy, let h = handler(data) else { return }
        h.preeditHint(proxy, start: start, end: end, hint: hint)
    }
}
