// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for wp_drm_lease_connector_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol WpDrmLeaseConnectorV1Events: AnyObject {
    func name(_ proxy: OpaquePointer, name: UnsafePointer<CChar>?)
    func description(_ proxy: OpaquePointer, description: UnsafePointer<CChar>?)
    func connectorId(_ proxy: OpaquePointer, connector_id: UInt32)
    func done(_ proxy: OpaquePointer)
    func withdrawn(_ proxy: OpaquePointer)
}

public enum WpDrmLeaseConnectorV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<wp_drm_lease_connector_v1_listener> = {
        let p = UnsafeMutablePointer<wp_drm_lease_connector_v1_listener>.allocate(capacity: 1)
        p.initialize(to: wp_drm_lease_connector_v1_listener())
        p.pointee.name = name_impl
        p.pointee.description = description_impl
        p.pointee.connector_id = connectorId_impl
        p.pointee.done = done_impl
        p.pointee.withdrawn = withdrawn_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        wp_drm_lease_connector_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any WpDrmLeaseConnectorV1Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any WpDrmLeaseConnectorV1Events
    }

    private static let name_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, name in
        guard let data, let proxy, let h = handler(data) else { return }
        h.name(proxy, name: name)
    }
    private static let description_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, description in
        guard let data, let proxy, let h = handler(data) else { return }
        h.description(proxy, description: description)
    }
    private static let connectorId_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, connector_id in
        guard let data, let proxy, let h = handler(data) else { return }
        h.connectorId(proxy, connector_id: connector_id)
    }
    private static let done_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.done(proxy)
    }
    private static let withdrawn_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.withdrawn(proxy)
    }
}
