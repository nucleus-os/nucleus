// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zwlr_foreign_toplevel_handle_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol ZwlrForeignToplevelHandleV1Events: AnyObject {
    func title(_ proxy: OpaquePointer, title: UnsafePointer<CChar>?)
    func appId(_ proxy: OpaquePointer, app_id: UnsafePointer<CChar>?)
    func outputEnter(_ proxy: OpaquePointer, output: OpaquePointer?)
    func outputLeave(_ proxy: OpaquePointer, output: OpaquePointer?)
    func state(_ proxy: OpaquePointer, state: UnsafeMutablePointer<wl_array>?)
    func done(_ proxy: OpaquePointer)
    func closed(_ proxy: OpaquePointer)
    func parent(_ proxy: OpaquePointer, parent: OpaquePointer?)
}

public enum ZwlrForeignToplevelHandleV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zwlr_foreign_toplevel_handle_v1_listener> = {
        let p = UnsafeMutablePointer<zwlr_foreign_toplevel_handle_v1_listener>.allocate(capacity: 1)
        p.initialize(to: zwlr_foreign_toplevel_handle_v1_listener())
        p.pointee.title = title_impl
        p.pointee.app_id = appId_impl
        p.pointee.output_enter = outputEnter_impl
        p.pointee.output_leave = outputLeave_impl
        p.pointee.state = state_impl
        p.pointee.done = done_impl
        p.pointee.closed = closed_impl
        p.pointee.parent = parent_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zwlr_foreign_toplevel_handle_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> ZwlrForeignToplevelHandleV1Events? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? ZwlrForeignToplevelHandleV1Events
    }

    private static let title_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, title in
        guard let data, let proxy, let h = handler(data) else { return }
        h.title(proxy, title: title)
    }
    private static let appId_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, app_id in
        guard let data, let proxy, let h = handler(data) else { return }
        h.appId(proxy, app_id: app_id)
    }
    private static let outputEnter_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, output in
        guard let data, let proxy, let h = handler(data) else { return }
        h.outputEnter(proxy, output: output)
    }
    private static let outputLeave_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, output in
        guard let data, let proxy, let h = handler(data) else { return }
        h.outputLeave(proxy, output: output)
    }
    private static let state_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafeMutablePointer<wl_array>?) -> Void = { data, proxy, state in
        guard let data, let proxy, let h = handler(data) else { return }
        h.state(proxy, state: state)
    }
    private static let done_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.done(proxy)
    }
    private static let closed_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.closed(proxy)
    }
    private static let parent_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, parent in
        guard let data, let proxy, let h = handler(data) else { return }
        h.parent(proxy, parent: parent)
    }
}
