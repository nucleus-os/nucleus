// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for wl_callback: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol WlCallbackEvents: AnyObject {
    func done(_ proxy: OpaquePointer, callback_data: UInt32)
}

public enum WlCallbackClient {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<wl_callback_listener> = {
        let p = UnsafeMutablePointer<wl_callback_listener>.allocate(capacity: 1)
        p.initialize(to: wl_callback_listener())
        p.pointee.done = done_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        wl_callback_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any WlCallbackEvents)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any WlCallbackEvents
    }

    private static let done_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, callback_data in
        guard let data, let proxy, let h = handler(data) else { return }
        h.done(proxy, callback_data: callback_data)
    }
}
