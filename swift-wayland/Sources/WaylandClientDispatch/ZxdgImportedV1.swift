// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zxdg_imported_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ZxdgImportedV1Events: AnyObject {
    func destroyed(_ proxy: OpaquePointer)
}

public enum ZxdgImportedV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zxdg_imported_v1_listener> = {
        let p = UnsafeMutablePointer<zxdg_imported_v1_listener>.allocate(capacity: 1)
        p.initialize(to: zxdg_imported_v1_listener())
        p.pointee.destroyed = destroyed_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zxdg_imported_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ZxdgImportedV1Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ZxdgImportedV1Events
    }

    private static let destroyed_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.destroyed(proxy)
    }
}
