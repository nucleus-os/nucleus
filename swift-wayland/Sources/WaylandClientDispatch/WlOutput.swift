// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for wl_output: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol WlOutputEvents: AnyObject {
    func geometry(_ proxy: OpaquePointer, x: Int32, y: Int32, physical_width: Int32, physical_height: Int32, subpixel: Int32, make: UnsafePointer<CChar>?, model: UnsafePointer<CChar>?, transform: Int32)
    func mode(_ proxy: OpaquePointer, flags: UInt32, width: Int32, height: Int32, refresh: Int32)
    func done(_ proxy: OpaquePointer)
    func scale(_ proxy: OpaquePointer, factor: Int32)
    func name(_ proxy: OpaquePointer, name: UnsafePointer<CChar>?)
    func description(_ proxy: OpaquePointer, description: UnsafePointer<CChar>?)
}

public enum WlOutputClient {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<wl_output_listener> = {
        let p = UnsafeMutablePointer<wl_output_listener>.allocate(capacity: 1)
        p.initialize(to: wl_output_listener())
        p.pointee.geometry = geometry_impl
        p.pointee.mode = mode_impl
        p.pointee.done = done_impl
        p.pointee.scale = scale_impl
        p.pointee.name = name_impl
        p.pointee.description = description_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        wl_output_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any WlOutputEvents)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any WlOutputEvents
    }

    private static let geometry_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32, Int32, Int32, Int32, Int32, UnsafePointer<CChar>?, UnsafePointer<CChar>?, Int32) -> Void = { data, proxy, x, y, physical_width, physical_height, subpixel, make, model, transform in
        guard let data, let proxy, let h = handler(data) else { return }
        h.geometry(proxy, x: x, y: y, physical_width: physical_width, physical_height: physical_height, subpixel: subpixel, make: make, model: model, transform: transform)
    }
    private static let mode_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, Int32, Int32, Int32) -> Void = { data, proxy, flags, width, height, refresh in
        guard let data, let proxy, let h = handler(data) else { return }
        h.mode(proxy, flags: flags, width: width, height: height, refresh: refresh)
    }
    private static let done_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.done(proxy)
    }
    private static let scale_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32) -> Void = { data, proxy, factor in
        guard let data, let proxy, let h = handler(data) else { return }
        h.scale(proxy, factor: factor)
    }
    private static let name_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, name in
        guard let data, let proxy, let h = handler(data) else { return }
        h.name(proxy, name: name)
    }
    private static let description_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, description in
        guard let data, let proxy, let h = handler(data) else { return }
        h.description(proxy, description: description)
    }
}
