// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for ext_session_lock_surface_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ExtSessionLockSurfaceV1Events: AnyObject {
    func configure(_ proxy: OpaquePointer, serial: UInt32, width: UInt32, height: UInt32)
}

public enum ExtSessionLockSurfaceV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<ext_session_lock_surface_v1_listener> = {
        let p = UnsafeMutablePointer<ext_session_lock_surface_v1_listener>.allocate(capacity: 1)
        p.initialize(to: ext_session_lock_surface_v1_listener())
        p.pointee.configure = configure_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        ext_session_lock_surface_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ExtSessionLockSurfaceV1Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ExtSessionLockSurfaceV1Events
    }

    private static let configure_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32, UInt32) -> Void = { data, proxy, serial, width, height in
        guard let data, let proxy, let h = handler(data) else { return }
        h.configure(proxy, serial: serial, width: width, height: height)
    }
}
