// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for wp_image_description_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol WpImageDescriptionV1Events: AnyObject {
    func failed(_ proxy: OpaquePointer, cause: UInt32, msg: UnsafePointer<CChar>?)
    func ready(_ proxy: OpaquePointer, identity: UInt32)
    func ready2(_ proxy: OpaquePointer, identity_hi: UInt32, identity_lo: UInt32)
}

public enum WpImageDescriptionV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<wp_image_description_v1_listener> = {
        let p = UnsafeMutablePointer<wp_image_description_v1_listener>.allocate(capacity: 1)
        p.initialize(to: wp_image_description_v1_listener())
        p.pointee.failed = failed_impl
        p.pointee.ready = ready_impl
        p.pointee.ready2 = ready2_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        wp_image_description_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> WpImageDescriptionV1Events? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? WpImageDescriptionV1Events
    }

    private static let failed_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UnsafePointer<CChar>?) -> Void = { data, proxy, cause, msg in
        guard let data, let proxy, let h = handler(data) else { return }
        h.failed(proxy, cause: cause, msg: msg)
    }
    private static let ready_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, identity in
        guard let data, let proxy, let h = handler(data) else { return }
        h.ready(proxy, identity: identity)
    }
    private static let ready2_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32) -> Void = { data, proxy, identity_hi, identity_lo in
        guard let data, let proxy, let h = handler(data) else { return }
        h.ready2(proxy, identity_hi: identity_hi, identity_lo: identity_lo)
    }
}
