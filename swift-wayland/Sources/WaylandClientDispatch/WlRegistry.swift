// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for wl_registry: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol WlRegistryEvents: AnyObject {
    func global(_ proxy: OpaquePointer, name: UInt32, interface: UnsafePointer<CChar>?, version: UInt32)
    func globalRemove(_ proxy: OpaquePointer, name: UInt32)
}

public enum WlRegistryClient {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<wl_registry_listener> = {
        let p = UnsafeMutablePointer<wl_registry_listener>.allocate(capacity: 1)
        p.initialize(to: wl_registry_listener())
        p.pointee.global = global_impl
        p.pointee.global_remove = globalRemove_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        wl_registry_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> WlRegistryEvents? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? WlRegistryEvents
    }

    private static let global_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UnsafePointer<CChar>?, UInt32) -> Void = { data, proxy, name, interface, version in
        guard let data, let proxy, let h = handler(data) else { return }
        h.global(proxy, name: name, interface: interface, version: version)
    }
    private static let globalRemove_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, name in
        guard let data, let proxy, let h = handler(data) else { return }
        h.globalRemove(proxy, name: name)
    }
}
