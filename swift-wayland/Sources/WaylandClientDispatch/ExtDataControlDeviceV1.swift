// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for ext_data_control_device_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ExtDataControlDeviceV1Events: AnyObject {
    func dataOffer(_ proxy: OpaquePointer, id: OpaquePointer?)
    func selection(_ proxy: OpaquePointer, id: OpaquePointer?)
    func finished(_ proxy: OpaquePointer)
    func primarySelection(_ proxy: OpaquePointer, id: OpaquePointer?)
}

public enum ExtDataControlDeviceV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<ext_data_control_device_v1_listener> = {
        let p = UnsafeMutablePointer<ext_data_control_device_v1_listener>.allocate(capacity: 1)
        p.initialize(to: ext_data_control_device_v1_listener())
        p.pointee.data_offer = dataOffer_impl
        p.pointee.selection = selection_impl
        p.pointee.finished = finished_impl
        p.pointee.primary_selection = primarySelection_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        ext_data_control_device_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ExtDataControlDeviceV1Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ExtDataControlDeviceV1Events
    }

    private static let dataOffer_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, id in
        guard let data, let proxy, let h = handler(data) else { return }
        h.dataOffer(proxy, id: id)
    }
    private static let selection_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, id in
        guard let data, let proxy, let h = handler(data) else { return }
        h.selection(proxy, id: id)
    }
    private static let finished_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.finished(proxy)
    }
    private static let primarySelection_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, id in
        guard let data, let proxy, let h = handler(data) else { return }
        h.primarySelection(proxy, id: id)
    }
}
