// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zxdg_popup_v6: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ZxdgPopupV6Events: AnyObject {
    func configure(_ proxy: OpaquePointer, x: Int32, y: Int32, width: Int32, height: Int32)
    func popupDone(_ proxy: OpaquePointer)
}

public enum ZxdgPopupV6Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zxdg_popup_v6_listener> = {
        let p = UnsafeMutablePointer<zxdg_popup_v6_listener>.allocate(capacity: 1)
        p.initialize(to: zxdg_popup_v6_listener())
        p.pointee.configure = configure_impl
        p.pointee.popup_done = popupDone_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zxdg_popup_v6_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ZxdgPopupV6Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ZxdgPopupV6Events
    }

    private static let configure_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32, Int32, Int32, Int32) -> Void = { data, proxy, x, y, width, height in
        guard let data, let proxy, let h = handler(data) else { return }
        h.configure(proxy, x: x, y: y, width: width, height: height)
    }
    private static let popupDone_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.popupDone(proxy)
    }
}
