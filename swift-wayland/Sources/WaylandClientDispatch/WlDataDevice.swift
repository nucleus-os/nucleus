// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for wl_data_device: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol WlDataDeviceEvents: AnyObject {
    func dataOffer(_ proxy: OpaquePointer, id: OpaquePointer?)
    func enter(_ proxy: OpaquePointer, serial: UInt32, surface: OpaquePointer?, x: Double, y: Double, id: OpaquePointer?)
    func leave(_ proxy: OpaquePointer)
    func motion(_ proxy: OpaquePointer, time: UInt32, x: Double, y: Double)
    func drop(_ proxy: OpaquePointer)
    func selection(_ proxy: OpaquePointer, id: OpaquePointer?)
}

public enum WlDataDeviceClient {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<wl_data_device_listener> = {
        let p = UnsafeMutablePointer<wl_data_device_listener>.allocate(capacity: 1)
        p.initialize(to: wl_data_device_listener())
        p.pointee.data_offer = dataOffer_impl
        p.pointee.enter = enter_impl
        p.pointee.leave = leave_impl
        p.pointee.motion = motion_impl
        p.pointee.drop = drop_impl
        p.pointee.selection = selection_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        wl_data_device_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> WlDataDeviceEvents? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? WlDataDeviceEvents
    }

    private static let dataOffer_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, id in
        guard let data, let proxy, let h = handler(data) else { return }
        h.dataOffer(proxy, id: id)
    }
    private static let enter_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, OpaquePointer?, wl_fixed_t, wl_fixed_t, OpaquePointer?) -> Void = { data, proxy, serial, surface, x, y, id in
        guard let data, let proxy, let h = handler(data) else { return }
        h.enter(proxy, serial: serial, surface: surface, x: swift_wayland_fixed_to_double(x), y: swift_wayland_fixed_to_double(y), id: id)
    }
    private static let leave_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.leave(proxy)
    }
    private static let motion_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, wl_fixed_t, wl_fixed_t) -> Void = { data, proxy, time, x, y in
        guard let data, let proxy, let h = handler(data) else { return }
        h.motion(proxy, time: time, x: swift_wayland_fixed_to_double(x), y: swift_wayland_fixed_to_double(y))
    }
    private static let drop_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.drop(proxy)
    }
    private static let selection_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, id in
        guard let data, let proxy, let h = handler(data) else { return }
        h.selection(proxy, id: id)
    }
}
