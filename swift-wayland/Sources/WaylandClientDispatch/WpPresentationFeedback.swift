// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for wp_presentation_feedback: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol WpPresentationFeedbackEvents: AnyObject {
    func syncOutput(_ proxy: OpaquePointer, output: OpaquePointer?)
    func presented(_ proxy: OpaquePointer, tv_sec_hi: UInt32, tv_sec_lo: UInt32, tv_nsec: UInt32, refresh: UInt32, seq_hi: UInt32, seq_lo: UInt32, flags: UInt32)
    func discarded(_ proxy: OpaquePointer)
}

public enum WpPresentationFeedbackClient {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<wp_presentation_feedback_listener> = {
        let p = UnsafeMutablePointer<wp_presentation_feedback_listener>.allocate(capacity: 1)
        p.initialize(to: wp_presentation_feedback_listener())
        p.pointee.sync_output = syncOutput_impl
        p.pointee.presented = presented_impl
        p.pointee.discarded = discarded_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        wp_presentation_feedback_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any WpPresentationFeedbackEvents)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any WpPresentationFeedbackEvents
    }

    private static let syncOutput_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, output in
        guard let data, let proxy, let h = handler(data) else { return }
        h.syncOutput(proxy, output: output)
    }
    private static let presented_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32, UInt32, UInt32, UInt32, UInt32, UInt32) -> Void = { data, proxy, tv_sec_hi, tv_sec_lo, tv_nsec, refresh, seq_hi, seq_lo, flags in
        guard let data, let proxy, let h = handler(data) else { return }
        h.presented(proxy, tv_sec_hi: tv_sec_hi, tv_sec_lo: tv_sec_lo, tv_nsec: tv_nsec, refresh: refresh, seq_hi: seq_hi, seq_lo: seq_lo, flags: flags)
    }
    private static let discarded_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.discarded(proxy)
    }
}
