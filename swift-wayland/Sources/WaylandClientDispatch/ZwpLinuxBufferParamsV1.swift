// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zwp_linux_buffer_params_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol ZwpLinuxBufferParamsV1Events: AnyObject {
    func created(_ proxy: OpaquePointer, buffer: OpaquePointer?)
    func failed(_ proxy: OpaquePointer)
}

public enum ZwpLinuxBufferParamsV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zwp_linux_buffer_params_v1_listener> = {
        let p = UnsafeMutablePointer<zwp_linux_buffer_params_v1_listener>.allocate(capacity: 1)
        p.initialize(to: zwp_linux_buffer_params_v1_listener())
        p.pointee.created = created_impl
        p.pointee.failed = failed_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zwp_linux_buffer_params_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> ZwpLinuxBufferParamsV1Events? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? ZwpLinuxBufferParamsV1Events
    }

    private static let created_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, buffer in
        guard let data, let proxy, let h = handler(data) else { return }
        h.created(proxy, buffer: buffer)
    }
    private static let failed_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.failed(proxy)
    }
}
