// Generated by SwiftWaylandGen. Do not edit.
// Typed client descriptor and event dispatch for zwp_input_method_keyboard_grab_v2.

package import WaylandClientC
package import WaylandProtocolTypes
package enum ZwpInputMethodKeyboardGrabV2Client: WaylandClientInterface {
    package nonisolated static let descriptor = unsafe WaylandClientInterfaceDescriptor(
        nativeInterface: swift_wayland_iface_zwp_input_method_keyboard_grab_v2())
    package nonisolated static let maximumVersion: UInt32 = 1
}
package extension WaylandProxy where Interface == ZwpInputMethodKeyboardGrabV2Client {
    func release() throws(WaylandProxyError) {
        let _proxy = try unsafe requireNativeProxy()
        let _send = { () throws(WaylandProxyError) -> Void in
            unsafe swift_wayland_client_request_zwp_input_method_keyboard_grab_v2_release(_proxy)
            return
        }
        try _send()
        try unsafe invalidateAfterProtocolDestructor()
    }
}
@MainActor
package protocol ZwpInputMethodKeyboardGrabV2Events: AnyObject {
    func keymap(_ proxy: WaylandBorrowedProxy<ZwpInputMethodKeyboardGrabV2Client>, format: WlKeyboardKeymapFormat, fd: consuming WaylandClientOwnedFileDescriptor, size: UInt32)
    func key(_ proxy: WaylandBorrowedProxy<ZwpInputMethodKeyboardGrabV2Client>, serial: UInt32, time: UInt32, key: UInt32, state: WlKeyboardKeyState)
    func modifiers(_ proxy: WaylandBorrowedProxy<ZwpInputMethodKeyboardGrabV2Client>, serial: UInt32, mods_depressed: UInt32, mods_latched: UInt32, mods_locked: UInt32, group: UInt32)
    func repeatInfo(_ proxy: WaylandBorrowedProxy<ZwpInputMethodKeyboardGrabV2Client>, rate: Int32, delay: Int32)
}
package extension ZwpInputMethodKeyboardGrabV2Client {
    @MainActor static let listener: UnsafeMutablePointer<swift_wayland_zwp_input_method_keyboard_grab_v2_events> = {
        let p = UnsafeMutablePointer<swift_wayland_zwp_input_method_keyboard_grab_v2_events>.allocate(capacity: 1)
        unsafe p.initialize(to: swift_wayland_zwp_input_method_keyboard_grab_v2_events())
        unsafe p.pointee.keymap = keymap_impl
        unsafe p.pointee.key = key_impl
        unsafe p.pointee.modifiers = modifiers_impl
        unsafe p.pointee.repeat_info = repeatInfo_impl
        return unsafe p
    }()
    private static func handler(_ context: WaylandClientListenerContext) -> any ZwpInputMethodKeyboardGrabV2Events? {
        context.owner as? any ZwpInputMethodKeyboardGrabV2Events
    }
    private static let keymap_impl: @MainActor @Sendable @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, Int32, UInt32) -> Void = { data, proxy, format, fd, size in
        guard let data = unsafe data, let proxy = unsafe proxy else {
            return
        }
        let listenerContext = unsafe WaylandClientListenerContext.recover(data)
        guard let h = handler(listenerContext) else {
            return
        }
        unsafe h.keymap(WaylandBorrowedProxy<ZwpInputMethodKeyboardGrabV2Client>(proxy), format: WlKeyboardKeymapFormat(rawValue: format), fd: WaylandClientOwnedFileDescriptor(fd), size: size)
    }
    private static let key_impl: @MainActor @Sendable @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32, UInt32, UInt32) -> Void = { data, proxy, serial, time, key, state in
        guard let data = unsafe data, let proxy = unsafe proxy else {
            return
        }
        let listenerContext = unsafe WaylandClientListenerContext.recover(data)
        guard let h = handler(listenerContext) else {
            return
        }
        unsafe h.key(WaylandBorrowedProxy<ZwpInputMethodKeyboardGrabV2Client>(proxy), serial: serial, time: time, key: key, state: WlKeyboardKeyState(rawValue: state))
    }
    private static let modifiers_impl: @MainActor @Sendable @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32, UInt32, UInt32, UInt32) -> Void = { data, proxy, serial, mods_depressed, mods_latched, mods_locked, group in
        guard let data = unsafe data, let proxy = unsafe proxy else {
            return
        }
        let listenerContext = unsafe WaylandClientListenerContext.recover(data)
        guard let h = handler(listenerContext) else {
            return
        }
        unsafe h.modifiers(WaylandBorrowedProxy<ZwpInputMethodKeyboardGrabV2Client>(proxy), serial: serial, mods_depressed: mods_depressed, mods_latched: mods_latched, mods_locked: mods_locked, group: group)
    }
    private static let repeatInfo_impl: @MainActor @Sendable @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32, Int32) -> Void = { data, proxy, rate, delay in
        guard let data = unsafe data, let proxy = unsafe proxy else {
            return
        }
        let listenerContext = unsafe WaylandClientListenerContext.recover(data)
        guard let h = handler(listenerContext) else {
            return
        }
        unsafe h.repeatInfo(WaylandBorrowedProxy<ZwpInputMethodKeyboardGrabV2Client>(proxy), rate: rate, delay: delay)
    }
}
package extension WaylandProxy where Interface == ZwpInputMethodKeyboardGrabV2Client {
    func installListener(_ owner: any ZwpInputMethodKeyboardGrabV2Events) throws(WaylandProxyError) {
        try unsafe installListener(owner: owner) { proxy, data in
            unsafe swift_wayland_client_install_listener(proxy, ZwpInputMethodKeyboardGrabV2Client.listener, data)
        }
    }
}