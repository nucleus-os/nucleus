// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for ext_session_lock_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol ExtSessionLockV1Events: AnyObject {
    func locked(_ proxy: OpaquePointer)
    func finished(_ proxy: OpaquePointer)
}

public enum ExtSessionLockV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<ext_session_lock_v1_listener> = {
        let p = UnsafeMutablePointer<ext_session_lock_v1_listener>.allocate(capacity: 1)
        p.initialize(to: ext_session_lock_v1_listener())
        p.pointee.locked = locked_impl
        p.pointee.finished = finished_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        ext_session_lock_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> ExtSessionLockV1Events? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? ExtSessionLockV1Events
    }

    private static let locked_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.locked(proxy)
    }
    private static let finished_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.finished(proxy)
    }
}
