// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zwp_pointer_gesture_hold_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol ZwpPointerGestureHoldV1Events: AnyObject {
    func begin(_ proxy: OpaquePointer, serial: UInt32, time: UInt32, surface: OpaquePointer?, fingers: UInt32)
    func end(_ proxy: OpaquePointer, serial: UInt32, time: UInt32, cancelled: Int32)
}

public enum ZwpPointerGestureHoldV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zwp_pointer_gesture_hold_v1_listener> = {
        let p = UnsafeMutablePointer<zwp_pointer_gesture_hold_v1_listener>.allocate(capacity: 1)
        p.initialize(to: zwp_pointer_gesture_hold_v1_listener())
        p.pointee.begin = begin_impl
        p.pointee.end = end_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zwp_pointer_gesture_hold_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> ZwpPointerGestureHoldV1Events? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? ZwpPointerGestureHoldV1Events
    }

    private static let begin_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32, OpaquePointer?, UInt32) -> Void = { data, proxy, serial, time, surface, fingers in
        guard let data, let proxy, let h = handler(data) else { return }
        h.begin(proxy, serial: serial, time: time, surface: surface, fingers: fingers)
    }
    private static let end_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32, Int32) -> Void = { data, proxy, serial, time, cancelled in
        guard let data, let proxy, let h = handler(data) else { return }
        h.end(proxy, serial: serial, time: time, cancelled: cancelled)
    }
}
