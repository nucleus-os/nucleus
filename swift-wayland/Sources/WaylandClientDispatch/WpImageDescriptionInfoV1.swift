// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for wp_image_description_info_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol WpImageDescriptionInfoV1Events: AnyObject {
    func done(_ proxy: OpaquePointer)
    func iccFile(_ proxy: OpaquePointer, icc: Int32, icc_size: UInt32)
    func primaries(_ proxy: OpaquePointer, r_x: Int32, r_y: Int32, g_x: Int32, g_y: Int32, b_x: Int32, b_y: Int32, w_x: Int32, w_y: Int32)
    func primariesNamed(_ proxy: OpaquePointer, primaries: UInt32)
    func tfPower(_ proxy: OpaquePointer, eexp: UInt32)
    func tfNamed(_ proxy: OpaquePointer, tf: UInt32)
    func luminances(_ proxy: OpaquePointer, min_lum: UInt32, max_lum: UInt32, reference_lum: UInt32)
    func targetPrimaries(_ proxy: OpaquePointer, r_x: Int32, r_y: Int32, g_x: Int32, g_y: Int32, b_x: Int32, b_y: Int32, w_x: Int32, w_y: Int32)
    func targetLuminance(_ proxy: OpaquePointer, min_lum: UInt32, max_lum: UInt32)
    func targetMaxCll(_ proxy: OpaquePointer, max_cll: UInt32)
    func targetMaxFall(_ proxy: OpaquePointer, max_fall: UInt32)
}

public enum WpImageDescriptionInfoV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<wp_image_description_info_v1_listener> = {
        let p = UnsafeMutablePointer<wp_image_description_info_v1_listener>.allocate(capacity: 1)
        p.initialize(to: wp_image_description_info_v1_listener())
        p.pointee.done = done_impl
        p.pointee.icc_file = iccFile_impl
        p.pointee.primaries = primaries_impl
        p.pointee.primaries_named = primariesNamed_impl
        p.pointee.tf_power = tfPower_impl
        p.pointee.tf_named = tfNamed_impl
        p.pointee.luminances = luminances_impl
        p.pointee.target_primaries = targetPrimaries_impl
        p.pointee.target_luminance = targetLuminance_impl
        p.pointee.target_max_cll = targetMaxCll_impl
        p.pointee.target_max_fall = targetMaxFall_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        wp_image_description_info_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> WpImageDescriptionInfoV1Events? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? WpImageDescriptionInfoV1Events
    }

    private static let done_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.done(proxy)
    }
    private static let iccFile_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32, UInt32) -> Void = { data, proxy, icc, icc_size in
        guard let data, let proxy, let h = handler(data) else { return }
        h.iccFile(proxy, icc: icc, icc_size: icc_size)
    }
    private static let primaries_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32, Int32, Int32, Int32, Int32, Int32, Int32, Int32) -> Void = { data, proxy, r_x, r_y, g_x, g_y, b_x, b_y, w_x, w_y in
        guard let data, let proxy, let h = handler(data) else { return }
        h.primaries(proxy, r_x: r_x, r_y: r_y, g_x: g_x, g_y: g_y, b_x: b_x, b_y: b_y, w_x: w_x, w_y: w_y)
    }
    private static let primariesNamed_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, primaries in
        guard let data, let proxy, let h = handler(data) else { return }
        h.primariesNamed(proxy, primaries: primaries)
    }
    private static let tfPower_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, eexp in
        guard let data, let proxy, let h = handler(data) else { return }
        h.tfPower(proxy, eexp: eexp)
    }
    private static let tfNamed_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, tf in
        guard let data, let proxy, let h = handler(data) else { return }
        h.tfNamed(proxy, tf: tf)
    }
    private static let luminances_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32, UInt32) -> Void = { data, proxy, min_lum, max_lum, reference_lum in
        guard let data, let proxy, let h = handler(data) else { return }
        h.luminances(proxy, min_lum: min_lum, max_lum: max_lum, reference_lum: reference_lum)
    }
    private static let targetPrimaries_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32, Int32, Int32, Int32, Int32, Int32, Int32, Int32) -> Void = { data, proxy, r_x, r_y, g_x, g_y, b_x, b_y, w_x, w_y in
        guard let data, let proxy, let h = handler(data) else { return }
        h.targetPrimaries(proxy, r_x: r_x, r_y: r_y, g_x: g_x, g_y: g_y, b_x: b_x, b_y: b_y, w_x: w_x, w_y: w_y)
    }
    private static let targetLuminance_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32) -> Void = { data, proxy, min_lum, max_lum in
        guard let data, let proxy, let h = handler(data) else { return }
        h.targetLuminance(proxy, min_lum: min_lum, max_lum: max_lum)
    }
    private static let targetMaxCll_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, max_cll in
        guard let data, let proxy, let h = handler(data) else { return }
        h.targetMaxCll(proxy, max_cll: max_cll)
    }
    private static let targetMaxFall_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, max_fall in
        guard let data, let proxy, let h = handler(data) else { return }
        h.targetMaxFall(proxy, max_fall: max_fall)
    }
}
