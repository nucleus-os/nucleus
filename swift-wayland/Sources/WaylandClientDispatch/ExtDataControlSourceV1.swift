// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for ext_data_control_source_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol ExtDataControlSourceV1Events: AnyObject {
    func send(_ proxy: OpaquePointer, mime_type: UnsafePointer<CChar>?, fd: Int32)
    func cancelled(_ proxy: OpaquePointer)
}

public enum ExtDataControlSourceV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<ext_data_control_source_v1_listener> = {
        let p = UnsafeMutablePointer<ext_data_control_source_v1_listener>.allocate(capacity: 1)
        p.initialize(to: ext_data_control_source_v1_listener())
        p.pointee.send = send_impl
        p.pointee.cancelled = cancelled_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        ext_data_control_source_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> ExtDataControlSourceV1Events? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? ExtDataControlSourceV1Events
    }

    private static let send_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?, Int32) -> Void = { data, proxy, mime_type, fd in
        guard let data, let proxy, let h = handler(data) else { return }
        h.send(proxy, mime_type: mime_type, fd: fd)
    }
    private static let cancelled_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.cancelled(proxy)
    }
}
