// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zwp_tablet_tool_v2: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ZwpTabletToolV2Events: AnyObject {
    func type(_ proxy: OpaquePointer, tool_type: UInt32)
    func hardwareSerial(_ proxy: OpaquePointer, hardware_serial_hi: UInt32, hardware_serial_lo: UInt32)
    func hardwareIdWacom(_ proxy: OpaquePointer, hardware_id_hi: UInt32, hardware_id_lo: UInt32)
    func capability(_ proxy: OpaquePointer, capability: UInt32)
    func done(_ proxy: OpaquePointer)
    func removed(_ proxy: OpaquePointer)
    func proximityIn(_ proxy: OpaquePointer, serial: UInt32, tablet: OpaquePointer?, surface: OpaquePointer?)
    func proximityOut(_ proxy: OpaquePointer)
    func down(_ proxy: OpaquePointer, serial: UInt32)
    func up(_ proxy: OpaquePointer)
    func motion(_ proxy: OpaquePointer, x: Double, y: Double)
    func pressure(_ proxy: OpaquePointer, pressure: UInt32)
    func distance(_ proxy: OpaquePointer, distance: UInt32)
    func tilt(_ proxy: OpaquePointer, tilt_x: Double, tilt_y: Double)
    func rotation(_ proxy: OpaquePointer, degrees: Double)
    func slider(_ proxy: OpaquePointer, position: Int32)
    func wheel(_ proxy: OpaquePointer, degrees: Double, clicks: Int32)
    func button(_ proxy: OpaquePointer, serial: UInt32, button: UInt32, state: UInt32)
    func frame(_ proxy: OpaquePointer, time: UInt32)
}

public enum ZwpTabletToolV2Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zwp_tablet_tool_v2_listener> = {
        let p = UnsafeMutablePointer<zwp_tablet_tool_v2_listener>.allocate(capacity: 1)
        p.initialize(to: zwp_tablet_tool_v2_listener())
        p.pointee.type = type_impl
        p.pointee.hardware_serial = hardwareSerial_impl
        p.pointee.hardware_id_wacom = hardwareIdWacom_impl
        p.pointee.capability = capability_impl
        p.pointee.done = done_impl
        p.pointee.removed = removed_impl
        p.pointee.proximity_in = proximityIn_impl
        p.pointee.proximity_out = proximityOut_impl
        p.pointee.down = down_impl
        p.pointee.up = up_impl
        p.pointee.motion = motion_impl
        p.pointee.pressure = pressure_impl
        p.pointee.distance = distance_impl
        p.pointee.tilt = tilt_impl
        p.pointee.rotation = rotation_impl
        p.pointee.slider = slider_impl
        p.pointee.wheel = wheel_impl
        p.pointee.button = button_impl
        p.pointee.frame = frame_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zwp_tablet_tool_v2_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ZwpTabletToolV2Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ZwpTabletToolV2Events
    }

    private static let type_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, tool_type in
        guard let data, let proxy, let h = handler(data) else { return }
        h.type(proxy, tool_type: tool_type)
    }
    private static let hardwareSerial_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32) -> Void = { data, proxy, hardware_serial_hi, hardware_serial_lo in
        guard let data, let proxy, let h = handler(data) else { return }
        h.hardwareSerial(proxy, hardware_serial_hi: hardware_serial_hi, hardware_serial_lo: hardware_serial_lo)
    }
    private static let hardwareIdWacom_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32) -> Void = { data, proxy, hardware_id_hi, hardware_id_lo in
        guard let data, let proxy, let h = handler(data) else { return }
        h.hardwareIdWacom(proxy, hardware_id_hi: hardware_id_hi, hardware_id_lo: hardware_id_lo)
    }
    private static let capability_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, capability in
        guard let data, let proxy, let h = handler(data) else { return }
        h.capability(proxy, capability: capability)
    }
    private static let done_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.done(proxy)
    }
    private static let removed_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.removed(proxy)
    }
    private static let proximityIn_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, serial, tablet, surface in
        guard let data, let proxy, let h = handler(data) else { return }
        h.proximityIn(proxy, serial: serial, tablet: tablet, surface: surface)
    }
    private static let proximityOut_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.proximityOut(proxy)
    }
    private static let down_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, serial in
        guard let data, let proxy, let h = handler(data) else { return }
        h.down(proxy, serial: serial)
    }
    private static let up_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.up(proxy)
    }
    private static let motion_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, wl_fixed_t, wl_fixed_t) -> Void = { data, proxy, x, y in
        guard let data, let proxy, let h = handler(data) else { return }
        h.motion(proxy, x: swift_wayland_fixed_to_double(x), y: swift_wayland_fixed_to_double(y))
    }
    private static let pressure_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, pressure in
        guard let data, let proxy, let h = handler(data) else { return }
        h.pressure(proxy, pressure: pressure)
    }
    private static let distance_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, distance in
        guard let data, let proxy, let h = handler(data) else { return }
        h.distance(proxy, distance: distance)
    }
    private static let tilt_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, wl_fixed_t, wl_fixed_t) -> Void = { data, proxy, tilt_x, tilt_y in
        guard let data, let proxy, let h = handler(data) else { return }
        h.tilt(proxy, tilt_x: swift_wayland_fixed_to_double(tilt_x), tilt_y: swift_wayland_fixed_to_double(tilt_y))
    }
    private static let rotation_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, wl_fixed_t) -> Void = { data, proxy, degrees in
        guard let data, let proxy, let h = handler(data) else { return }
        h.rotation(proxy, degrees: swift_wayland_fixed_to_double(degrees))
    }
    private static let slider_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32) -> Void = { data, proxy, position in
        guard let data, let proxy, let h = handler(data) else { return }
        h.slider(proxy, position: position)
    }
    private static let wheel_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, wl_fixed_t, Int32) -> Void = { data, proxy, degrees, clicks in
        guard let data, let proxy, let h = handler(data) else { return }
        h.wheel(proxy, degrees: swift_wayland_fixed_to_double(degrees), clicks: clicks)
    }
    private static let button_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32, UInt32) -> Void = { data, proxy, serial, button, state in
        guard let data, let proxy, let h = handler(data) else { return }
        h.button(proxy, serial: serial, button: button, state: state)
    }
    private static let frame_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, time in
        guard let data, let proxy, let h = handler(data) else { return }
        h.frame(proxy, time: time)
    }
}
