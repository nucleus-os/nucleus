// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zwp_tablet_pad_group_v2: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ZwpTabletPadGroupV2Events: AnyObject {
    func buttons(_ proxy: OpaquePointer, buttons: UnsafeMutablePointer<wl_array>?)
    func ring(_ proxy: OpaquePointer, ring: OpaquePointer?)
    func strip(_ proxy: OpaquePointer, strip: OpaquePointer?)
    func modes(_ proxy: OpaquePointer, modes: UInt32)
    func done(_ proxy: OpaquePointer)
    func modeSwitch(_ proxy: OpaquePointer, time: UInt32, serial: UInt32, mode: UInt32)
    func dial(_ proxy: OpaquePointer, dial: OpaquePointer?)
}

public enum ZwpTabletPadGroupV2Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zwp_tablet_pad_group_v2_listener> = {
        let p = UnsafeMutablePointer<zwp_tablet_pad_group_v2_listener>.allocate(capacity: 1)
        p.initialize(to: zwp_tablet_pad_group_v2_listener())
        p.pointee.buttons = buttons_impl
        p.pointee.ring = ring_impl
        p.pointee.strip = strip_impl
        p.pointee.modes = modes_impl
        p.pointee.done = done_impl
        p.pointee.mode_switch = modeSwitch_impl
        p.pointee.dial = dial_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zwp_tablet_pad_group_v2_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ZwpTabletPadGroupV2Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ZwpTabletPadGroupV2Events
    }

    private static let buttons_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafeMutablePointer<wl_array>?) -> Void = { data, proxy, buttons in
        guard let data, let proxy, let h = handler(data) else { return }
        h.buttons(proxy, buttons: buttons)
    }
    private static let ring_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, ring in
        guard let data, let proxy, let h = handler(data) else { return }
        h.ring(proxy, ring: ring)
    }
    private static let strip_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, strip in
        guard let data, let proxy, let h = handler(data) else { return }
        h.strip(proxy, strip: strip)
    }
    private static let modes_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, modes in
        guard let data, let proxy, let h = handler(data) else { return }
        h.modes(proxy, modes: modes)
    }
    private static let done_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.done(proxy)
    }
    private static let modeSwitch_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32, UInt32) -> Void = { data, proxy, time, serial, mode in
        guard let data, let proxy, let h = handler(data) else { return }
        h.modeSwitch(proxy, time: time, serial: serial, mode: mode)
    }
    private static let dial_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, dial in
        guard let data, let proxy, let h = handler(data) else { return }
        h.dial(proxy, dial: dial)
    }
}
