// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for wl_data_source: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol WlDataSourceEvents: AnyObject {
    func target(_ proxy: OpaquePointer, mime_type: UnsafePointer<CChar>?)
    func send(_ proxy: OpaquePointer, mime_type: UnsafePointer<CChar>?, fd: Int32)
    func cancelled(_ proxy: OpaquePointer)
    func dndDropPerformed(_ proxy: OpaquePointer)
    func dndFinished(_ proxy: OpaquePointer)
    func action(_ proxy: OpaquePointer, dnd_action: UInt32)
}

public enum WlDataSourceClient {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<wl_data_source_listener> = {
        let p = UnsafeMutablePointer<wl_data_source_listener>.allocate(capacity: 1)
        p.initialize(to: wl_data_source_listener())
        p.pointee.target = target_impl
        p.pointee.send = send_impl
        p.pointee.cancelled = cancelled_impl
        p.pointee.dnd_drop_performed = dndDropPerformed_impl
        p.pointee.dnd_finished = dndFinished_impl
        p.pointee.action = action_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        wl_data_source_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any WlDataSourceEvents)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any WlDataSourceEvents
    }

    private static let target_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, mime_type in
        guard let data, let proxy, let h = handler(data) else { return }
        h.target(proxy, mime_type: mime_type)
    }
    private static let send_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?, Int32) -> Void = { data, proxy, mime_type, fd in
        guard let data, let proxy, let h = handler(data) else { return }
        h.send(proxy, mime_type: mime_type, fd: fd)
    }
    private static let cancelled_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.cancelled(proxy)
    }
    private static let dndDropPerformed_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.dndDropPerformed(proxy)
    }
    private static let dndFinished_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.dndFinished(proxy)
    }
    private static let action_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, dnd_action in
        guard let data, let proxy, let h = handler(data) else { return }
        h.action(proxy, dnd_action: dnd_action)
    }
}
