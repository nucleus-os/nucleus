// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zwp_tablet_pad_strip_v2: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol ZwpTabletPadStripV2Events: AnyObject {
    func source(_ proxy: OpaquePointer, source: UInt32)
    func position(_ proxy: OpaquePointer, position: UInt32)
    func stop(_ proxy: OpaquePointer)
    func frame(_ proxy: OpaquePointer, time: UInt32)
}

public enum ZwpTabletPadStripV2Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zwp_tablet_pad_strip_v2_listener> = {
        let p = UnsafeMutablePointer<zwp_tablet_pad_strip_v2_listener>.allocate(capacity: 1)
        p.initialize(to: zwp_tablet_pad_strip_v2_listener())
        p.pointee.source = source_impl
        p.pointee.position = position_impl
        p.pointee.stop = stop_impl
        p.pointee.frame = frame_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zwp_tablet_pad_strip_v2_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> ZwpTabletPadStripV2Events? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? ZwpTabletPadStripV2Events
    }

    private static let source_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, source in
        guard let data, let proxy, let h = handler(data) else { return }
        h.source(proxy, source: source)
    }
    private static let position_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, position in
        guard let data, let proxy, let h = handler(data) else { return }
        h.position(proxy, position: position)
    }
    private static let stop_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.stop(proxy)
    }
    private static let frame_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, time in
        guard let data, let proxy, let h = handler(data) else { return }
        h.frame(proxy, time: time)
    }
}
