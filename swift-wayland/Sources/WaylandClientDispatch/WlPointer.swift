// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for wl_pointer: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol WlPointerEvents: AnyObject {
    func enter(_ proxy: OpaquePointer, serial: UInt32, surface: OpaquePointer?, surface_x: Double, surface_y: Double)
    func leave(_ proxy: OpaquePointer, serial: UInt32, surface: OpaquePointer?)
    func motion(_ proxy: OpaquePointer, time: UInt32, surface_x: Double, surface_y: Double)
    func button(_ proxy: OpaquePointer, serial: UInt32, time: UInt32, button: UInt32, state: UInt32)
    func axis(_ proxy: OpaquePointer, time: UInt32, axis: UInt32, value: Double)
    func frame(_ proxy: OpaquePointer)
    func axisSource(_ proxy: OpaquePointer, axis_source: UInt32)
    func axisStop(_ proxy: OpaquePointer, time: UInt32, axis: UInt32)
    func axisDiscrete(_ proxy: OpaquePointer, axis: UInt32, discrete: Int32)
    func axisValue120(_ proxy: OpaquePointer, axis: UInt32, value120: Int32)
    func axisRelativeDirection(_ proxy: OpaquePointer, axis: UInt32, direction: UInt32)
}

public enum WlPointerClient {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<wl_pointer_listener> = {
        let p = UnsafeMutablePointer<wl_pointer_listener>.allocate(capacity: 1)
        p.initialize(to: wl_pointer_listener())
        p.pointee.enter = enter_impl
        p.pointee.leave = leave_impl
        p.pointee.motion = motion_impl
        p.pointee.button = button_impl
        p.pointee.axis = axis_impl
        p.pointee.frame = frame_impl
        p.pointee.axis_source = axisSource_impl
        p.pointee.axis_stop = axisStop_impl
        p.pointee.axis_discrete = axisDiscrete_impl
        p.pointee.axis_value120 = axisValue120_impl
        p.pointee.axis_relative_direction = axisRelativeDirection_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        wl_pointer_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> WlPointerEvents? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? WlPointerEvents
    }

    private static let enter_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, OpaquePointer?, wl_fixed_t, wl_fixed_t) -> Void = { data, proxy, serial, surface, surface_x, surface_y in
        guard let data, let proxy, let h = handler(data) else { return }
        h.enter(proxy, serial: serial, surface: surface, surface_x: swift_wayland_fixed_to_double(surface_x), surface_y: swift_wayland_fixed_to_double(surface_y))
    }
    private static let leave_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, OpaquePointer?) -> Void = { data, proxy, serial, surface in
        guard let data, let proxy, let h = handler(data) else { return }
        h.leave(proxy, serial: serial, surface: surface)
    }
    private static let motion_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, wl_fixed_t, wl_fixed_t) -> Void = { data, proxy, time, surface_x, surface_y in
        guard let data, let proxy, let h = handler(data) else { return }
        h.motion(proxy, time: time, surface_x: swift_wayland_fixed_to_double(surface_x), surface_y: swift_wayland_fixed_to_double(surface_y))
    }
    private static let button_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32, UInt32, UInt32) -> Void = { data, proxy, serial, time, button, state in
        guard let data, let proxy, let h = handler(data) else { return }
        h.button(proxy, serial: serial, time: time, button: button, state: state)
    }
    private static let axis_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32, wl_fixed_t) -> Void = { data, proxy, time, axis, value in
        guard let data, let proxy, let h = handler(data) else { return }
        h.axis(proxy, time: time, axis: axis, value: swift_wayland_fixed_to_double(value))
    }
    private static let frame_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.frame(proxy)
    }
    private static let axisSource_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, axis_source in
        guard let data, let proxy, let h = handler(data) else { return }
        h.axisSource(proxy, axis_source: axis_source)
    }
    private static let axisStop_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32) -> Void = { data, proxy, time, axis in
        guard let data, let proxy, let h = handler(data) else { return }
        h.axisStop(proxy, time: time, axis: axis)
    }
    private static let axisDiscrete_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, Int32) -> Void = { data, proxy, axis, discrete in
        guard let data, let proxy, let h = handler(data) else { return }
        h.axisDiscrete(proxy, axis: axis, discrete: discrete)
    }
    private static let axisValue120_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, Int32) -> Void = { data, proxy, axis, value120 in
        guard let data, let proxy, let h = handler(data) else { return }
        h.axisValue120(proxy, axis: axis, value120: value120)
    }
    private static let axisRelativeDirection_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32) -> Void = { data, proxy, axis, direction in
        guard let data, let proxy, let h = handler(data) else { return }
        h.axisRelativeDirection(proxy, axis: axis, direction: direction)
    }
}
