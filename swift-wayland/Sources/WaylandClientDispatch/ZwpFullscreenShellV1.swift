// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zwp_fullscreen_shell_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol ZwpFullscreenShellV1Events: AnyObject {
    func capability(_ proxy: OpaquePointer, capability: UInt32)
}

public enum ZwpFullscreenShellV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zwp_fullscreen_shell_v1_listener> = {
        let p = UnsafeMutablePointer<zwp_fullscreen_shell_v1_listener>.allocate(capacity: 1)
        p.initialize(to: zwp_fullscreen_shell_v1_listener())
        p.pointee.capability = capability_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zwp_fullscreen_shell_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> ZwpFullscreenShellV1Events? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? ZwpFullscreenShellV1Events
    }

    private static let capability_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, capability in
        guard let data, let proxy, let h = handler(data) else { return }
        h.capability(proxy, capability: capability)
    }
}
