// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for wl_surface: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol WlSurfaceEvents: AnyObject {
    func enter(_ proxy: OpaquePointer, output: OpaquePointer?)
    func leave(_ proxy: OpaquePointer, output: OpaquePointer?)
    func preferredBufferScale(_ proxy: OpaquePointer, factor: Int32)
    func preferredBufferTransform(_ proxy: OpaquePointer, transform: UInt32)
}

public enum WlSurfaceClient {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<wl_surface_listener> = {
        let p = UnsafeMutablePointer<wl_surface_listener>.allocate(capacity: 1)
        p.initialize(to: wl_surface_listener())
        p.pointee.enter = enter_impl
        p.pointee.leave = leave_impl
        p.pointee.preferred_buffer_scale = preferredBufferScale_impl
        p.pointee.preferred_buffer_transform = preferredBufferTransform_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        wl_surface_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> WlSurfaceEvents? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? WlSurfaceEvents
    }

    private static let enter_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, output in
        guard let data, let proxy, let h = handler(data) else { return }
        h.enter(proxy, output: output)
    }
    private static let leave_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, output in
        guard let data, let proxy, let h = handler(data) else { return }
        h.leave(proxy, output: output)
    }
    private static let preferredBufferScale_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32) -> Void = { data, proxy, factor in
        guard let data, let proxy, let h = handler(data) else { return }
        h.preferredBufferScale(proxy, factor: factor)
    }
    private static let preferredBufferTransform_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, transform in
        guard let data, let proxy, let h = handler(data) else { return }
        h.preferredBufferTransform(proxy, transform: transform)
    }
}
