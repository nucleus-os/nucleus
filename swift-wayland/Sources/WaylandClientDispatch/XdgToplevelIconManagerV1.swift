// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for xdg_toplevel_icon_manager_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol XdgToplevelIconManagerV1Events: AnyObject {
    func iconSize(_ proxy: OpaquePointer, size: Int32)
    func done(_ proxy: OpaquePointer)
}

public enum XdgToplevelIconManagerV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<xdg_toplevel_icon_manager_v1_listener> = {
        let p = UnsafeMutablePointer<xdg_toplevel_icon_manager_v1_listener>.allocate(capacity: 1)
        p.initialize(to: xdg_toplevel_icon_manager_v1_listener())
        p.pointee.icon_size = iconSize_impl
        p.pointee.done = done_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        xdg_toplevel_icon_manager_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> XdgToplevelIconManagerV1Events? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? XdgToplevelIconManagerV1Events
    }

    private static let iconSize_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32) -> Void = { data, proxy, size in
        guard let data, let proxy, let h = handler(data) else { return }
        h.iconSize(proxy, size: size)
    }
    private static let done_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.done(proxy)
    }
}
