// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zwp_text_input_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ZwpTextInputV1Events: AnyObject {
    func enter(_ proxy: OpaquePointer, surface: OpaquePointer?)
    func leave(_ proxy: OpaquePointer)
    func modifiersMap(_ proxy: OpaquePointer, map: UnsafeMutablePointer<wl_array>?)
    func inputPanelState(_ proxy: OpaquePointer, state: UInt32)
    func preeditString(_ proxy: OpaquePointer, serial: UInt32, text: UnsafePointer<CChar>?, commit: UnsafePointer<CChar>?)
    func preeditStyling(_ proxy: OpaquePointer, index: UInt32, length: UInt32, style: UInt32)
    func preeditCursor(_ proxy: OpaquePointer, index: Int32)
    func commitString(_ proxy: OpaquePointer, serial: UInt32, text: UnsafePointer<CChar>?)
    func cursorPosition(_ proxy: OpaquePointer, index: Int32, anchor: Int32)
    func deleteSurroundingText(_ proxy: OpaquePointer, index: Int32, length: UInt32)
    func keysym(_ proxy: OpaquePointer, serial: UInt32, time: UInt32, sym: UInt32, state: UInt32, modifiers: UInt32)
    func language(_ proxy: OpaquePointer, serial: UInt32, language: UnsafePointer<CChar>?)
    func textDirection(_ proxy: OpaquePointer, serial: UInt32, direction: UInt32)
}

public enum ZwpTextInputV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zwp_text_input_v1_listener> = {
        let p = UnsafeMutablePointer<zwp_text_input_v1_listener>.allocate(capacity: 1)
        p.initialize(to: zwp_text_input_v1_listener())
        p.pointee.enter = enter_impl
        p.pointee.leave = leave_impl
        p.pointee.modifiers_map = modifiersMap_impl
        p.pointee.input_panel_state = inputPanelState_impl
        p.pointee.preedit_string = preeditString_impl
        p.pointee.preedit_styling = preeditStyling_impl
        p.pointee.preedit_cursor = preeditCursor_impl
        p.pointee.commit_string = commitString_impl
        p.pointee.cursor_position = cursorPosition_impl
        p.pointee.delete_surrounding_text = deleteSurroundingText_impl
        p.pointee.keysym = keysym_impl
        p.pointee.language = language_impl
        p.pointee.text_direction = textDirection_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zwp_text_input_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ZwpTextInputV1Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ZwpTextInputV1Events
    }

    private static let enter_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, surface in
        guard let data, let proxy, let h = handler(data) else { return }
        h.enter(proxy, surface: surface)
    }
    private static let leave_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.leave(proxy)
    }
    private static let modifiersMap_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafeMutablePointer<wl_array>?) -> Void = { data, proxy, map in
        guard let data, let proxy, let h = handler(data) else { return }
        h.modifiersMap(proxy, map: map)
    }
    private static let inputPanelState_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, state in
        guard let data, let proxy, let h = handler(data) else { return }
        h.inputPanelState(proxy, state: state)
    }
    private static let preeditString_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UnsafePointer<CChar>?, UnsafePointer<CChar>?) -> Void = { data, proxy, serial, text, commit in
        guard let data, let proxy, let h = handler(data) else { return }
        h.preeditString(proxy, serial: serial, text: text, commit: commit)
    }
    private static let preeditStyling_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32, UInt32) -> Void = { data, proxy, index, length, style in
        guard let data, let proxy, let h = handler(data) else { return }
        h.preeditStyling(proxy, index: index, length: length, style: style)
    }
    private static let preeditCursor_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32) -> Void = { data, proxy, index in
        guard let data, let proxy, let h = handler(data) else { return }
        h.preeditCursor(proxy, index: index)
    }
    private static let commitString_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UnsafePointer<CChar>?) -> Void = { data, proxy, serial, text in
        guard let data, let proxy, let h = handler(data) else { return }
        h.commitString(proxy, serial: serial, text: text)
    }
    private static let cursorPosition_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32, Int32) -> Void = { data, proxy, index, anchor in
        guard let data, let proxy, let h = handler(data) else { return }
        h.cursorPosition(proxy, index: index, anchor: anchor)
    }
    private static let deleteSurroundingText_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32, UInt32) -> Void = { data, proxy, index, length in
        guard let data, let proxy, let h = handler(data) else { return }
        h.deleteSurroundingText(proxy, index: index, length: length)
    }
    private static let keysym_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32, UInt32, UInt32, UInt32) -> Void = { data, proxy, serial, time, sym, state, modifiers in
        guard let data, let proxy, let h = handler(data) else { return }
        h.keysym(proxy, serial: serial, time: time, sym: sym, state: state, modifiers: modifiers)
    }
    private static let language_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UnsafePointer<CChar>?) -> Void = { data, proxy, serial, language in
        guard let data, let proxy, let h = handler(data) else { return }
        h.language(proxy, serial: serial, language: language)
    }
    private static let textDirection_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32) -> Void = { data, proxy, serial, direction in
        guard let data, let proxy, let h = handler(data) else { return }
        h.textDirection(proxy, serial: serial, direction: direction)
    }
}
