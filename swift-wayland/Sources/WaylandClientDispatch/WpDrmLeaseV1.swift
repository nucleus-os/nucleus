// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for wp_drm_lease_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol WpDrmLeaseV1Events: AnyObject {
    func leaseFd(_ proxy: OpaquePointer, leased_fd: Int32)
    func finished(_ proxy: OpaquePointer)
}

public enum WpDrmLeaseV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<wp_drm_lease_v1_listener> = {
        let p = UnsafeMutablePointer<wp_drm_lease_v1_listener>.allocate(capacity: 1)
        p.initialize(to: wp_drm_lease_v1_listener())
        p.pointee.lease_fd = leaseFd_impl
        p.pointee.finished = finished_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        wp_drm_lease_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any WpDrmLeaseV1Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any WpDrmLeaseV1Events
    }

    private static let leaseFd_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32) -> Void = { data, proxy, leased_fd in
        guard let data, let proxy, let h = handler(data) else { return }
        h.leaseFd(proxy, leased_fd: leased_fd)
    }
    private static let finished_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.finished(proxy)
    }
}
