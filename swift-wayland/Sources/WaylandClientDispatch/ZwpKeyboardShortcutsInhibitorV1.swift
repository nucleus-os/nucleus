// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zwp_keyboard_shortcuts_inhibitor_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ZwpKeyboardShortcutsInhibitorV1Events: AnyObject {
    func active(_ proxy: OpaquePointer)
    func inactive(_ proxy: OpaquePointer)
}

public enum ZwpKeyboardShortcutsInhibitorV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zwp_keyboard_shortcuts_inhibitor_v1_listener> = {
        let p = UnsafeMutablePointer<zwp_keyboard_shortcuts_inhibitor_v1_listener>.allocate(capacity: 1)
        p.initialize(to: zwp_keyboard_shortcuts_inhibitor_v1_listener())
        p.pointee.active = active_impl
        p.pointee.inactive = inactive_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zwp_keyboard_shortcuts_inhibitor_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ZwpKeyboardShortcutsInhibitorV1Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ZwpKeyboardShortcutsInhibitorV1Events
    }

    private static let active_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.active(proxy)
    }
    private static let inactive_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.inactive(proxy)
    }
}
