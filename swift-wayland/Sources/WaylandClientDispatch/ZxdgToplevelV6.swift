// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zxdg_toplevel_v6: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ZxdgToplevelV6Events: AnyObject {
    func configure(_ proxy: OpaquePointer, width: Int32, height: Int32, states: UnsafeMutablePointer<wl_array>?)
    func close(_ proxy: OpaquePointer)
}

public enum ZxdgToplevelV6Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zxdg_toplevel_v6_listener> = {
        let p = UnsafeMutablePointer<zxdg_toplevel_v6_listener>.allocate(capacity: 1)
        p.initialize(to: zxdg_toplevel_v6_listener())
        p.pointee.configure = configure_impl
        p.pointee.close = close_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zxdg_toplevel_v6_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ZxdgToplevelV6Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ZxdgToplevelV6Events
    }

    private static let configure_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32, Int32, UnsafeMutablePointer<wl_array>?) -> Void = { data, proxy, width, height, states in
        guard let data, let proxy, let h = handler(data) else { return }
        h.configure(proxy, width: width, height: height, states: states)
    }
    private static let close_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.close(proxy)
    }
}
