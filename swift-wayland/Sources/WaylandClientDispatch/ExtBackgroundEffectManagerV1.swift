// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for ext_background_effect_manager_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ExtBackgroundEffectManagerV1Events: AnyObject {
    func capabilities(_ proxy: OpaquePointer, flags: UInt32)
}

public enum ExtBackgroundEffectManagerV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<ext_background_effect_manager_v1_listener> = {
        let p = UnsafeMutablePointer<ext_background_effect_manager_v1_listener>.allocate(capacity: 1)
        p.initialize(to: ext_background_effect_manager_v1_listener())
        p.pointee.capabilities = capabilities_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        ext_background_effect_manager_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ExtBackgroundEffectManagerV1Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ExtBackgroundEffectManagerV1Events
    }

    private static let capabilities_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, flags in
        guard let data, let proxy, let h = handler(data) else { return }
        h.capabilities(proxy, flags: flags)
    }
}
