// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zwp_linux_dmabuf_feedback_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol ZwpLinuxDmabufFeedbackV1Events: AnyObject {
    func done(_ proxy: OpaquePointer)
    func formatTable(_ proxy: OpaquePointer, fd: Int32, size: UInt32)
    func mainDevice(_ proxy: OpaquePointer, device: UnsafeMutablePointer<wl_array>?)
    func trancheDone(_ proxy: OpaquePointer)
    func trancheTargetDevice(_ proxy: OpaquePointer, device: UnsafeMutablePointer<wl_array>?)
    func trancheFormats(_ proxy: OpaquePointer, indices: UnsafeMutablePointer<wl_array>?)
    func trancheFlags(_ proxy: OpaquePointer, flags: UInt32)
}

public enum ZwpLinuxDmabufFeedbackV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zwp_linux_dmabuf_feedback_v1_listener> = {
        let p = UnsafeMutablePointer<zwp_linux_dmabuf_feedback_v1_listener>.allocate(capacity: 1)
        p.initialize(to: zwp_linux_dmabuf_feedback_v1_listener())
        p.pointee.done = done_impl
        p.pointee.format_table = formatTable_impl
        p.pointee.main_device = mainDevice_impl
        p.pointee.tranche_done = trancheDone_impl
        p.pointee.tranche_target_device = trancheTargetDevice_impl
        p.pointee.tranche_formats = trancheFormats_impl
        p.pointee.tranche_flags = trancheFlags_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zwp_linux_dmabuf_feedback_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> ZwpLinuxDmabufFeedbackV1Events? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? ZwpLinuxDmabufFeedbackV1Events
    }

    private static let done_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.done(proxy)
    }
    private static let formatTable_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32, UInt32) -> Void = { data, proxy, fd, size in
        guard let data, let proxy, let h = handler(data) else { return }
        h.formatTable(proxy, fd: fd, size: size)
    }
    private static let mainDevice_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafeMutablePointer<wl_array>?) -> Void = { data, proxy, device in
        guard let data, let proxy, let h = handler(data) else { return }
        h.mainDevice(proxy, device: device)
    }
    private static let trancheDone_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.trancheDone(proxy)
    }
    private static let trancheTargetDevice_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafeMutablePointer<wl_array>?) -> Void = { data, proxy, device in
        guard let data, let proxy, let h = handler(data) else { return }
        h.trancheTargetDevice(proxy, device: device)
    }
    private static let trancheFormats_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafeMutablePointer<wl_array>?) -> Void = { data, proxy, indices in
        guard let data, let proxy, let h = handler(data) else { return }
        h.trancheFormats(proxy, indices: indices)
    }
    private static let trancheFlags_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, flags in
        guard let data, let proxy, let h = handler(data) else { return }
        h.trancheFlags(proxy, flags: flags)
    }
}
