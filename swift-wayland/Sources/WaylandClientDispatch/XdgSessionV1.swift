// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for xdg_session_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol XdgSessionV1Events: AnyObject {
    func created(_ proxy: OpaquePointer, session_id: UnsafePointer<CChar>?)
    func restored(_ proxy: OpaquePointer)
    func replaced(_ proxy: OpaquePointer)
}

public enum XdgSessionV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<xdg_session_v1_listener> = {
        let p = UnsafeMutablePointer<xdg_session_v1_listener>.allocate(capacity: 1)
        p.initialize(to: xdg_session_v1_listener())
        p.pointee.created = created_impl
        p.pointee.restored = restored_impl
        p.pointee.replaced = replaced_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        xdg_session_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any XdgSessionV1Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any XdgSessionV1Events
    }

    private static let created_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, session_id in
        guard let data, let proxy, let h = handler(data) else { return }
        h.created(proxy, session_id: session_id)
    }
    private static let restored_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.restored(proxy)
    }
    private static let replaced_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.replaced(proxy)
    }
}
