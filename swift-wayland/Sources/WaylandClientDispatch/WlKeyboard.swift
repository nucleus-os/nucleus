// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for wl_keyboard: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol WlKeyboardEvents: AnyObject {
    func keymap(_ proxy: OpaquePointer, format: UInt32, fd: Int32, size: UInt32)
    func enter(_ proxy: OpaquePointer, serial: UInt32, surface: OpaquePointer?, keys: UnsafeMutablePointer<wl_array>?)
    func leave(_ proxy: OpaquePointer, serial: UInt32, surface: OpaquePointer?)
    func key(_ proxy: OpaquePointer, serial: UInt32, time: UInt32, key: UInt32, state: UInt32)
    func modifiers(_ proxy: OpaquePointer, serial: UInt32, mods_depressed: UInt32, mods_latched: UInt32, mods_locked: UInt32, group: UInt32)
    func repeatInfo(_ proxy: OpaquePointer, rate: Int32, delay: Int32)
}

public enum WlKeyboardClient {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<wl_keyboard_listener> = {
        let p = UnsafeMutablePointer<wl_keyboard_listener>.allocate(capacity: 1)
        p.initialize(to: wl_keyboard_listener())
        p.pointee.keymap = keymap_impl
        p.pointee.enter = enter_impl
        p.pointee.leave = leave_impl
        p.pointee.key = key_impl
        p.pointee.modifiers = modifiers_impl
        p.pointee.repeat_info = repeatInfo_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        wl_keyboard_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> WlKeyboardEvents? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? WlKeyboardEvents
    }

    private static let keymap_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, Int32, UInt32) -> Void = { data, proxy, format, fd, size in
        guard let data, let proxy, let h = handler(data) else { return }
        h.keymap(proxy, format: format, fd: fd, size: size)
    }
    private static let enter_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, OpaquePointer?, UnsafeMutablePointer<wl_array>?) -> Void = { data, proxy, serial, surface, keys in
        guard let data, let proxy, let h = handler(data) else { return }
        h.enter(proxy, serial: serial, surface: surface, keys: keys)
    }
    private static let leave_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, OpaquePointer?) -> Void = { data, proxy, serial, surface in
        guard let data, let proxy, let h = handler(data) else { return }
        h.leave(proxy, serial: serial, surface: surface)
    }
    private static let key_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32, UInt32, UInt32) -> Void = { data, proxy, serial, time, key, state in
        guard let data, let proxy, let h = handler(data) else { return }
        h.key(proxy, serial: serial, time: time, key: key, state: state)
    }
    private static let modifiers_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32, UInt32, UInt32, UInt32) -> Void = { data, proxy, serial, mods_depressed, mods_latched, mods_locked, group in
        guard let data, let proxy, let h = handler(data) else { return }
        h.modifiers(proxy, serial: serial, mods_depressed: mods_depressed, mods_latched: mods_latched, mods_locked: mods_locked, group: group)
    }
    private static let repeatInfo_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32, Int32) -> Void = { data, proxy, rate, delay in
        guard let data, let proxy, let h = handler(data) else { return }
        h.repeatInfo(proxy, rate: rate, delay: delay)
    }
}
