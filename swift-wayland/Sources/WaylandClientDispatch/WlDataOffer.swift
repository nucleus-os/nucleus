// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for wl_data_offer: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol WlDataOfferEvents: AnyObject {
    func offer(_ proxy: OpaquePointer, mime_type: UnsafePointer<CChar>?)
    func sourceActions(_ proxy: OpaquePointer, source_actions: UInt32)
    func action(_ proxy: OpaquePointer, dnd_action: UInt32)
}

public enum WlDataOfferClient {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<wl_data_offer_listener> = {
        let p = UnsafeMutablePointer<wl_data_offer_listener>.allocate(capacity: 1)
        p.initialize(to: wl_data_offer_listener())
        p.pointee.offer = offer_impl
        p.pointee.source_actions = sourceActions_impl
        p.pointee.action = action_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        wl_data_offer_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> WlDataOfferEvents? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? WlDataOfferEvents
    }

    private static let offer_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, mime_type in
        guard let data, let proxy, let h = handler(data) else { return }
        h.offer(proxy, mime_type: mime_type)
    }
    private static let sourceActions_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, source_actions in
        guard let data, let proxy, let h = handler(data) else { return }
        h.sourceActions(proxy, source_actions: source_actions)
    }
    private static let action_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, dnd_action in
        guard let data, let proxy, let h = handler(data) else { return }
        h.action(proxy, dnd_action: dnd_action)
    }
}
