// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for wp_color_management_surface_feedback_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol WpColorManagementSurfaceFeedbackV1Events: AnyObject {
    func preferredChanged(_ proxy: OpaquePointer, identity: UInt32)
    func preferredChanged2(_ proxy: OpaquePointer, identity_hi: UInt32, identity_lo: UInt32)
}

public enum WpColorManagementSurfaceFeedbackV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<wp_color_management_surface_feedback_v1_listener> = {
        let p = UnsafeMutablePointer<wp_color_management_surface_feedback_v1_listener>.allocate(capacity: 1)
        p.initialize(to: wp_color_management_surface_feedback_v1_listener())
        p.pointee.preferred_changed = preferredChanged_impl
        p.pointee.preferred_changed2 = preferredChanged2_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        wp_color_management_surface_feedback_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any WpColorManagementSurfaceFeedbackV1Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any WpColorManagementSurfaceFeedbackV1Events
    }

    private static let preferredChanged_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, identity in
        guard let data, let proxy, let h = handler(data) else { return }
        h.preferredChanged(proxy, identity: identity)
    }
    private static let preferredChanged2_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32) -> Void = { data, proxy, identity_hi, identity_lo in
        guard let data, let proxy, let h = handler(data) else { return }
        h.preferredChanged2(proxy, identity_hi: identity_hi, identity_lo: identity_lo)
    }
}
