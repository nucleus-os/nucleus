// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zwlr_gamma_control_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ZwlrGammaControlV1Events: AnyObject {
    func gammaSize(_ proxy: OpaquePointer, size: UInt32)
    func failed(_ proxy: OpaquePointer)
}

public enum ZwlrGammaControlV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zwlr_gamma_control_v1_listener> = {
        let p = UnsafeMutablePointer<zwlr_gamma_control_v1_listener>.allocate(capacity: 1)
        p.initialize(to: zwlr_gamma_control_v1_listener())
        p.pointee.gamma_size = gammaSize_impl
        p.pointee.failed = failed_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zwlr_gamma_control_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ZwlrGammaControlV1Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ZwlrGammaControlV1Events
    }

    private static let gammaSize_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, size in
        guard let data, let proxy, let h = handler(data) else { return }
        h.gammaSize(proxy, size: size)
    }
    private static let failed_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.failed(proxy)
    }
}
