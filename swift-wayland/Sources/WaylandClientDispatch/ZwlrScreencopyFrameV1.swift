// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zwlr_screencopy_frame_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ZwlrScreencopyFrameV1Events: AnyObject {
    func buffer(_ proxy: OpaquePointer, format: UInt32, width: UInt32, height: UInt32, stride: UInt32)
    func flags(_ proxy: OpaquePointer, flags: UInt32)
    func ready(_ proxy: OpaquePointer, tv_sec_hi: UInt32, tv_sec_lo: UInt32, tv_nsec: UInt32)
    func failed(_ proxy: OpaquePointer)
    func damage(_ proxy: OpaquePointer, x: UInt32, y: UInt32, width: UInt32, height: UInt32)
    func linuxDmabuf(_ proxy: OpaquePointer, format: UInt32, width: UInt32, height: UInt32)
    func bufferDone(_ proxy: OpaquePointer)
}

public enum ZwlrScreencopyFrameV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zwlr_screencopy_frame_v1_listener> = {
        let p = UnsafeMutablePointer<zwlr_screencopy_frame_v1_listener>.allocate(capacity: 1)
        p.initialize(to: zwlr_screencopy_frame_v1_listener())
        p.pointee.buffer = buffer_impl
        p.pointee.flags = flags_impl
        p.pointee.ready = ready_impl
        p.pointee.failed = failed_impl
        p.pointee.damage = damage_impl
        p.pointee.linux_dmabuf = linuxDmabuf_impl
        p.pointee.buffer_done = bufferDone_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zwlr_screencopy_frame_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ZwlrScreencopyFrameV1Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ZwlrScreencopyFrameV1Events
    }

    private static let buffer_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32, UInt32, UInt32) -> Void = { data, proxy, format, width, height, stride in
        guard let data, let proxy, let h = handler(data) else { return }
        h.buffer(proxy, format: format, width: width, height: height, stride: stride)
    }
    private static let flags_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, flags in
        guard let data, let proxy, let h = handler(data) else { return }
        h.flags(proxy, flags: flags)
    }
    private static let ready_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32, UInt32) -> Void = { data, proxy, tv_sec_hi, tv_sec_lo, tv_nsec in
        guard let data, let proxy, let h = handler(data) else { return }
        h.ready(proxy, tv_sec_hi: tv_sec_hi, tv_sec_lo: tv_sec_lo, tv_nsec: tv_nsec)
    }
    private static let failed_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.failed(proxy)
    }
    private static let damage_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32, UInt32, UInt32) -> Void = { data, proxy, x, y, width, height in
        guard let data, let proxy, let h = handler(data) else { return }
        h.damage(proxy, x: x, y: y, width: width, height: height)
    }
    private static let linuxDmabuf_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32, UInt32) -> Void = { data, proxy, format, width, height in
        guard let data, let proxy, let h = handler(data) else { return }
        h.linuxDmabuf(proxy, format: format, width: width, height: height)
    }
    private static let bufferDone_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.bufferDone(proxy)
    }
}
