// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for ext_workspace_manager_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ExtWorkspaceManagerV1Events: AnyObject {
    func workspaceGroup(_ proxy: OpaquePointer, workspace_group: OpaquePointer?)
    func workspace(_ proxy: OpaquePointer, workspace: OpaquePointer?)
    func done(_ proxy: OpaquePointer)
    func finished(_ proxy: OpaquePointer)
}

public enum ExtWorkspaceManagerV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<ext_workspace_manager_v1_listener> = {
        let p = UnsafeMutablePointer<ext_workspace_manager_v1_listener>.allocate(capacity: 1)
        p.initialize(to: ext_workspace_manager_v1_listener())
        p.pointee.workspace_group = workspaceGroup_impl
        p.pointee.workspace = workspace_impl
        p.pointee.done = done_impl
        p.pointee.finished = finished_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        ext_workspace_manager_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ExtWorkspaceManagerV1Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ExtWorkspaceManagerV1Events
    }

    private static let workspaceGroup_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, workspace_group in
        guard let data, let proxy, let h = handler(data) else { return }
        h.workspaceGroup(proxy, workspace_group: workspace_group)
    }
    private static let workspace_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, workspace in
        guard let data, let proxy, let h = handler(data) else { return }
        h.workspace(proxy, workspace: workspace)
    }
    private static let done_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.done(proxy)
    }
    private static let finished_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.finished(proxy)
    }
}
