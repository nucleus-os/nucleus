// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zxdg_output_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ZxdgOutputV1Events: AnyObject {
    func logicalPosition(_ proxy: OpaquePointer, x: Int32, y: Int32)
    func logicalSize(_ proxy: OpaquePointer, width: Int32, height: Int32)
    func done(_ proxy: OpaquePointer)
    func name(_ proxy: OpaquePointer, name: UnsafePointer<CChar>?)
    func description(_ proxy: OpaquePointer, description: UnsafePointer<CChar>?)
}

public enum ZxdgOutputV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zxdg_output_v1_listener> = {
        let p = UnsafeMutablePointer<zxdg_output_v1_listener>.allocate(capacity: 1)
        p.initialize(to: zxdg_output_v1_listener())
        p.pointee.logical_position = logicalPosition_impl
        p.pointee.logical_size = logicalSize_impl
        p.pointee.done = done_impl
        p.pointee.name = name_impl
        p.pointee.description = description_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zxdg_output_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ZxdgOutputV1Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ZxdgOutputV1Events
    }

    private static let logicalPosition_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32, Int32) -> Void = { data, proxy, x, y in
        guard let data, let proxy, let h = handler(data) else { return }
        h.logicalPosition(proxy, x: x, y: y)
    }
    private static let logicalSize_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32, Int32) -> Void = { data, proxy, width, height in
        guard let data, let proxy, let h = handler(data) else { return }
        h.logicalSize(proxy, width: width, height: height)
    }
    private static let done_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.done(proxy)
    }
    private static let name_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, name in
        guard let data, let proxy, let h = handler(data) else { return }
        h.name(proxy, name: name)
    }
    private static let description_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, description in
        guard let data, let proxy, let h = handler(data) else { return }
        h.description(proxy, description: description)
    }
}
