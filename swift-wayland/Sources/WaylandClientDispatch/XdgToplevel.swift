// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for xdg_toplevel: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol XdgToplevelEvents: AnyObject {
    func configure(_ proxy: OpaquePointer, width: Int32, height: Int32, states: UnsafeMutablePointer<wl_array>?)
    func close(_ proxy: OpaquePointer)
    func configureBounds(_ proxy: OpaquePointer, width: Int32, height: Int32)
    func wmCapabilities(_ proxy: OpaquePointer, capabilities: UnsafeMutablePointer<wl_array>?)
}

public enum XdgToplevelClient {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<xdg_toplevel_listener> = {
        let p = UnsafeMutablePointer<xdg_toplevel_listener>.allocate(capacity: 1)
        p.initialize(to: xdg_toplevel_listener())
        p.pointee.configure = configure_impl
        p.pointee.close = close_impl
        p.pointee.configure_bounds = configureBounds_impl
        p.pointee.wm_capabilities = wmCapabilities_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        xdg_toplevel_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> XdgToplevelEvents? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? XdgToplevelEvents
    }

    private static let configure_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32, Int32, UnsafeMutablePointer<wl_array>?) -> Void = { data, proxy, width, height, states in
        guard let data, let proxy, let h = handler(data) else { return }
        h.configure(proxy, width: width, height: height, states: states)
    }
    private static let close_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.close(proxy)
    }
    private static let configureBounds_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32, Int32) -> Void = { data, proxy, width, height in
        guard let data, let proxy, let h = handler(data) else { return }
        h.configureBounds(proxy, width: width, height: height)
    }
    private static let wmCapabilities_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafeMutablePointer<wl_array>?) -> Void = { data, proxy, capabilities in
        guard let data, let proxy, let h = handler(data) else { return }
        h.wmCapabilities(proxy, capabilities: capabilities)
    }
}
