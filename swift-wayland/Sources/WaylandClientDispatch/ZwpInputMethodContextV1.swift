// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zwp_input_method_context_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ZwpInputMethodContextV1Events: AnyObject {
    func surroundingText(_ proxy: OpaquePointer, text: UnsafePointer<CChar>?, cursor: UInt32, anchor: UInt32)
    func reset(_ proxy: OpaquePointer)
    func contentType(_ proxy: OpaquePointer, hint: UInt32, purpose: UInt32)
    func invokeAction(_ proxy: OpaquePointer, button: UInt32, index: UInt32)
    func commitState(_ proxy: OpaquePointer, serial: UInt32)
    func preferredLanguage(_ proxy: OpaquePointer, language: UnsafePointer<CChar>?)
}

public enum ZwpInputMethodContextV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zwp_input_method_context_v1_listener> = {
        let p = UnsafeMutablePointer<zwp_input_method_context_v1_listener>.allocate(capacity: 1)
        p.initialize(to: zwp_input_method_context_v1_listener())
        p.pointee.surrounding_text = surroundingText_impl
        p.pointee.reset = reset_impl
        p.pointee.content_type = contentType_impl
        p.pointee.invoke_action = invokeAction_impl
        p.pointee.commit_state = commitState_impl
        p.pointee.preferred_language = preferredLanguage_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zwp_input_method_context_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ZwpInputMethodContextV1Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ZwpInputMethodContextV1Events
    }

    private static let surroundingText_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?, UInt32, UInt32) -> Void = { data, proxy, text, cursor, anchor in
        guard let data, let proxy, let h = handler(data) else { return }
        h.surroundingText(proxy, text: text, cursor: cursor, anchor: anchor)
    }
    private static let reset_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.reset(proxy)
    }
    private static let contentType_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32) -> Void = { data, proxy, hint, purpose in
        guard let data, let proxy, let h = handler(data) else { return }
        h.contentType(proxy, hint: hint, purpose: purpose)
    }
    private static let invokeAction_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32) -> Void = { data, proxy, button, index in
        guard let data, let proxy, let h = handler(data) else { return }
        h.invokeAction(proxy, button: button, index: index)
    }
    private static let commitState_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, serial in
        guard let data, let proxy, let h = handler(data) else { return }
        h.commitState(proxy, serial: serial)
    }
    private static let preferredLanguage_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, language in
        guard let data, let proxy, let h = handler(data) else { return }
        h.preferredLanguage(proxy, language: language)
    }
}
