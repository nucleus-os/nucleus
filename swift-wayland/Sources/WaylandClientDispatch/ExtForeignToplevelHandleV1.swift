// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for ext_foreign_toplevel_handle_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol ExtForeignToplevelHandleV1Events: AnyObject {
    func closed(_ proxy: OpaquePointer)
    func done(_ proxy: OpaquePointer)
    func title(_ proxy: OpaquePointer, title: UnsafePointer<CChar>?)
    func appId(_ proxy: OpaquePointer, app_id: UnsafePointer<CChar>?)
    func identifier(_ proxy: OpaquePointer, identifier: UnsafePointer<CChar>?)
}

public enum ExtForeignToplevelHandleV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<ext_foreign_toplevel_handle_v1_listener> = {
        let p = UnsafeMutablePointer<ext_foreign_toplevel_handle_v1_listener>.allocate(capacity: 1)
        p.initialize(to: ext_foreign_toplevel_handle_v1_listener())
        p.pointee.closed = closed_impl
        p.pointee.done = done_impl
        p.pointee.title = title_impl
        p.pointee.app_id = appId_impl
        p.pointee.identifier = identifier_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        ext_foreign_toplevel_handle_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> ExtForeignToplevelHandleV1Events? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? ExtForeignToplevelHandleV1Events
    }

    private static let closed_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.closed(proxy)
    }
    private static let done_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.done(proxy)
    }
    private static let title_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, title in
        guard let data, let proxy, let h = handler(data) else { return }
        h.title(proxy, title: title)
    }
    private static let appId_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, app_id in
        guard let data, let proxy, let h = handler(data) else { return }
        h.appId(proxy, app_id: app_id)
    }
    private static let identifier_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, identifier in
        guard let data, let proxy, let h = handler(data) else { return }
        h.identifier(proxy, identifier: identifier)
    }
}
