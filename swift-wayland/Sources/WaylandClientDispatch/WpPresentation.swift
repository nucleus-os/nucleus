// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for wp_presentation: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol WpPresentationEvents: AnyObject {
    func clockId(_ proxy: OpaquePointer, clk_id: UInt32)
}

public enum WpPresentationClient {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<wp_presentation_listener> = {
        let p = UnsafeMutablePointer<wp_presentation_listener>.allocate(capacity: 1)
        p.initialize(to: wp_presentation_listener())
        p.pointee.clock_id = clockId_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        wp_presentation_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any WpPresentationEvents)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any WpPresentationEvents
    }

    private static let clockId_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, clk_id in
        guard let data, let proxy, let h = handler(data) else { return }
        h.clockId(proxy, clk_id: clk_id)
    }
}
