// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zwp_input_method_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ZwpInputMethodV1Events: AnyObject {
    func activate(_ proxy: OpaquePointer, id: OpaquePointer?)
    func deactivate(_ proxy: OpaquePointer, context: OpaquePointer?)
}

public enum ZwpInputMethodV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zwp_input_method_v1_listener> = {
        let p = UnsafeMutablePointer<zwp_input_method_v1_listener>.allocate(capacity: 1)
        p.initialize(to: zwp_input_method_v1_listener())
        p.pointee.activate = activate_impl
        p.pointee.deactivate = deactivate_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zwp_input_method_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ZwpInputMethodV1Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ZwpInputMethodV1Events
    }

    private static let activate_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, id in
        guard let data, let proxy, let h = handler(data) else { return }
        h.activate(proxy, id: id)
    }
    private static let deactivate_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, context in
        guard let data, let proxy, let h = handler(data) else { return }
        h.deactivate(proxy, context: context)
    }
}
