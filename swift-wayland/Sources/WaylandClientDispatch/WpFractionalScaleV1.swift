// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for wp_fractional_scale_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol WpFractionalScaleV1Events: AnyObject {
    func preferredScale(_ proxy: OpaquePointer, scale: UInt32)
}

public enum WpFractionalScaleV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<wp_fractional_scale_v1_listener> = {
        let p = UnsafeMutablePointer<wp_fractional_scale_v1_listener>.allocate(capacity: 1)
        p.initialize(to: wp_fractional_scale_v1_listener())
        p.pointee.preferred_scale = preferredScale_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        wp_fractional_scale_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> WpFractionalScaleV1Events? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? WpFractionalScaleV1Events
    }

    private static let preferredScale_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, scale in
        guard let data, let proxy, let h = handler(data) else { return }
        h.preferredScale(proxy, scale: scale)
    }
}
