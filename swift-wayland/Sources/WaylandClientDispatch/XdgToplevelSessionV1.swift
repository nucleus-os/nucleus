// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for xdg_toplevel_session_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol XdgToplevelSessionV1Events: AnyObject {
    func restored(_ proxy: OpaquePointer)
}

public enum XdgToplevelSessionV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<xdg_toplevel_session_v1_listener> = {
        let p = UnsafeMutablePointer<xdg_toplevel_session_v1_listener>.allocate(capacity: 1)
        p.initialize(to: xdg_toplevel_session_v1_listener())
        p.pointee.restored = restored_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        xdg_toplevel_session_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> XdgToplevelSessionV1Events? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? XdgToplevelSessionV1Events
    }

    private static let restored_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.restored(proxy)
    }
}
