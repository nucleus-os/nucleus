// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for wp_color_representation_manager_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol WpColorRepresentationManagerV1Events: AnyObject {
    func supportedAlphaMode(_ proxy: OpaquePointer, alpha_mode: UInt32)
    func supportedCoefficientsAndRanges(_ proxy: OpaquePointer, coefficients: UInt32, range: UInt32)
    func done(_ proxy: OpaquePointer)
}

public enum WpColorRepresentationManagerV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<wp_color_representation_manager_v1_listener> = {
        let p = UnsafeMutablePointer<wp_color_representation_manager_v1_listener>.allocate(capacity: 1)
        p.initialize(to: wp_color_representation_manager_v1_listener())
        p.pointee.supported_alpha_mode = supportedAlphaMode_impl
        p.pointee.supported_coefficients_and_ranges = supportedCoefficientsAndRanges_impl
        p.pointee.done = done_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        wp_color_representation_manager_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any WpColorRepresentationManagerV1Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any WpColorRepresentationManagerV1Events
    }

    private static let supportedAlphaMode_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, alpha_mode in
        guard let data, let proxy, let h = handler(data) else { return }
        h.supportedAlphaMode(proxy, alpha_mode: alpha_mode)
    }
    private static let supportedCoefficientsAndRanges_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32) -> Void = { data, proxy, coefficients, range in
        guard let data, let proxy, let h = handler(data) else { return }
        h.supportedCoefficientsAndRanges(proxy, coefficients: coefficients, range: range)
    }
    private static let done_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.done(proxy)
    }
}
