// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zwlr_output_mode_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ZwlrOutputModeV1Events: AnyObject {
    func size(_ proxy: OpaquePointer, width: Int32, height: Int32)
    func refresh(_ proxy: OpaquePointer, refresh: Int32)
    func preferred(_ proxy: OpaquePointer)
    func finished(_ proxy: OpaquePointer)
}

public enum ZwlrOutputModeV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zwlr_output_mode_v1_listener> = {
        let p = UnsafeMutablePointer<zwlr_output_mode_v1_listener>.allocate(capacity: 1)
        p.initialize(to: zwlr_output_mode_v1_listener())
        p.pointee.size = size_impl
        p.pointee.refresh = refresh_impl
        p.pointee.preferred = preferred_impl
        p.pointee.finished = finished_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zwlr_output_mode_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ZwlrOutputModeV1Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ZwlrOutputModeV1Events
    }

    private static let size_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32, Int32) -> Void = { data, proxy, width, height in
        guard let data, let proxy, let h = handler(data) else { return }
        h.size(proxy, width: width, height: height)
    }
    private static let refresh_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32) -> Void = { data, proxy, refresh in
        guard let data, let proxy, let h = handler(data) else { return }
        h.refresh(proxy, refresh: refresh)
    }
    private static let preferred_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.preferred(proxy)
    }
    private static let finished_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.finished(proxy)
    }
}
