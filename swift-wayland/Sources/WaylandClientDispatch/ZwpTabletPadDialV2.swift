// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zwp_tablet_pad_dial_v2: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ZwpTabletPadDialV2Events: AnyObject {
    func delta(_ proxy: OpaquePointer, value120: Int32)
    func frame(_ proxy: OpaquePointer, time: UInt32)
}

public enum ZwpTabletPadDialV2Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zwp_tablet_pad_dial_v2_listener> = {
        let p = UnsafeMutablePointer<zwp_tablet_pad_dial_v2_listener>.allocate(capacity: 1)
        p.initialize(to: zwp_tablet_pad_dial_v2_listener())
        p.pointee.delta = delta_impl
        p.pointee.frame = frame_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zwp_tablet_pad_dial_v2_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ZwpTabletPadDialV2Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ZwpTabletPadDialV2Events
    }

    private static let delta_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32) -> Void = { data, proxy, value120 in
        guard let data, let proxy, let h = handler(data) else { return }
        h.delta(proxy, value120: value120)
    }
    private static let frame_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, time in
        guard let data, let proxy, let h = handler(data) else { return }
        h.frame(proxy, time: time)
    }
}
