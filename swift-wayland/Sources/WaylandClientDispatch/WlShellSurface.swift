// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for wl_shell_surface: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol WlShellSurfaceEvents: AnyObject {
    func ping(_ proxy: OpaquePointer, serial: UInt32)
    func configure(_ proxy: OpaquePointer, edges: UInt32, width: Int32, height: Int32)
    func popupDone(_ proxy: OpaquePointer)
}

public enum WlShellSurfaceClient {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<wl_shell_surface_listener> = {
        let p = UnsafeMutablePointer<wl_shell_surface_listener>.allocate(capacity: 1)
        p.initialize(to: wl_shell_surface_listener())
        p.pointee.ping = ping_impl
        p.pointee.configure = configure_impl
        p.pointee.popup_done = popupDone_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        wl_shell_surface_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any WlShellSurfaceEvents)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any WlShellSurfaceEvents
    }

    private static let ping_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, serial in
        guard let data, let proxy, let h = handler(data) else { return }
        h.ping(proxy, serial: serial)
    }
    private static let configure_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, Int32, Int32) -> Void = { data, proxy, edges, width, height in
        guard let data, let proxy, let h = handler(data) else { return }
        h.configure(proxy, edges: edges, width: width, height: height)
    }
    private static let popupDone_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.popupDone(proxy)
    }
}
