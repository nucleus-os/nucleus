// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for ext_workspace_handle_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ExtWorkspaceHandleV1Events: AnyObject {
    func id(_ proxy: OpaquePointer, id: UnsafePointer<CChar>?)
    func name(_ proxy: OpaquePointer, name: UnsafePointer<CChar>?)
    func coordinates(_ proxy: OpaquePointer, coordinates: UnsafeMutablePointer<wl_array>?)
    func state(_ proxy: OpaquePointer, state: UInt32)
    func capabilities(_ proxy: OpaquePointer, capabilities: UInt32)
    func removed(_ proxy: OpaquePointer)
}

public enum ExtWorkspaceHandleV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<ext_workspace_handle_v1_listener> = {
        let p = UnsafeMutablePointer<ext_workspace_handle_v1_listener>.allocate(capacity: 1)
        p.initialize(to: ext_workspace_handle_v1_listener())
        p.pointee.id = id_impl
        p.pointee.name = name_impl
        p.pointee.coordinates = coordinates_impl
        p.pointee.state = state_impl
        p.pointee.capabilities = capabilities_impl
        p.pointee.removed = removed_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        ext_workspace_handle_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ExtWorkspaceHandleV1Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ExtWorkspaceHandleV1Events
    }

    private static let id_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, id in
        guard let data, let proxy, let h = handler(data) else { return }
        h.id(proxy, id: id)
    }
    private static let name_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, name in
        guard let data, let proxy, let h = handler(data) else { return }
        h.name(proxy, name: name)
    }
    private static let coordinates_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafeMutablePointer<wl_array>?) -> Void = { data, proxy, coordinates in
        guard let data, let proxy, let h = handler(data) else { return }
        h.coordinates(proxy, coordinates: coordinates)
    }
    private static let state_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, state in
        guard let data, let proxy, let h = handler(data) else { return }
        h.state(proxy, state: state)
    }
    private static let capabilities_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, capabilities in
        guard let data, let proxy, let h = handler(data) else { return }
        h.capabilities(proxy, capabilities: capabilities)
    }
    private static let removed_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.removed(proxy)
    }
}
