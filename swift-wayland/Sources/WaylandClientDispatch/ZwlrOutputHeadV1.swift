// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zwlr_output_head_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ZwlrOutputHeadV1Events: AnyObject {
    func name(_ proxy: OpaquePointer, name: UnsafePointer<CChar>?)
    func description(_ proxy: OpaquePointer, description: UnsafePointer<CChar>?)
    func physicalSize(_ proxy: OpaquePointer, width: Int32, height: Int32)
    func mode(_ proxy: OpaquePointer, mode: OpaquePointer?)
    func enabled(_ proxy: OpaquePointer, enabled: Int32)
    func currentMode(_ proxy: OpaquePointer, mode: OpaquePointer?)
    func position(_ proxy: OpaquePointer, x: Int32, y: Int32)
    func transform(_ proxy: OpaquePointer, transform: Int32)
    func scale(_ proxy: OpaquePointer, scale: Double)
    func finished(_ proxy: OpaquePointer)
    func make(_ proxy: OpaquePointer, make: UnsafePointer<CChar>?)
    func model(_ proxy: OpaquePointer, model: UnsafePointer<CChar>?)
    func serialNumber(_ proxy: OpaquePointer, serial_number: UnsafePointer<CChar>?)
    func adaptiveSync(_ proxy: OpaquePointer, state: UInt32)
}

public enum ZwlrOutputHeadV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zwlr_output_head_v1_listener> = {
        let p = UnsafeMutablePointer<zwlr_output_head_v1_listener>.allocate(capacity: 1)
        p.initialize(to: zwlr_output_head_v1_listener())
        p.pointee.name = name_impl
        p.pointee.description = description_impl
        p.pointee.physical_size = physicalSize_impl
        p.pointee.mode = mode_impl
        p.pointee.enabled = enabled_impl
        p.pointee.current_mode = currentMode_impl
        p.pointee.position = position_impl
        p.pointee.transform = transform_impl
        p.pointee.scale = scale_impl
        p.pointee.finished = finished_impl
        p.pointee.make = make_impl
        p.pointee.model = model_impl
        p.pointee.serial_number = serialNumber_impl
        p.pointee.adaptive_sync = adaptiveSync_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zwlr_output_head_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ZwlrOutputHeadV1Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ZwlrOutputHeadV1Events
    }

    private static let name_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, name in
        guard let data, let proxy, let h = handler(data) else { return }
        h.name(proxy, name: name)
    }
    private static let description_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, description in
        guard let data, let proxy, let h = handler(data) else { return }
        h.description(proxy, description: description)
    }
    private static let physicalSize_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32, Int32) -> Void = { data, proxy, width, height in
        guard let data, let proxy, let h = handler(data) else { return }
        h.physicalSize(proxy, width: width, height: height)
    }
    private static let mode_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, mode in
        guard let data, let proxy, let h = handler(data) else { return }
        h.mode(proxy, mode: mode)
    }
    private static let enabled_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32) -> Void = { data, proxy, enabled in
        guard let data, let proxy, let h = handler(data) else { return }
        h.enabled(proxy, enabled: enabled)
    }
    private static let currentMode_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, mode in
        guard let data, let proxy, let h = handler(data) else { return }
        h.currentMode(proxy, mode: mode)
    }
    private static let position_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32, Int32) -> Void = { data, proxy, x, y in
        guard let data, let proxy, let h = handler(data) else { return }
        h.position(proxy, x: x, y: y)
    }
    private static let transform_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, Int32) -> Void = { data, proxy, transform in
        guard let data, let proxy, let h = handler(data) else { return }
        h.transform(proxy, transform: transform)
    }
    private static let scale_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, wl_fixed_t) -> Void = { data, proxy, scale in
        guard let data, let proxy, let h = handler(data) else { return }
        h.scale(proxy, scale: swift_wayland_fixed_to_double(scale))
    }
    private static let finished_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.finished(proxy)
    }
    private static let make_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, make in
        guard let data, let proxy, let h = handler(data) else { return }
        h.make(proxy, make: make)
    }
    private static let model_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, model in
        guard let data, let proxy, let h = handler(data) else { return }
        h.model(proxy, model: model)
    }
    private static let serialNumber_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UnsafePointer<CChar>?) -> Void = { data, proxy, serial_number in
        guard let data, let proxy, let h = handler(data) else { return }
        h.serialNumber(proxy, serial_number: serial_number)
    }
    private static let adaptiveSync_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, state in
        guard let data, let proxy, let h = handler(data) else { return }
        h.adaptiveSync(proxy, state: state)
    }
}
