// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for wl_shm: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol WlShmEvents: AnyObject {
    func format(_ proxy: OpaquePointer, format: UInt32)
}

public enum WlShmClient {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<wl_shm_listener> = {
        let p = UnsafeMutablePointer<wl_shm_listener>.allocate(capacity: 1)
        p.initialize(to: wl_shm_listener())
        p.pointee.format = format_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        wl_shm_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> WlShmEvents? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? WlShmEvents
    }

    private static let format_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, format in
        guard let data, let proxy, let h = handler(data) else { return }
        h.format(proxy, format: format)
    }
}
