// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zwp_confined_pointer_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol ZwpConfinedPointerV1Events: AnyObject {
    func confined(_ proxy: OpaquePointer)
    func unconfined(_ proxy: OpaquePointer)
}

public enum ZwpConfinedPointerV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zwp_confined_pointer_v1_listener> = {
        let p = UnsafeMutablePointer<zwp_confined_pointer_v1_listener>.allocate(capacity: 1)
        p.initialize(to: zwp_confined_pointer_v1_listener())
        p.pointee.confined = confined_impl
        p.pointee.unconfined = unconfined_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zwp_confined_pointer_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> ZwpConfinedPointerV1Events? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? ZwpConfinedPointerV1Events
    }

    private static let confined_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.confined(proxy)
    }
    private static let unconfined_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.unconfined(proxy)
    }
}
