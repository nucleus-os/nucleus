// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zwp_linux_dmabuf_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol ZwpLinuxDmabufV1Events: AnyObject {
    func format(_ proxy: OpaquePointer, format: UInt32)
    func modifier(_ proxy: OpaquePointer, format: UInt32, modifier_hi: UInt32, modifier_lo: UInt32)
}

public enum ZwpLinuxDmabufV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zwp_linux_dmabuf_v1_listener> = {
        let p = UnsafeMutablePointer<zwp_linux_dmabuf_v1_listener>.allocate(capacity: 1)
        p.initialize(to: zwp_linux_dmabuf_v1_listener())
        p.pointee.format = format_impl
        p.pointee.modifier = modifier_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zwp_linux_dmabuf_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> ZwpLinuxDmabufV1Events? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? ZwpLinuxDmabufV1Events
    }

    private static let format_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, format in
        guard let data, let proxy, let h = handler(data) else { return }
        h.format(proxy, format: format)
    }
    private static let modifier_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32, UInt32, UInt32) -> Void = { data, proxy, format, modifier_hi, modifier_lo in
        guard let data, let proxy, let h = handler(data) else { return }
        h.modifier(proxy, format: format, modifier_hi: modifier_hi, modifier_lo: modifier_lo)
    }
}
