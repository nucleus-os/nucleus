// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for ext_workspace_group_handle_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ExtWorkspaceGroupHandleV1Events: AnyObject {
    func capabilities(_ proxy: OpaquePointer, capabilities: UInt32)
    func outputEnter(_ proxy: OpaquePointer, output: OpaquePointer?)
    func outputLeave(_ proxy: OpaquePointer, output: OpaquePointer?)
    func workspaceEnter(_ proxy: OpaquePointer, workspace: OpaquePointer?)
    func workspaceLeave(_ proxy: OpaquePointer, workspace: OpaquePointer?)
    func removed(_ proxy: OpaquePointer)
}

public enum ExtWorkspaceGroupHandleV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<ext_workspace_group_handle_v1_listener> = {
        let p = UnsafeMutablePointer<ext_workspace_group_handle_v1_listener>.allocate(capacity: 1)
        p.initialize(to: ext_workspace_group_handle_v1_listener())
        p.pointee.capabilities = capabilities_impl
        p.pointee.output_enter = outputEnter_impl
        p.pointee.output_leave = outputLeave_impl
        p.pointee.workspace_enter = workspaceEnter_impl
        p.pointee.workspace_leave = workspaceLeave_impl
        p.pointee.removed = removed_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        ext_workspace_group_handle_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ExtWorkspaceGroupHandleV1Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ExtWorkspaceGroupHandleV1Events
    }

    private static let capabilities_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, capabilities in
        guard let data, let proxy, let h = handler(data) else { return }
        h.capabilities(proxy, capabilities: capabilities)
    }
    private static let outputEnter_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, output in
        guard let data, let proxy, let h = handler(data) else { return }
        h.outputEnter(proxy, output: output)
    }
    private static let outputLeave_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, output in
        guard let data, let proxy, let h = handler(data) else { return }
        h.outputLeave(proxy, output: output)
    }
    private static let workspaceEnter_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, workspace in
        guard let data, let proxy, let h = handler(data) else { return }
        h.workspaceEnter(proxy, workspace: workspace)
    }
    private static let workspaceLeave_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, workspace in
        guard let data, let proxy, let h = handler(data) else { return }
        h.workspaceLeave(proxy, workspace: workspace)
    }
    private static let removed_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.removed(proxy)
    }
}
