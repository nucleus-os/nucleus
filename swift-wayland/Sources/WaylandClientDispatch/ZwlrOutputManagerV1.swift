// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zwlr_output_manager_v1: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

public import WaylandClientC

public protocol ZwlrOutputManagerV1Events: AnyObject {
    func head(_ proxy: OpaquePointer, head: OpaquePointer?)
    func done(_ proxy: OpaquePointer, serial: UInt32)
    func finished(_ proxy: OpaquePointer)
}

public enum ZwlrOutputManagerV1Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zwlr_output_manager_v1_listener> = {
        let p = UnsafeMutablePointer<zwlr_output_manager_v1_listener>.allocate(capacity: 1)
        p.initialize(to: zwlr_output_manager_v1_listener())
        p.pointee.head = head_impl
        p.pointee.done = done_impl
        p.pointee.finished = finished_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zwlr_output_manager_v1_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> (any ZwlrOutputManagerV1Events)? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? any ZwlrOutputManagerV1Events
    }

    private static let head_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, OpaquePointer?) -> Void = { data, proxy, head in
        guard let data, let proxy, let h = handler(data) else { return }
        h.head(proxy, head: head)
    }
    private static let done_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, serial in
        guard let data, let proxy, let h = handler(data) else { return }
        h.done(proxy, serial: serial)
    }
    private static let finished_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?) -> Void = { data, proxy in
        guard let data, let proxy, let h = handler(data) else { return }
        h.finished(proxy)
    }
}
