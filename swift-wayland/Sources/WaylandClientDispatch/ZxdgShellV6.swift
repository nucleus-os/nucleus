// Generated by SwiftWaylandGen. Do not edit.
//
// Typed client dispatch for zxdg_shell_v6: a handler protocol (one method per event), the
// libwayland listener + owner recovery + arg marshalling, and an addListener that wires it.

import WaylandClientC

public protocol ZxdgShellV6Events: AnyObject {
    func ping(_ proxy: OpaquePointer, serial: UInt32)
}

public enum ZxdgShellV6Client {
    public nonisolated(unsafe) static let listener: UnsafeMutablePointer<zxdg_shell_v6_listener> = {
        let p = UnsafeMutablePointer<zxdg_shell_v6_listener>.allocate(capacity: 1)
        p.initialize(to: zxdg_shell_v6_listener())
        p.pointee.ping = ping_impl
        return p
    }()

    /// Wire the listener to a proxy. The owner is borrowed (unretained) — the caller must
    /// keep it alive for the proxy's lifetime, matching libwayland's user_data contract.
    @discardableResult
    public static func addListener(_ proxy: OpaquePointer, owner: AnyObject) -> Int32 {
        zxdg_shell_v6_add_listener(proxy, listener, Unmanaged.passUnretained(owner).toOpaque())
    }

    private static func handler(_ data: UnsafeMutableRawPointer) -> ZxdgShellV6Events? {
        Unmanaged<AnyObject>.fromOpaque(data).takeUnretainedValue() as? ZxdgShellV6Events
    }

    private static let ping_impl: @convention(c) (UnsafeMutableRawPointer?, OpaquePointer?, UInt32) -> Void = { data, proxy, serial in
        guard let data, let proxy, let h = handler(data) else { return }
        h.ping(proxy, serial: serial)
    }
}
