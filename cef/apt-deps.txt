# Host packages needed to bootstrap the CEF/Chromium source build.
# The bulk of Chromium's build dependencies are installed by Chromium's own
# build/install-build-deps.sh (run by `collider bootstrap browser` after
# source preparation); these are the tools needed before that point.
git
curl
python3
pkg-config
lsb-release
