diff --git a/src/dawn/native/vulkan/PhysicalDeviceVk.cpp b/src/dawn/native/vulkan/PhysicalDeviceVk.cpp
index a96eae987..20bd6aa50 100644
--- a/src/dawn/native/vulkan/PhysicalDeviceVk.cpp
+++ b/src/dawn/native/vulkan/PhysicalDeviceVk.cpp
@@ -472,6 +472,20 @@ void PhysicalDevice::InitializeSupportedFeaturesImpl() {
         EnableFeature(Feature::MultiPlanarFormatExtendedUsages);
     }
 
+    VkFormatProperties p010Properties;
+    mVulkanInstance->GetFunctions().GetPhysicalDeviceFormatProperties(
+        mVkPhysicalDevice, VK_FORMAT_G10X6_B10X6R10X6_2PLANE_420_UNORM_3PACK16,
+        &p010Properties);
+    constexpr auto kRequiredP010Features =
+        static_cast<VkFormatFeatureFlags>(VK_FORMAT_FEATURE_SAMPLED_IMAGE_BIT |
+                                          VK_FORMAT_FEATURE_SAMPLED_IMAGE_FILTER_LINEAR_BIT |
+                                          VK_FORMAT_FEATURE_TRANSFER_SRC_BIT |
+                                          VK_FORMAT_FEATURE_TRANSFER_DST_BIT);
+    if (unorm16FormatsFilterabilitySupported &&
+        IsSubset(kRequiredP010Features, p010Properties.optimalTilingFeatures)) {
+        EnableFeature(Feature::MultiPlanarFormatP010);
+    }
+
     EnableFeature(Feature::TransientAttachments);
     EnableFeature(Feature::AdapterPropertiesVk);
     EnableFeature(Feature::DawnLoadResolveTexture);
diff --git a/src/dawn/native/vulkan/SharedTextureMemoryVk.cpp b/src/dawn/native/vulkan/SharedTextureMemoryVk.cpp
index eec5e3a7c..ab0f225b9 100644
--- a/src/dawn/native/vulkan/SharedTextureMemoryVk.cpp
+++ b/src/dawn/native/vulkan/SharedTextureMemoryVk.cpp
@@ -45,6 +45,10 @@
 #include "src/dawn/utils/SystemHandle.h"
 #include "src/utils/compiler.h"
 
+#if DAWN_PLATFORM_IS(LINUX)
+#include <sys/stat.h>
+#endif
+
 #if DAWN_PLATFORM_IS(ANDROID)
 #include <android/hardware_buffer.h>
 
@@ -83,6 +87,8 @@ constexpr auto kDrmFormatABGR2101010 =
 constexpr auto kDrmFormatABGR16161616F =
     DrmFourccCode('A', 'B', '4', 'H'); /* [63:0] A:B:G:R 16:16:16:16 little endian */
 constexpr auto kDrmFormatNV12 = DrmFourccCode('N', 'V', '1', '2'); /* 2x2 subsampled Cr:Cb plane */
+constexpr auto kDrmFormatP010 =
+    DrmFourccCode('P', '0', '1', '0'); /* 2x2 subsampled Cr:Cb 10-bit plane */
 
 ResultOrError<wgpu::TextureFormat> FormatFromDrmFormat(uint32_t drmFormat) {
     switch (drmFormat) {
@@ -102,6 +108,8 @@ ResultOrError<wgpu::TextureFormat> FormatFromDrmFormat(uint32_t drmFormat) {
             return wgpu::TextureFormat::RGBA16Float;
         case kDrmFormatNV12:
             return wgpu::TextureFormat::R8BG8Biplanar420Unorm;
+        case kDrmFormatP010:
+            return wgpu::TextureFormat::R10X6BG10X6Biplanar420Unorm;
         default:
             return DAWN_VALIDATION_ERROR("Unsupported drm format %x.", drmFormat);
     }
@@ -266,6 +274,44 @@ ResultOrError<Ref<SharedTextureMemory>> SharedTextureMemory::Create(
     VkImageCreateFlags vkCreateFlags =
         VulkanImageCreateFlags(device, properties.usage, *internalFormat, /*sampleCount=*/1);
 
+    DAWN_INVALID_IF(descriptor->planeCount == 0, "DMA-BUF plane count must not be 0.");
+    DAWN_INVALID_IF(descriptor->planeCount > kMaxPlanesPerFormat,
+                    "DMA-BUF plane count (%u) must not exceed %u.", descriptor->planeCount,
+                    kMaxPlanesPerFormat);
+
+    std::array<struct stat, kMaxPlanesPerFormat> planeStats{};
+    for (uint32_t plane = 0; plane < descriptor->planeCount; ++plane) {
+        DAWN_UNSAFE_TODO(
+            DAWN_INVALID_IF(fstat(descriptor->planes[plane].fd, &planeStats[plane]) != 0,
+                            "Unable to inspect dma-buf plane %u.", plane));
+    }
+
+    bool isDisjoint = false;
+    for (uint32_t plane = 1; plane < descriptor->planeCount; ++plane) {
+        if (planeStats[plane].st_dev != planeStats[0].st_dev ||
+            planeStats[plane].st_ino != planeStats[0].st_ino) {
+            isDisjoint = true;
+            break;
+        }
+    }
+    if (isDisjoint) {
+        DAWN_INVALID_IF(!internalFormat->IsMultiPlanar(),
+                        "Disjoint dma-buf import requires a multi-planar format.");
+        for (uint32_t plane = 0; plane < descriptor->planeCount; ++plane) {
+            DAWN_UNSAFE_TODO(
+                DAWN_INVALID_IF(descriptor->planes[plane].offset != 0,
+                                "Disjoint dma-buf plane %u has non-zero offset %llu.", plane,
+                                static_cast<unsigned long long>(descriptor->planes[plane].offset)));
+            for (uint32_t other = plane + 1; other < descriptor->planeCount; ++other) {
+                DAWN_INVALID_IF(planeStats[plane].st_dev == planeStats[other].st_dev &&
+                                    planeStats[plane].st_ino == planeStats[other].st_ino,
+                                "Disjoint dma-buf planes %u and %u use the same memory object.",
+                                plane, other);
+            }
+        }
+        vkCreateFlags |= VK_IMAGE_CREATE_DISJOINT_BIT;
+    }
+
     // Number of memory planes in the image which will be queried from the DRM modifier.
     uint32_t memoryPlaneCount;
 
@@ -295,6 +341,9 @@ ResultOrError<Ref<SharedTextureMemory>> SharedTextureMemory::Create(
         DAWN_TRY_ASSIGN(drmModifierProps,
                         GetFormatModifierProps(device->fn, vkPhysicalDevice, vkFormat,
                                                descriptor->drmModifier));
+        DAWN_INVALID_IF(isDisjoint && (drmModifierProps.drmFormatModifierTilingFeatures &
+                                       VK_FORMAT_FEATURE_DISJOINT_BIT) == 0,
+                        "DRM format modifier does not support disjoint plane binding.");
         memoryPlaneCount = drmModifierProps.drmFormatModifierPlaneCount;
         if (drmModifierProps.drmFormatModifier == 0 /* DRM_FORMAT_MOD_LINEAR */) {
             uint32_t formatPlaneCount = GetAspectCount(internalFormat->aspects);
@@ -384,19 +433,6 @@ ResultOrError<Ref<SharedTextureMemory>> SharedTextureMemory::Create(
         }
     }
 
-    // Validate that there is a single FD. If there is more than one FD, Dawn will need to validate
-    // the format has VK_FORMAT_FEATURE_DISJOINT_BIT, create the VkImage with
-    // VK_IMAGE_CREATE_DISJOINT_BIT, and separately bind the image planes to memory. Dawn doesn't
-    // support use of VK_IMAGE_CREATE_DISJOINT_BIT currently. See crbug.com/42240514.
-    int fd = descriptor->planes[0].fd;
-    for (uint32_t i = 1; i < descriptor->planeCount; ++i) {
-        DAWN_UNSAFE_TODO(DAWN_INVALID_IF(
-            descriptor->planes[i].fd != fd,
-            "descriptor->planes[%u].fd (%i) does not match other plane fd (%i). All "
-            "fds must be the same.",
-            i, descriptor->planes[i].fd, fd));
-    }
-
     // Don't add the view format if backend validation is enabled, otherwise most image creations
     // will fail with VVL. This view format is only needed for sRGB reinterpretation.
     // TODO(crbug.com/dawn/2304): Investigate if this is a bug in VVL.
@@ -433,54 +469,82 @@ ResultOrError<Ref<SharedTextureMemory>> SharedTextureMemory::Create(
             AcquireRef(new RefCountedVkHandle<VkImage>(device, vkImage));
     }
 
-    // Import the memory plane(s) as VkDeviceMemory and bind to the VkImage.
-    VkMemoryFdPropertiesKHR fdProperties;
-    fdProperties.sType = VK_STRUCTURE_TYPE_MEMORY_FD_PROPERTIES_KHR;
-    fdProperties.pNext = nullptr;
-
-    // Get the valid memory types that the external memory can be imported as.
-    DAWN_TRY(CheckVkSuccess(device->fn.GetMemoryFdPropertiesKHR(
-                                vkDevice, handleType, descriptor->planes[0].fd, &fdProperties),
-                            "vkGetMemoryFdPropertiesKHR"));
-
-    // Get the valid memory types for the VkImage.
-    VkMemoryRequirements memoryRequirements;
-    device->fn.GetImageMemoryRequirements(vkDevice, sharedTextureMemory->mVkImage->Get(),
-                                          &memoryRequirements);
-
-    // Choose the best memory type that satisfies both the image's constraint and the
-    // import's constraint.
-    memoryRequirements.memoryTypeBits &= fdProperties.memoryTypeBits;
-    int memoryTypeIndex = device->GetResourceMemoryAllocator()->FindBestTypeIndex(
-        memoryRequirements, MemoryKind::DeviceLocal);
-    DAWN_INVALID_IF(memoryTypeIndex == -1, "Unable to find an appropriate memory type for import.");
-
-    utils::SystemHandle memoryFD = utils::SystemHandle::Duplicate(fd);
-
-    VkMemoryAllocateInfo memoryAllocateInfo = {};
-    memoryAllocateInfo.sType = VK_STRUCTURE_TYPE_MEMORY_ALLOCATE_INFO;
-    memoryAllocateInfo.allocationSize = memoryRequirements.size;
-    memoryAllocateInfo.memoryTypeIndex = memoryTypeIndex;
-
-    VkImportMemoryFdInfoKHR importMemoryFdInfo;
-    importMemoryFdInfo.sType = VK_STRUCTURE_TYPE_IMPORT_MEMORY_FD_INFO_KHR;
-    importMemoryFdInfo.handleType = handleType;
-    importMemoryFdInfo.fd = memoryFD.Get();
-
-    // Import the fd as VkDeviceMemory
-    VkDeviceMemory vkDeviceMemory;
-    DAWN_TRY_ASSIGN(vkDeviceMemory,
-                    AllocateDeviceMemory(device, &memoryAllocateInfo, &importMemoryFdInfo));
+    constexpr std::array<VkImageAspectFlagBits, kMaxPlanesPerFormat> kMemoryPlaneAspects = {
+        VK_IMAGE_ASPECT_MEMORY_PLANE_0_BIT_EXT, VK_IMAGE_ASPECT_MEMORY_PLANE_1_BIT_EXT,
+        VK_IMAGE_ASPECT_MEMORY_PLANE_2_BIT_EXT};
+    const uint32_t memoryImportCount = isDisjoint ? memoryPlaneCount : 1;
+    std::array<VkBindImageMemoryInfo, kMaxPlanesPerFormat> bindInfos{};
+    std::array<VkBindImagePlaneMemoryInfo, kMaxPlanesPerFormat> bindPlaneInfos{};
+
+    for (uint32_t memoryPlane = 0; memoryPlane < memoryImportCount; ++memoryPlane) {
+        VkMemoryRequirements memoryRequirements;
+        if (isDisjoint) {
+            VkImagePlaneMemoryRequirementsInfo planeRequirements = {};
+            planeRequirements.sType = VK_STRUCTURE_TYPE_IMAGE_PLANE_MEMORY_REQUIREMENTS_INFO;
+            planeRequirements.planeAspect = kMemoryPlaneAspects[memoryPlane];
+
+            VkImageMemoryRequirementsInfo2 imageRequirements = {};
+            imageRequirements.sType = VK_STRUCTURE_TYPE_IMAGE_MEMORY_REQUIREMENTS_INFO_2;
+            imageRequirements.pNext = &planeRequirements;
+            imageRequirements.image = sharedTextureMemory->mVkImage->Get();
+
+            VkMemoryRequirements2 memoryRequirements2 = {};
+            memoryRequirements2.sType = VK_STRUCTURE_TYPE_MEMORY_REQUIREMENTS_2;
+            device->fn.GetImageMemoryRequirements2(vkDevice, &imageRequirements,
+                                                   &memoryRequirements2);
+            memoryRequirements = memoryRequirements2.memoryRequirements;
+        } else {
+            device->fn.GetImageMemoryRequirements(vkDevice, sharedTextureMemory->mVkImage->Get(),
+                                                  &memoryRequirements);
+        }
 
-    memoryFD.Detach();  // Ownership transfered to the VkDeviceMemory.
-    sharedTextureMemory->mVkDeviceMemory =
-        AcquireRef(new RefCountedVkHandle<VkDeviceMemory>(device, vkDeviceMemory));
+        const int memoryPlaneFd = DAWN_UNSAFE_TODO(descriptor->planes[memoryPlane]).fd;
+        VkMemoryFdPropertiesKHR fdProperties = {};
+        fdProperties.sType = VK_STRUCTURE_TYPE_MEMORY_FD_PROPERTIES_KHR;
+        DAWN_TRY(CheckVkSuccess(
+            device->fn.GetMemoryFdPropertiesKHR(vkDevice, handleType, memoryPlaneFd, &fdProperties),
+            "vkGetMemoryFdPropertiesKHR"));
+
+        memoryRequirements.memoryTypeBits &= fdProperties.memoryTypeBits;
+        int memoryTypeIndex = device->GetResourceMemoryAllocator()->FindBestTypeIndex(
+            memoryRequirements, MemoryKind::DeviceLocal);
+        DAWN_INVALID_IF(memoryTypeIndex == -1,
+                        "Unable to find an appropriate memory type for dma-buf plane %u.",
+                        memoryPlane);
+
+        utils::SystemHandle memoryFD = utils::SystemHandle::Duplicate(memoryPlaneFd);
+
+        VkMemoryAllocateInfo memoryAllocateInfo = {};
+        memoryAllocateInfo.sType = VK_STRUCTURE_TYPE_MEMORY_ALLOCATE_INFO;
+        memoryAllocateInfo.allocationSize = memoryRequirements.size;
+        memoryAllocateInfo.memoryTypeIndex = memoryTypeIndex;
+
+        VkImportMemoryFdInfoKHR importMemoryFdInfo = {};
+        importMemoryFdInfo.sType = VK_STRUCTURE_TYPE_IMPORT_MEMORY_FD_INFO_KHR;
+        importMemoryFdInfo.handleType = handleType;
+        importMemoryFdInfo.fd = memoryFD.Get();
+
+        VkDeviceMemory vkDeviceMemory;
+        DAWN_TRY_ASSIGN(vkDeviceMemory,
+                        AllocateDeviceMemory(device, &memoryAllocateInfo, &importMemoryFdInfo));
+        memoryFD.Detach();  // Ownership transferred to VkDeviceMemory.
+        sharedTextureMemory->mVkDeviceMemories.push_back(
+            AcquireRef(new RefCountedVkHandle<VkDeviceMemory>(device, vkDeviceMemory)));
+
+        bindInfos[memoryPlane].sType = VK_STRUCTURE_TYPE_BIND_IMAGE_MEMORY_INFO;
+        bindInfos[memoryPlane].image = sharedTextureMemory->mVkImage->Get();
+        bindInfos[memoryPlane].memory = sharedTextureMemory->mVkDeviceMemories[memoryPlane]->Get();
+        bindInfos[memoryPlane].memoryOffset = 0;
+        if (isDisjoint) {
+            bindPlaneInfos[memoryPlane].sType = VK_STRUCTURE_TYPE_BIND_IMAGE_PLANE_MEMORY_INFO;
+            bindPlaneInfos[memoryPlane].planeAspect = kMemoryPlaneAspects[memoryPlane];
+            bindInfos[memoryPlane].pNext = &bindPlaneInfos[memoryPlane];
+        }
+    }
 
-    // Bind the VkImage to the memory.
     DAWN_TRY(
-        CheckVkSuccess(device->fn.BindImageMemory(vkDevice, sharedTextureMemory->mVkImage->Get(),
-                                                  sharedTextureMemory->mVkDeviceMemory->Get(), 0),
-                       "vkBindImageMemory"));
+        CheckVkSuccess(device->fn.BindImageMemory2(vkDevice, memoryImportCount, bindInfos.data()),
+                       "vkBindImageMemory2"));
     return sharedTextureMemory;
 #else
     DAWN_UNREACHABLE();
@@ -713,13 +777,13 @@ ResultOrError<Ref<SharedTextureMemory>> SharedTextureMemory::Create(
                         AllocateDeviceMemory(device, &memoryAllocateInfo, &dedicatedAllocateInfo,
                                              &importMemoryAHBInfo));
 
-        sharedTextureMemory->mVkDeviceMemory =
-            AcquireRef(new RefCountedVkHandle<VkDeviceMemory>(device, vkDeviceMemory));
+        sharedTextureMemory->mVkDeviceMemories.push_back(
+            AcquireRef(new RefCountedVkHandle<VkDeviceMemory>(device, vkDeviceMemory)));
 
         // Bind the VkImage to the memory.
         DAWN_TRY(CheckVkSuccess(
             device->fn.BindImageMemory(vkDevice, sharedTextureMemory->mVkImage->Get(),
-                                       sharedTextureMemory->mVkDeviceMemory->Get(), 0),
+                                       sharedTextureMemory->mVkDeviceMemories[0]->Get(), 0),
             "vkBindImageMemory"));
 
         // Verify the texture memory requirements fit within the constraints of the AHardwareBuffer.
@@ -949,13 +1013,13 @@ ResultOrError<Ref<SharedTextureMemory>> SharedTextureMemory::Create(
         }
 
         memoryFD.Detach();  // Ownership transfered to the VkDeviceMemory.
-        sharedTextureMemory->mVkDeviceMemory =
-            AcquireRef(new RefCountedVkHandle<VkDeviceMemory>(device, vkDeviceMemory));
+        sharedTextureMemory->mVkDeviceMemories.push_back(
+            AcquireRef(new RefCountedVkHandle<VkDeviceMemory>(device, vkDeviceMemory)));
 
         // Bind the VkImage to the memory.
         DAWN_TRY(CheckVkSuccess(
             device->fn.BindImageMemory(vkDevice, sharedTextureMemory->mVkImage->Get(),
-                                       sharedTextureMemory->mVkDeviceMemory->Get(), 0),
+                                       sharedTextureMemory->mVkDeviceMemories[0]->Get(), 0),
             "vkBindImageMemory"));
     }
     return sharedTextureMemory;
@@ -986,8 +1050,9 @@ SharedTextureMemory::SharedTextureMemory(Device* device,
       mQueueFamilyIndex(queueFamilyIndex),
       mYCbCrVkDesc(yCbCrVkDesc) {}
 
-RefCountedVkHandle<VkDeviceMemory>* SharedTextureMemory::GetVkDeviceMemory() const {
-    return mVkDeviceMemory.Get();
+const std::vector<Ref<RefCountedVkHandle<VkDeviceMemory>>>&
+SharedTextureMemory::GetVkDeviceMemories() const {
+    return mVkDeviceMemories;
 }
 
 RefCountedVkHandle<VkImage>* SharedTextureMemory::GetVkImage() const {
@@ -1000,7 +1065,7 @@ uint32_t SharedTextureMemory::GetQueueFamilyIndex() const {
 
 void SharedTextureMemory::DestroyImpl(DestroyReason reason) {
     mVkImage = nullptr;
-    mVkDeviceMemory = nullptr;
+    mVkDeviceMemories.clear();
 }
 
 Ref<SharedResourceMemoryContents> SharedTextureMemory::CreateContents() {
diff --git a/src/dawn/native/vulkan/SharedTextureMemoryVk.h b/src/dawn/native/vulkan/SharedTextureMemoryVk.h
index 8d2b848f0..acc34cd92 100644
--- a/src/dawn/native/vulkan/SharedTextureMemoryVk.h
+++ b/src/dawn/native/vulkan/SharedTextureMemoryVk.h
@@ -28,6 +28,8 @@
 #ifndef SRC_DAWN_NATIVE_VULKAN_SHAREDTEXTUREMEMORYVK_H_
 #define SRC_DAWN_NATIVE_VULKAN_SHAREDTEXTUREMEMORYVK_H_
 
+#include <vector>
+
 #include "src/dawn/common/vulkan_platform.h"
 #include "src/dawn/native/Error.h"
 #include "src/dawn/native/SharedTextureMemory.h"
@@ -68,7 +70,7 @@ class SharedTextureMemory final : public SharedTextureMemoryBase {
         StringView label,
         const SharedTextureMemoryOpaqueFDDescriptor* descriptor);
 
-    RefCountedVkHandle<VkDeviceMemory>* GetVkDeviceMemory() const;
+    const std::vector<Ref<RefCountedVkHandle<VkDeviceMemory>>>& GetVkDeviceMemories() const;
     RefCountedVkHandle<VkImage>* GetVkImage() const;
     uint32_t GetQueueFamilyIndex() const;
 
@@ -100,7 +102,7 @@ class SharedTextureMemory final : public SharedTextureMemoryBase {
         UnpackedPtr<SharedTextureMemoryProperties>& properties) const override;
 
     Ref<RefCountedVkHandle<VkImage>> mVkImage;
-    Ref<RefCountedVkHandle<VkDeviceMemory>> mVkDeviceMemory;
+    std::vector<Ref<RefCountedVkHandle<VkDeviceMemory>>> mVkDeviceMemories;
     const uint32_t mQueueFamilyIndex;
 
     // Populated if this instance was created from an AHardwareBuffer.
diff --git a/src/dawn/native/vulkan/TextureVk.cpp b/src/dawn/native/vulkan/TextureVk.cpp
index 83a12e601..3759c2d01 100644
--- a/src/dawn/native/vulkan/TextureVk.cpp
+++ b/src/dawn/native/vulkan/TextureVk.cpp
@@ -2064,7 +2064,7 @@ void SharedTexture::DestroyImpl(DestroyReason reason) {
 
 void SharedTexture::Initialize(SharedTextureMemory* memory) {
     mSharedResourceMemoryContents = memory->GetContents();
-    mSharedTextureMemoryObjects = {memory->GetVkImage(), memory->GetVkDeviceMemory()};
+    mSharedTextureMemoryObjects = {memory->GetVkImage(), memory->GetVkDeviceMemories()};
     mHandle = mSharedTextureMemoryObjects.vkImage->Get();
     mExportQueueFamilyIndex = memory->GetQueueFamilyIndex();
 }
diff --git a/src/dawn/native/vulkan/TextureVk.h b/src/dawn/native/vulkan/TextureVk.h
index 82b1906bf..596d556cb 100644
--- a/src/dawn/native/vulkan/TextureVk.h
+++ b/src/dawn/native/vulkan/TextureVk.h
@@ -317,7 +317,7 @@ class SharedTexture final : public ImportedTextureBase {
 
     struct SharedTextureMemoryObjects {
         Ref<RefCountedVkHandle<VkImage>> vkImage;
-        Ref<RefCountedVkHandle<VkDeviceMemory>> vkDeviceMemory;
+        std::vector<Ref<RefCountedVkHandle<VkDeviceMemory>>> vkDeviceMemories;
     };
     SharedTextureMemoryObjects mSharedTextureMemoryObjects;
 };
